[
  {
    "id": "PYV130_A1_ENV_VARS",
    "title": "환경변수 기본 감각",
    "body": "환경변수는 코드 밖에서 프로그램에 설정값을 전달하는 방법이다. 같은 코드라도 개발 환경, 테스트 환경, 배포 환경에서 다른 값을 넣어 실행할 수 있어 설정 관리가 쉬워진다.",
    "detail": "예를 들어 API 주소, 실행 모드, 로그 레벨 같은 값은 코드에 고정하기보다 환경변수로 읽으면 바꾸기 쉽다. Python에서는 os.environ.get('NAME') 형태로 값을 읽을 수 있다. 기본값이 필요한 설정은 os.environ.get('APP_MODE', 'dev')처럼 쓸 수 있고, 반드시 필요한 값은 없을 때 SystemExit로 멈추게 만들 수 있다.",
    "examples": [
      "mode = os.environ.get('APP_MODE', 'dev')",
      "base_url = os.environ.get('API_BASE_URL')"
    ],
    "related_concepts": [
      "environment variable",
      "os.environ",
      "config",
      "default value",
      "SystemExit"
    ]
  },
  {
    "id": "PYV130_A1_SECRET_HANDLING",
    "title": "API Key와 비밀값 다루기",
    "body": "API Key, 토큰, 비밀번호 같은 값은 코드에 직접 쓰지 않는 것이 기본이다. 이런 값은 GitHub, 캡처, 공유 파일을 통해 새어 나가면 비용이나 권한 문제가 생길 수 있다.",
    "detail": "비밀값은 코드와 분리해 환경변수, 서버 비밀 저장소, 로컬 .env 파일 등에서 읽게 만든다. 특히 공개 저장소나 포트폴리오 프로젝트에서는 실제 키를 절대 넣지 말고, README에는 OPENAI_API_KEY=your_key_here처럼 예시만 적는 편이 안전하다. 프로그램 시작 시 필요한 키가 없으면 요청을 보내기 전에 쉬운 메시지로 멈추게 하는 습관도 중요하다.",
    "examples": [
      "api_key = os.environ.get('OPENAI_API_KEY')",
      "if not api_key: raise SystemExit('OPENAI_API_KEY를 설정하세요')"
    ],
    "related_concepts": [
      "API key",
      "secret",
      "token",
      "environment variable",
      "GitHub"
    ]
  },
  {
    "id": "PYV130_A1_DOTENV_FLOW",
    "title": ".env와 load_dotenv 흐름",
    "body": "load_dotenv()는 .env 값을 환경변수처럼 읽게 하며 기본적으로 이미 존재하는 같은 이름의 환경변수를 덮어쓰지 않는다. 로컬 편의용 파일일 뿐 비밀 저장소 자체는 아니므로 Git 제외와 파일 권한을 확인한다. 배포에서는 플랫폼 secret 설정을 우선한다.",
    "detail": "load_dotenv()는 .env 값을 환경변수처럼 읽게 하며 기본적으로 이미 존재하는 같은 이름의 환경변수를 덮어쓰지 않는다. 로컬 편의용 파일일 뿐 비밀 저장소 자체는 아니므로 Git 제외와 파일 권한을 확인한다. 배포에서는 플랫폼 secret 설정을 우선한다.",
    "examples": [
      "load_dotenv()",
      "api_key = os.environ.get('OPENAI_API_KEY')"
    ],
    "related_concepts": [
      ".env",
      "python-dotenv",
      "load_dotenv",
      "local development",
      "os.environ"
    ]
  },
  {
    "id": "PYV130_A1_GITIGNORE_ENV",
    "title": ".env와 .gitignore",
    "body": ".gitignore는 아직 추적하지 않은 .env가 새로 커밋되는 위험을 줄인다. 이미 추적된 파일이나 과거 commit에서는 비밀을 지우지 못하므로 키를 즉시 폐기·교체하고 필요한 기록 정리 절차를 따로 진행한다.",
    "detail": ".gitignore는 아직 추적하지 않은 .env가 새로 커밋되는 위험을 줄인다. 이미 추적된 파일이나 과거 commit에서는 비밀을 지우지 못하므로 키를 즉시 폐기·교체하고 필요한 기록 정리 절차를 따로 진행한다.",
    "examples": [
      ".env",
      ".env.local"
    ],
    "related_concepts": [
      ".gitignore",
      ".env",
      "secret",
      "Git",
      "repository hygiene"
    ]
  }
]
