[
  {
    "id": "PYV126_A1_ARGPARSE_PATH_OBJECT",
    "title": "Linking argparse Values to Path Objects",
    "body": "File paths received via `argparse` are initially strings. By converting this value into a `Path` object—such as `Path(args.input)`—you can more easily work with path information, such as the filename, extension, parent folder, and whether the file exists.",
    "detail": "CLI tools are typically run like this: python tool.py --input data.txt --output out/result.txt. In this case, args.input and args.output are passed as strings. While you can use them as strings, converting them to Path objects allows you to naturally use functions such as `input_path.exists()`, `input_path.suffix`, and `output_path.parent`. As a beginner, it’s a good idea to get into the habit of treating paths not just as simple strings, but as objects for working with the file system.",
    "examples": [
      "input_path = Path(args.input)",
      "print(input_path.name, input_path.suffix)"
    ],
    "related_concepts": [
      "argparse",
      "Path",
      "pathlib",
      "file path",
      "CLI"
    ]
  },
  {
    "id": "PYV126_A1_EXISTS_SUFFIX_CHECK",
    "title": "Checking Input Using `exists` and `suffix`",
    "body": "Path.exists() checks whether a file or folder actually exists. Path.suffix reads file extensions such as .csv, .json, and .txt. By using these two together, you can first verify that the input file exists and determine its format.",
    "detail": "The first thing to do in a file processing tool is to verify that the input is valid. Calling `read_text()` when no file exists will result in an error, and it can be difficult for the user to understand why it failed. By checking for the file’s existence with `exists()` and verifying the file extension with `suffix`, you can determine whether to process only CSV, only JSON, or only text files. These checks make the program more user-friendly and secure.",
    "examples": [
      "if not input_path.exists(): raise SystemExit('파일 없음')",
      "if input_path.suffix != '.txt': raise SystemExit('Only .txt files are allowed')"
    ],
    "related_concepts": [
      "Path.exists",
      "Path.suffix",
      "validation",
      "file type",
      "SystemExit"
    ]
  },
  {
    "id": "PYV126_A1_READ_WRITE_TEXT",
    "title": "read_text, write_text, mkdir flow",
    "body": "`read_text()` reads a text file into a string, and `write_text()` writes a string to a file. Since the parent folder of the output path may not exist, it is safer to create the folder using `parent.mkdir()` before writing.",
    "detail": "Small CLI tools often read and process files before saving them again. `input_path.read_text(encoding='utf-8')` reads the input file as a string, and `output_path.write_text(result, encoding='utf-8')` saves the result string. However, if you want to save the file to a subfolder, such as `out/result.txt`, the `out` folder must already exist. Using `output_path.parent.mkdir(parents=True, exist_ok=True)` will create the folder even if intermediate folders are missing, and it won’t raise an error if the folder already exists.",
    "examples": [
      "text = input_path.read_text(encoding='utf-8')",
      "output_path.parent.mkdir(parents=True, exist_ok=True)"
    ],
    "related_concepts": [
      "read_text",
      "write_text",
      "mkdir",
      "encoding",
      "output path"
    ]
  },
  {
    "id": "PYV126_A1_FILE_TOOL_FLOW",
    "title": "Basic Workflow of the CLI for Processing Actual Files",
    "body": "The actual file-processing CLI is easier to understand if broken down into the following steps: parsing arguments, converting paths, validating input, reading files, processing them, creating an output folder, and saving the results. This step brings together the argparse, pathlib, regex, CSV, and JSON concepts we learned earlier.",
    "detail": "At first, it’s easy to write all the code in a single file. However, if you plan to use it repeatedly as a tool, it’s better to break the code into separate sections. `parse_args()` reads the command-line arguments, `Path(args.input)` creates a path object, and `exists()` checks for the input. Next, you can retrieve data using `read_text()` or by reading CSV/JSON files, perform the necessary processing, and then save the results using `write_text()` or by saving to a CSV file. Once you get the hang of this structure, you can turn small automation scripts into actual tools.",
    "examples": [
      "parse_args() -> Path conversion -> exists() -> read_text() -> write_text()",
      "python clean_text.py --input raw.txt --output out/clean.txt"
    ],
    "related_concepts": [
      "CLI tool",
      "parse_args",
      "pathlib",
      "file processing",
      "automation"
    ]
  }
]
