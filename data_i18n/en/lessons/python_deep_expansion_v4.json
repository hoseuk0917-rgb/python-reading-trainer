[
  {
    "id": "PY4_L03_append_extend_001",
    "level": 3,
    "title": "Understanding the Difference Between `append` and `extend`",
    "concepts": [
      "call",
      "attribute_access"
    ],
    "reading_goal": "Examine the actual behavior of the `call` statement in the focus expression and read how its result leads to the next code.",
    "code": "items = [\"A\"]\nitems.append([\"B\", \"C\"])\nprint(items)",
    "question_type": "output_prediction",
    "question": "What is the output?",
    "choices": [
      "[\"A\", \"B\", \"C\"]",
      "[\"B\", \"C\"]",
      "[\"A\", [\"B\", \"C\"]]",
      "Error"
    ],
    "answer": "[\"A\", [\"B\", \"C\"]]",
    "explanation": "1) `append` adds the passed value as a single item to the list. 2) When you `append` a list, the entire list is added as a single element. 3) The difference becomes clear when you compare this to `extend`, which unfolds the values in the list one by one. 4) The final output is ‘[\"A\", [\"B\", \"C\"]]’. Taking into account both the execution order and the changes in values, the correct answer is `[\"A\", [\"B\", \"C\"]]`. Common Misconceptions: [\"A\", \"B\", \"C\"] Don’t just look at the function or method names; you must check the input inside the parentheses and the return value or state change after the call in the actual order of the code. Points of Misinterpretation: If you selected this incorrect answer, double-check to see if you skipped reading one of the following steps: the input values for the `focus` expression, the intermediate results produced by the function/syntax, or the step where those results are used in the next line. For a call expression, prepare the input inside the parentheses and then verify the return value or state change produced by the function or method.",
    "side_card_ids": [
      "SC_list_why_used"
    ],
    "project_context": "If you confuse `append` and `extend` when merging batch results, you’ll encounter a nested list bug.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "call의 실제 Python 동작을 focus 표현식에서 확인하고, 그 결과가 다음 코드에 어떻게 이어지는지 읽는다.",
    "concept_explanation": {
      "what_it_is": "`append()` is a method that adds **the entire value passed to it** to the end of a list.",
      "how_to_read": "`items.append([\"B\", \"C\"])` In this case, although the value inside the parentheses is a list, the list is not unpacked; instead, it is passed as a single item to `items`.",
      "key_point": "`append(x)` Adds **one item** regardless of the value of x.",
      "common_mistake": "It’s easy to assume that B and C from the list in parentheses are added separately. That behavior is closer to `extend()`, whereas `append()` adds the entire `[\"B\", \"C\"]` as a single item."
    },
    "teaching_example": {
      "code": "items = [\"X\"]\nitems.append([\"Y\", \"Z\"])\nprint(items)",
      "walkthrough": "`append()` Since `[\"Y\",\"Z\"]` is treated as a single item, the result is nested as `[\"X\", [\"Y\", \"Z\"]]`."
    },
    "answer_explanation": {
      "step_by_step": "1) `append` adds the passed value as a single item to the list. 2) When you use `append` on a list, the entire list is added as a single element. 3) The difference becomes clear when you compare this to `extend`, which unfolds the values in the list one by one. 4) The final output is ‘[\"A\", [\"B\", \"C\"]]’.",
      "why_correct": "If we take into account both the execution order and the changes in values, the correct answer is `[\"A\", [\"B\", \"C\"]]`.",
      "common_wrong_choice": {
        "choice": "[\"A\", \"B\", \"C\"]",
        "why_wrong": "Don’t just look at function or method names; check the inputs inside parentheses and the return values or state changes following the call in the actual order of the code.",
        "misread_step": "If you selected this as the incorrect answer, double-check whether you skipped reading one of the following: the input value of the `focus` expression, an intermediate result produced by a function or syntax, or the step where that result is used on the next line."
      },
      "takeaway": "A call expression prepares the input inside the parentheses and then checks the return value or state change produced by the function or method."
    },
    "target_statement": "print(items)",
    "focus_span": "items.append([\"B\", \"C\"])",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "append",
      "nested list",
      "list mutation"
    ],
    "pedagogical_intent": "predict_output"
  },
  {
    "id": "PY4_L03_extend_001",
    "level": 3,
    "title": "Use `extend` to add the elements of a list individually",
    "concepts": [
      "call",
      "attribute_access"
    ],
    "reading_goal": "Examine the actual behavior of the `call` statement in the focus expression and read how its result leads to the next code.",
    "code": "items = [\"A\"]\nitems.extend([\"B\", \"C\"])\nprint(items)",
    "question_type": "output_prediction",
    "question": "What is the output?",
    "choices": [
      "[\"A\", [\"B\", \"C\"]]",
      "[\"B\", \"C\"]",
      "[\"A\", \"B\", \"C\"]",
      "Error"
    ],
    "answer": "[\"A\", \"B\", \"C\"]",
    "explanation": "1) `extend` expands the elements of the passed iterable and adds them one by one to the existing list. 2) It behaves differently from `append` when joining lists. 3) `append` can add the list itself as one item, while `extend` adds its inner elements individually. 4) The final output is `[\"A\", \"B\", \"C\"]`. Taking the execution order and state changes into account, the answer is `[\"A\", \"B\", \"C\"]`. Common wrong answer: `[\"A\", [\"B\", \"C\"]]`. Do not look only at the function or method name; inspect the input inside the parentheses and the actual return value or state change afterward in code order. Misreading point: If you chose this answer, recheck whether you skipped the input to the focus expression, an intermediate result produced by the function/syntax, or the step where that result is used next. For a call expression, prepare the input inside the parentheses, then inspect the return value or state change produced by the function or method.",
    "side_card_ids": [
      "SC_list_why_used"
    ],
    "project_context": "It is frequently used when combining the results of multiple shards into a single list.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "call의 실제 Python 동작을 focus 표현식에서 확인하고, 그 결과가 다음 코드에 어떻게 이어지는지 읽는다.",
    "concept_explanation": {
      "what_it_is": "`extend()` is a method that **appends** items from another iterable to a list, one at a time.",
      "how_to_read": "If the code is `items.extend([\"B\", \"C\"])`, it first examines the list that was passed and appends `\"B\"` and `\"C\"` to the end of the existing list, one after the other.",
      "key_point": "While `append()` adds a single value, `extend()` expands the contents to include multiple items.",
      "common_mistake": "It’s easy to assume that the entire expression is treated as a single item. In `extend()`, the two values inside the square brackets are added as separate items."
    },
    "teaching_example": {
      "code": "items = [\"X\"]\nitems.extend([\"Y\", \"Z\"])\nprint(items)",
      "walkthrough": "`extend()` extracts Y and Z from the passed list and appends them as the last two elements to the existing list."
    },
    "answer_explanation": {
      "step_by_step": "1) `extend` unfolds the elements of the iterable passed to it one by one and adds them to the existing list. 2) It behaves differently from `append` when concatenating lists. 3) The key difference is that `append` can add the list itself as a single item, whereas `extend` unpacks its elements and adds them individually. 4) The final output is ‘[\"A\", \"B\", \"C\"]’.",
      "why_correct": "If you take into account both the execution order and the changes in values, the correct answer is `[\"A\", \"B\", \"C\"]`.",
      "common_wrong_choice": {
        "choice": "[\"A\", [\"B\", \"C\"]]",
        "why_wrong": "Don’t just look at function or method names; check the inputs inside parentheses and the return values or state changes following the call in the actual order of the code.",
        "misread_step": "If you selected this as the incorrect answer, double-check whether you skipped reading one of the following: the input value of the `focus` expression, an intermediate result produced by a function or syntax, or the step where that result is used on the next line."
      },
      "takeaway": "A call expression prepares the input inside the parentheses and then checks the return value or state change produced by the function or method."
    },
    "target_statement": "print(items)",
    "focus_span": "items.extend([\"B\", \"C\"])",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "extend",
      "list mutation",
      "append comparison"
    ],
    "pedagogical_intent": "predict_output"
  },
  {
    "id": "PY4_L03_dict_update_001",
    "level": 3,
    "title": "Reading `dict.update()`",
    "concepts": [
      "call",
      "attribute_access"
    ],
    "reading_goal": "Examine the actual behavior of the `call` statement in the focus expression and read how its result leads to the next code.",
    "code": "meta = {\"source\": \"arXiv\", \"score\": 0.5}\nmeta.update({\"score\": 0.9, \"domain\": \"UAM\"})\nprint(meta[\"score\"])",
    "question_type": "output_prediction",
    "question": "What is the output?",
    "choices": [
      "0.5",
      "0.9",
      "UAM",
      "source"
    ],
    "answer": "0.9",
    "explanation": "1) dict. 2) `update` adds a new key-value pair to the existing dict or overwrites the value for the same key. 3) Therefore, the `score` value is 0. 4) It is updated to 9. If you account for both the execution order and the changes in values, the correct answer is `0.9`. Common misconceptions: 0.5 Do not just look at the function or method name; you must check the input inside the parentheses and the return value or state change after the call in the actual order of the code. Points of misinterpretation: If you selected this incorrect answer, double-check to see if you skipped reading one of the following steps: the input value of the `focus` expression, the intermediate result produced by the function/syntax, or the step where that result is used on the next line. For a call expression, prepare the input inside the parentheses, then verify the return value or state change produced by the function or method.",
    "side_card_ids": [
      "CS_hash_table_001"
    ],
    "project_context": "This is commonly seen in metadata enrichment, merging configuration values, and overriding default values.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "call의 실제 Python 동작을 focus 표현식에서 확인하고, 그 결과가 다음 코드에 어떻게 이어지는지 읽는다.",
    "concept_explanation": {
      "what_it_is": "`dict.update()` is a method that updates a dictionary with multiple key-value pairs at once.",
      "how_to_read": "New keys are added, and if an existing key appears again, its value is replaced with the new value. Even if `score` already exists, if a new `score` value is provided, it is updated to that value.",
      "key_point": "If the same key exists, **the value added later via an update becomes the current value**.",
      "common_mistake": "It is easy to assume that an `update` operation is ignored if the key already exists. In reality, it overwrites the value of the existing key and adds a new key if it does not exist."
    },
    "teaching_example": {
      "code": "meta = {\"score\": 1}\nmeta.update({\"score\": 2, \"mode\": \"auto\"})\nprint(meta)",
      "walkthrough": "The existing `score` key is changed to `2`, and a new `mode` key is added."
    },
    "answer_explanation": {
      "step_by_step": "1) dict. 2) `update` adds a new key-value pair to an existing dict or overwrites the value for the same key. 3) Therefore, the `score` value is 0. 4) It is updated to 9.",
      "why_correct": "If you take into account both the execution order and the changes in values, the correct answer is `0.9`.",
      "common_wrong_choice": {
        "choice": "0.5",
        "why_wrong": "Don’t just look at function or method names; check the inputs inside parentheses and the return values or state changes following the call in the actual order of the code.",
        "misread_step": "If you selected this as the incorrect answer, double-check whether you skipped reading one of the following: the input value of the `focus` expression, an intermediate result produced by a function or syntax, or the step where that result is used on the next line."
      },
      "takeaway": "A call expression prepares the input inside the parentheses and then checks the return value or state change produced by the function or method."
    },
    "target_statement": "print(meta[\"score\"])",
    "focus_span": "meta.update({\"score\": 0.9, \"domain\": \"UAM\"})",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "dict.update",
      "overwrite",
      "dictionary"
    ],
    "pedagogical_intent": "predict_output"
  },
  {
    "id": "PY4_L03_is_equal_001",
    "level": 3,
    "title": "A Taste of the Difference Between `is` and `==`",
    "concepts": [
      "identity_test",
      "if_statement"
    ],
    "reading_goal": "Check the actual Python behavior of identity check in the `focus` expression, and read how the result leads to the following code.",
    "code": "value = None\nif value is None:\n    print(\"empty\")",
    "question_type": "meaning_choice",
    "question": "What does “value is None” check for?",
    "choices": [
      "Check if the string is None",
      "Check if the file exists",
      "Check whether the value is the `None` object.",
      "Check the length of the list"
    ],
    "answer": "Check whether the value is the `None` object.",
    "explanation": "1) `None` is a special object representing the absence of a value. 2) To check whether a value is `None`, Python code normally uses `is` rather than the value-comparison operator `==`. 3) Because `is` compares object identity, it is especially clear for checking a singleton such as `None`. Taking this meaning into account, the correct answer is `checks whether the value is the None object`. Common wrong answer: checks whether it is the string None. `is None` checks for the actual `None` object, not for every value that merely looks empty.",
    "side_card_ids": [],
    "project_context": "This comes up very frequently in handling missing values and API responses.",
    "authoring_version": "V2.3",
    "primary_concept": "identity_test",
    "reading_goal_internal": "identity_test의 실제 Python 동작을 focus 표현식에서 확인하고, 그 결과가 다음 코드에 어떻게 이어지는지 읽는다.",
    "concept_explanation": {
      "what_it_is": "`None` is a special object that represents a state such as \"undetermined value.\" `is None` checks whether a variable points to that very `None` object.",
      "how_to_read": "`value is None` This should be read as: “`value`” checks not whether the value is the number 0 or an empty string, but specifically whether it is exactly `None`.",
      "key_point": "The result of `is None` is `True` when the condition matches and `False` otherwise.",
      "common_mistake": "It is easy to think of `None`, `0`, `\"\"`, and `[]` as the same “empty value.” Even though they may look similar in a conditional statement, they are distinct values, and `is None` checks only for `None` itself."
    },
    "teaching_example": {
      "code": "result = None\nprint(result is None)",
      "walkthrough": "Since `result` points exactly to `None`, `result is None` evaluates to `True`."
    },
    "answer_explanation": {
      "step_by_step": "1) None is a special object that indicates the absence of a value. 2) Therefore, when checking if a value is None, the `is` operator is typically used to check for object identity, rather than the value comparison operator `==`. 3) Since `is` checks for object identity, it is particularly clear when verifying singleton values such as None.",
      "why_correct": "If you take into account both the execution order and the changes in values, the correct answer is `checks whether the value is the None object`.",
      "common_wrong_choice": {
        "choice": "Check if the string is None",
        "why_wrong": "`is None` It does not check every value that appears to be empty, but rather verifies that it is specifically a None object.",
        "misread_step": "If you selected this as the incorrect answer, double-check whether you skipped reading one of the following: the input value of the `focus` expression, an intermediate result produced by a function or syntax, or the step where that result is used on the next line."
      },
      "takeaway": "`is None` Do not confuse it with other empty values; verify precisely whether it is a None object."
    },
    "target_statement": "    print(\"empty\")",
    "focus_span": "value is None",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "None",
      "identity",
      "is"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PY4_L04_mutable_alias_001",
    "level": 4,
    "title": "b = a: Binding two names to the same object without copying it to a new list",
    "concepts": [
      "assignment",
      "name_reference",
      "call"
    ],
    "reading_goal": "Read the list object pointed to by `a` on the right, store a reference to the same object in the new variable `b`, and then track the flow where a mutation on one side is reflected in both variables.",
    "code": "a = [\"LiDAR\"]\nb = a\nb.append(\"Radar\")\nprint(a)",
    "question_type": "output_prediction",
    "question": "What is the value of `a` after `b = a` and `append` are executed?",
    "choices": [
      "['LiDAR', 'Radar']",
      "['LiDAR']",
      "['Radar']",
      "None"
    ],
    "answer": "['LiDAR', 'Radar']",
    "explanation": "1) In the first line, `a` points to the `['LiDAR']` list object. 2) ``b = a`` stores a reference to the same object under the new name `b`. 3) `b.append('Radar')` directly modifies the shared list object. 4) Since `a` also points to the same object, it now shows `['LiDAR', 'Radar']`.",
    "side_card_ids": [
      "CS_mutable_immutable_001"
    ],
    "project_context": "This ties into understanding side-effect bugs that occur when multiple functions or variables share the same list of candidates.",
    "authoring_version": "V2.3",
    "primary_concept": "assignment",
    "reading_goal_internal": "오른쪽 a가 가리키는 list 객체를 읽어 새 이름 b에도 같은 객체 참조를 저장하고 이후 한쪽 mutation이 양쪽 이름에서 보이는 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "Assignment binds the value of the expression on the right to the name on the left. Assigning a mutable object from one name to another does not automatically copy the object.",
      "how_to_read": "`b = a` First, check what `a` refers to, and then read that `b` comes to refer to the same object.",
      "key_point": "`b.append` does not change the name “b”; rather, it modifies the list object itself that both names share.",
      "common_mistake": "`b = a` It is easy to think of this as code that creates an independent copy, such as ``b = list(a)``."
    },
    "teaching_example": {
      "code": "a = [1]\nb = a\nb.append(2)\nprint(a)",
      "walkthrough": "Since `b` points to the same list as `a`—not a new list—`a` after the `append` operation also displays `[1,2]`."
    },
    "answer_explanation": {
      "step_by_step": "1) In the first line, `a` points to the `['LiDAR']` list object. 2) ``b = a`` stores a reference to the same object under the new name `b`. 3) `b.append('Radar')` directly modifies the shared list object. 4) Since `a` also points to the same object, it now shows `['LiDAR', 'Radar']`.",
      "why_correct": "Since `b` and `a` share the same mutable list object, any changes made to `b` are reflected in what `a` reads.",
      "common_wrong_choice": {
        "choice": "['LiDAR']",
        "why_wrong": "That result would be possible if `b` were an independent copy, but `b = a` does not create a new list.",
        "misread_step": "`b = a` It is easy to think of this as code that creates an independent copy, such as ``b = list(a)``."
      },
      "takeaway": "\"Mutable alias\" should be interpreted as: **Check the object on the right → Both names share the same object → Observe the new state in all aliases after mutation**."
    },
    "target_statement": "b = a",
    "focus_span": "b = a",
    "focus_mode": "whole_statement",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "list",
      "mutable",
      "alias",
      "shared_object"
    ],
    "pedagogical_intent": "trace_state"
  },
  {
    "id": "PY4_L04_list_copy_001",
    "level": 4,
    "title": "Use the `list(original)` constructor call to copy the outer list into a new object.",
    "concepts": [
      "call",
      "assignment",
      "argument_passing"
    ],
    "reading_goal": "By using the elements of the `original iterable` to create a new list object and storing the result in `copy`, the flow ensures that changes to the external structure of `copy` are isolated from `original`.",
    "code": "original = [\"LiDAR\"]\ncopy = list(original)\ncopy.append(\"Radar\")\nprint(original)",
    "question_type": "output_prediction",
    "question": "After modifying `copy`, what is the value of `original`?",
    "choices": [
      "['LiDAR']",
      "['LiDAR', 'Radar']",
      "['Radar']",
      "None"
    ],
    "answer": "['LiDAR']",
    "explanation": "1) `original` points to the list ['LiDAR']. 2) `list(original)` creates a new outer list containing the same elements. 3) The new list is stored in `copy`, and `Radar` is appended to `copy`. 4) Since `original` is a separate outer list, it remains `['LiDAR']`.",
    "side_card_ids": [
      "CS_shallow_deep_copy_001"
    ],
    "project_context": "This relates to the process of temporarily modifying a list of candidates while preserving the original list.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "original iterable의 항목들을 사용해 새 list 객체를 만들고 그 결과를 copy에 저장하므로 copy의 바깥 구조 변경이 original과 분리되는 흐름을 읽는다.",
    "concept_explanation": {
      "what_it_is": "`list(iterable)` is a constructor call that creates a new list object containing the items of the iterable in order.",
      "how_to_read": "After verifying the items in the original input, note that while the results are identical, they are distinct external list objects.",
      "key_point": "Since this is a shallow copy, the outer list remains independent, but it does not perform a deep copy that includes nested mutable elements.",
      "common_mistake": "Since `list(original)` is simply an alias, it’s easy to assume that `copy.append` modifies `original` as well."
    },
    "teaching_example": {
      "code": "original = [1]\ncopy = list(original)\ncopy.append(2)\nprint(original)",
      "walkthrough": "Since `copy` is a separate external list, `append` applies only to `copy`, and the original remains `[1]`."
    },
    "answer_explanation": {
      "step_by_step": "1) `original` points to the list ['LiDAR']. 2) `list(original)` creates a new outer list containing the same elements. 3) The new list is stored in `copy`, and `Radar` is appended to `copy`. 4) Since `original` is a separate outer list, it remains `['LiDAR']`.",
      "why_correct": "Since the list constructor created a new list object, the `append` method on `copy` does not modify the outer list of `original`.",
      "common_wrong_choice": {
        "choice": "['LiDAR', 'Radar']",
        "why_wrong": "The result is alias behavior, as seen when sharing the same list, such as in ``copy = original``.",
        "misread_step": "Since `list(original)` is simply an alias, it’s easy to assume that `copy.append` modifies `original` as well."
      },
      "takeaway": "The sample code reads as follows: **Check whether a new object is created → Check how deep the sharing extends → Track the scope of mutation effects**."
    },
    "target_statement": "list(original)",
    "focus_span": "list(original)",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "list",
      "copy",
      "shallow_copy",
      "mutable"
    ],
    "pedagogical_intent": "compare_behavior"
  },
  {
    "id": "PY4_L04_any_001",
    "level": 4,
    "title": "Provide the sensor comparison results for each tag sequentially using generator expressions",
    "concepts": [
      "generator_expression",
      "comparison",
      "call",
      "for_statement"
    ],
    "reading_goal": "Read the flow where the generator expression iterates through the tags one by one, generating a `tag == 'sensor'` Boolean whenever necessary and passing it to `any`.",
    "code": "tags = [\"robotics\", \"sensor\"]\nchecks = (tag == \"sensor\" for tag in tags)\nif any(checks):\n    print(\"has sensor\")",
    "question_type": "meaning_choice",
    "question": "What values does a generator expression sequentially provide to `any`?",
    "choices": [
      "False Next True",
      "\"robotics\" followed by \"sensor\"—leave this string exactly as is",
      "True Next False",
      "A single dict"
    ],
    "answer": "False Next True",
    "explanation": "1) The first iteration tag is “robotics.” 2) The comparison ``robotics == sensor`` evaluates to `False`. 3) The next tag is “sensor,” so the comparison evaluates to `True`. 4) `any` finds `True` among the generated Booleans and evaluates the entire expression as `True`.",
    "side_card_ids": [
      "SC_filtering_pattern"
    ],
    "project_context": "This ties into the pattern of passing tags, warnings, and validation conditions to `any` or `all` without a large intermediate list.",
    "authoring_version": "V2.3",
    "primary_concept": "generator_expression",
    "reading_goal_internal": "generator expression이 tags를 하나씩 순회하며 `tag == 'sensor'` Boolean을 필요할 때마다 생성해 any에 전달하는 흐름을 읽는다.",
    "concept_explanation": {
      "what_it_is": "A generator expression, in the form `(expression for item in iterable)`, generates values one at a time as needed, rather than creating a list of values all at once.",
      "how_to_read": "Read it as follows: Set the current tag from the iterable after “for,” evaluate the comparison expression on the left side, and generate a Boolean value.",
      "key_point": "The first robotics comparison is False, the next sensor comparison is True, and if `any` encounters True, the overall result can be set to True.",
      "common_mistake": "Because there are parentheses, it’s easy to assume this is a tuple literal or to think that the generator creates a list of all results in advance."
    },
    "teaching_example": {
      "code": "values = [1, 4]\nchecks = (value > 3 for value in values)\nprint(any(checks))",
      "walkthrough": "\"1>3\" is False, \"4>3\" is True when provided sequentially, and the result of `any` is True."
    },
    "answer_explanation": {
      "step_by_step": "1) The first iteration tag is “robotics.” 2) The comparison ``robotics == sensor`` evaluates to `False`. 3) The next tag is “sensor,” so the comparison evaluates to `True`. 4) `any` finds `True` among the generated Booleans and evaluates the entire expression as `True`.",
      "why_correct": "Since the body of the `generator` is an equality comparison rather than a tag itself, the result for each iteration is a Boolean value of either `False` or `True`.",
      "common_wrong_choice": {
        "choice": "\"robotics\" followed by \"sensor\"—leave this string exactly as is",
        "why_wrong": "The string is the value of the loop variable, but the expression returned by the generator is the Boolean result of `tag == 'sensor'`.",
        "misread_step": "Because there are parentheses, it’s easy to assume this is a tuple literal or to think that the generator creates a list of all results in advance."
      },
      "takeaway": "A generator expression is read as **select an iterator → evaluate the preceding expression → yield a value → repeat if necessary**."
    },
    "target_statement": "(tag == \"sensor\" for tag in tags)",
    "focus_span": "(tag == \"sensor\" for tag in tags)",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "any",
      "generator_expression",
      "validation",
      "tags"
    ],
    "pedagogical_intent": "read_execution_order"
  },
  {
    "id": "PY4_L04_all_001",
    "level": 4,
    "title": "Check whether each key in `required` is present in the `row` dictionary using `in`",
    "concepts": [
      "membership_test",
      "generator_expression",
      "call",
      "for_statement"
    ],
    "reading_goal": "Track whether `key in row` returns True for each id and title while the generator changes the key one by one.",
    "code": "required = [\"id\", \"title\"]\nrow = {\"id\": \"1\", \"title\": \"News\"}\nchecks = (key in row for key in required)\nprint(all(checks))",
    "question_type": "output_prediction",
    "question": "In the two sessions of \"required,\" what is the order of the results produced by `key in row`?",
    "choices": [
      "True, then True.",
      "True Next False",
      "False Next True",
      "id Next title"
    ],
    "answer": "True, then True.",
    "explanation": "1) The first key is `id`, and since the row contains the `id` key, the result is `True`. 2) The second key is `title`, and since the row contains the `title` key, the result is `True`. 3) The generator provides the two Booleans to `all` in order. 4) Since both are `True`, the final result of `all` is also `True`.",
    "side_card_ids": [
      "DATA_schema_001"
    ],
    "project_context": "It connects to code that validates schema and card data to ensure no required fields are missing.",
    "authoring_version": "V2.3",
    "primary_concept": "membership_test",
    "reading_goal_internal": "generator가 현재 key를 하나씩 바꾸는 동안 `key in row`가 id와 title 각각에 대해 True를 만드는지 추적한다.",
    "concept_explanation": {
      "what_it_is": "`value in container` checks whether the value on the left is contained in the container on the right and returns a Boolean.",
      "how_to_read": "Currently, the key value is determined first, and then compared with the actual list of keys in the row to determine whether it is included.",
      "key_point": "Since both `id` and `title` exist as row keys, both membership tests evaluate to `True`, and `all` also evaluates to `True`.",
      "common_mistake": "It’s easy to think that ``in`` searches for a value in a dict. However, membership in a dict essentially checks whether a key exists."
    },
    "teaching_example": {
      "code": "required = [\"id\", \"name\"]\nrow = {\"id\": \"1\", \"name\": \"A\"}\nprint([key in row for key in required])",
      "walkthrough": "Since both `id` and `name` are keys for the row, the membership results are [True, True]."
    },
    "answer_explanation": {
      "step_by_step": "1) The first key is `id`, and since the row contains the `id` key, the result is `True`. 2) The second key is `title`, and since the row contains the `title` key, the result is `True`. 3) The generator provides the two Booleans to `all` in order. 4) Since both are `True`, the final result of `all` is also `True`.",
      "why_correct": "Since both required strings are actual dict keys in `row`, each membership test evaluates to True.",
      "common_wrong_choice": {
        "choice": "id Next title",
        "why_wrong": "`id` and `title` are the current strings of the left-hand operands, and the entire membership expression returns a Boolean.",
        "misread_step": "It’s easy to think that ``in`` searches for a value in a dict. However, membership in a dict essentially checks whether a key exists."
      },
      "takeaway": "\"dict membership\" is interpreted as **checking the current key → verifying whether the key exists in the dict → obtaining a Boolean result → passing it to functions like all or any**."
    },
    "target_statement": "key in row",
    "focus_span": "key in row",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "all",
      "membership",
      "required_fields",
      "schema_validation"
    ],
    "pedagogical_intent": "trace_state"
  },
  {
    "id": "PY4_L05_finally_001",
    "level": 5,
    "title": "Execute the `finally` block after the `try` block ends",
    "concepts": [
      "finally_clause",
      "try_statement",
      "call"
    ],
    "reading_goal": "Read the actual flow to understand that `finally:` is where cleanup code is executed before the `try` block completes normally or an exception is propagated.",
    "code": "events=[]\ntry:\n    events.append(\"run\")\nfinally:\n    events.append(\"cleanup\")\nprint(events)",
    "question_type": "output_prediction",
    "question": "What is the value of the last ``events``?",
    "choices": [
      "[\"run\"]",
      "[\"cleanup\"]",
      "[\"run\", \"cleanup\"]",
      "[]"
    ],
    "answer": "[\"run\", \"cleanup\"]",
    "explanation": "1) The `try` block is executed first, so add `run`. 2) The `try` block ends without an exception. 3) The `finally` block is executed, so add `cleanup`. 4) The list contains two values in this order.",
    "side_card_ids": [
      "DEV_debugging_001"
    ],
    "project_context": "It is linked to an exception-handling structure that cleans up resources such as files, connections, and temporary states.",
    "authoring_version": "V2.3",
    "primary_concept": "finally_clause",
    "reading_goal_internal": "`finally:`가 try의 정상 완료나 예외 전파 전에 정리 코드를 실행하는 위치라는 점을 실제 흐름으로 읽는다.",
    "concept_explanation": {
      "what_it_is": "`finally` is a block that executes cleanup code when exiting a try statement.",
      "how_to_read": "First, check the execution results inside the `try` block, then follow along to see if the `finally` block is executed as the final cleanup step.",
      "key_point": "Since there are no exceptions in this code, “run” is logged first, followed by “cleanup.”",
      "common_mistake": "It’s easy to think that `finally` runs only when an exception occurs or that it runs before `try`."
    },
    "teaching_example": {
      "code": "events=[]\ntry:\n    events.append(\"work\")\nfinally:\n    events.append(\"close\")\nprint(events)",
      "walkthrough": "Even if the `try` block completes successfully, the `finally` block executes, leaving `close` after `work`."
    },
    "answer_explanation": {
      "step_by_step": "1) The `try` block is executed first, so add `run`. 2) The `try` block ends without an exception. 3) The `finally` block is executed, so add `cleanup`. 4) The list contains two values in this order.",
      "why_correct": "Since the `finally` block is executed even in the normal completion path, it includes the `cleanup` item as well.",
      "common_wrong_choice": {
        "choice": "[\"run\"]",
        "why_wrong": "Even if the `try` block succeeds, the `finally` block is not skipped.",
        "misread_step": "Read the execution of `try` and `finally` as separate steps."
      },
      "takeaway": "\"finally\" should be interpreted as **execute `try` → check for normal or exception conditions → execute `finally` cleanup → continue to the next step**."
    },
    "target_statement": "finally:",
    "focus_span": "finally:",
    "focus_mode": "clause_header",
    "coverage_domain": "exceptions_debugging",
    "coverage_topics": [
      "try",
      "finally",
      "cleanup",
      "control_flow"
    ],
    "pedagogical_intent": "read_execution_order"
  },
  {
    "id": "PY4_L05_raise_001",
    "level": 5,
    "title": "Immediately halt the function flow at an incorrect score by raising a ValueError",
    "concepts": [
      "raise_statement",
      "call",
      "if_statement"
    ],
    "reading_goal": "When `raise exception` is executed, the current normal execution flow is interrupted, and the program proceeds to search for the nearest handler.",
    "code": "def validate(score):\n    if score < 0:\n        raise ValueError(\"score must be >= 0\")\n    return score\n\ntry:\n    validate(-1)\nexcept ValueError as exc:\n    print(type(exc).__name__)",
    "question_type": "output_prediction",
    "question": "What exception type name is printed after executing `validate(-1)`?",
    "choices": [
      "TypeError",
      "ValueError",
      "RuntimeError",
      "None"
    ],
    "answer": "ValueError",
    "explanation": "1) The score is -1. 2) -1 < 0 is True. 3) raise ValueError(...) immediately raises a ValueError. 4) The outer except ValueError catches it and prints the type name ValueError.",
    "side_card_ids": [],
    "project_context": "It is linked to defensive code that prevents incorrect data from being passed to subsequent stages during score collection, input validation, and schema validation.",
    "authoring_version": "V2.3",
    "primary_concept": "raise_statement",
    "reading_goal_internal": "`raise exception`이 예외를 발생시키는 순간 현재 정상 실행 흐름이 중단되고 가까운 handler를 찾는 순서를 읽는다.",
    "concept_explanation": {
      "what_it_is": "`raise exception` This is a statement that immediately raises a specified exception, interrupting the current normal flow of execution.",
      "how_to_read": "First, check the conditions for reaching `raise`, and then determine whether the next normal statement in the same function executes after an exception occurs.",
      "key_point": "After a `raise`, the program does not proceed to a normal `return` but instead searches the call stack for the appropriate `except` handler.",
      "common_mistake": "It is easy to assume that `raise` simply prints an error message to the screen while the function continues to execute."
    },
    "teaching_example": {
      "code": "def check(value):\n    if value == \"\":\n        raise ValueError(\"empty\")\n    return value\ntry:\n    check(\"\")\nexcept ValueError as exc:\n    print(type(exc).__name__)",
      "walkthrough": "Because the empty string condition is true, `raise` raises a `ValueError`, which is caught by the outer `except` block."
    },
    "answer_explanation": {
      "step_by_step": "1) The score is -1. 2) -1 < 0 is True. 3) raise ValueError(...) immediately raises a ValueError. 4) The outer except ValueError catches it and prints the type name ValueError.",
      "why_correct": "This is because the exception class specified in the `raise` statement is `ValueError`, and the condition actually causes the code to reach the `raise` statement.",
      "common_wrong_choice": {
        "choice": "None",
        "why_wrong": "Once `raise` executes, execution never reaches the normal `return score`, so this is not a flow that returns `None`.",
        "misread_step": "Note that the normal statements following `raise` are not executed."
      },
      "takeaway": "\"raise\" is interpreted as **checking for conditions → creating an exception object → immediately raising the exception → interrupting normal flow → searching for a handler**."
    },
    "target_statement": "raise ValueError(\"score must be >= 0\")",
    "focus_span": "raise ValueError(\"score must be >= 0\")",
    "focus_mode": "whole_statement",
    "coverage_domain": "exceptions_debugging",
    "coverage_topics": [
      "raise",
      "ValueError",
      "validation",
      "control_flow"
    ],
    "pedagogical_intent": "read_execution_order"
  },
  {
    "id": "PY4_L05_guard_return_001",
    "level": 5,
    "title": "If the `if guard` is true, skip the normalized code that follows",
    "concepts": [
      "if_statement",
      "boolean_operation",
      "return_statement",
      "function_definition"
    ],
    "reading_goal": "`if not label:` Read the flow where, when the condition is True, the program immediately enters the `return` path and does not execute the subsequent statements.",
    "code": "def normalize(label):\n    if not label:\n        return \"\"\n    return label.strip().lower()\n\nprint(normalize(None) == \"\")",
    "question_type": "output_prediction",
    "question": "What is the output of `normalize(None) == \"\"`?",
    "choices": [
      "True",
      "False",
      "None",
      "Error"
    ],
    "answer": "True",
    "explanation": "1) None is passed to `normalize`. 2) Since None is falsy, `not label` evaluates to True. 3) The `return \"\"` in the `if` block is executed immediately. 4) The subsequent `strip` and `lower` lines are not executed, so the comparison result is True.",
    "side_card_ids": [],
    "project_context": "It is linked to a guard clause in the input validation function that prevents incorrect values from being passed on to the subsequent processing stage.",
    "authoring_version": "V2.3",
    "primary_concept": "if_statement",
    "reading_goal_internal": "`if not label:` 조건이 True일 때 바로 return 경로로 들어가 이후 문장을 실행하지 않는 흐름을 읽는다.",
    "concept_explanation": {
      "what_it_is": "`if` A statement first evaluates a condition and executes the indented block only if the condition is True.",
      "how_to_read": "In `if not label:`, first determine the truth value of `label`, then reverse it with `not` to obtain the condition result.",
      "key_point": "Since `None` is falsy, `not None` evaluates to `True`, and the first `return` statement is executed.",
      "common_mistake": "It is easy to assume that even if the `guard` condition is true, the function will continue executing all the way to the `return` statement at the bottom."
    },
    "teaching_example": {
      "code": "def clean(text):\n    if not text:\n        return \"missing\"\n    return text.strip()\n\nprint(clean(\"\"))",
      "walkthrough": "Since an empty string is falsy, the `return` statement in the `if` block is executed immediately."
    },
    "answer_explanation": {
      "step_by_step": "1) None is passed to `normalize`. 2) Since None is falsy, `not label` evaluates to True. 3) The `return \"\"` in the `if` block is executed immediately. 4) The subsequent `strip` and `lower` lines are not executed, so the comparison result is True.",
      "why_correct": "Since the first `return` returns an empty string, `normalize(None) == \"\"` is True.",
      "common_wrong_choice": {
        "choice": "None",
        "why_wrong": "The function’s return value is not the input label itself, but rather the empty string specified in the guard.",
        "misread_step": "Check where the function execution ends after the condition is true."
      },
      "takeaway": "The `if` statement is read in the following order: **condition evaluation → enter the block if True → exit the function if `return`**."
    },
    "target_statement": "if not label:",
    "focus_span": "if not label:",
    "focus_mode": "compound_header",
    "coverage_domain": "functions_and_oop",
    "coverage_topics": [
      "guard_clause",
      "early_return",
      "normalization",
      "function_flow"
    ],
    "pedagogical_intent": "read_execution_order"
  },
  {
    "id": "PY4_L06_jsonl_stream_001",
    "level": 6,
    "title": "Reading a streaming loop that processes JSONL line by line",
    "concepts": [
      "for_statement",
      "call",
      "subscription"
    ],
    "reading_goal": "Read the flow where `for line in stream:` processes JSONL records one line at a time.",
    "code": "import json\nfrom io import StringIO\nstream = StringIO('{\"id\": \"A\"}\\n{\"id\": \"B\"}\\n')\nfor line in stream:\n    row = json.loads(line)\n    print(row['id'])",
    "question_type": "flow",
    "question": "Which sequence correctly describes how this code processes JSONL?",
    "choices": [
      "Read one line → Parse that line → Use id → Next line",
      "Convert the entire file into a list and parse only the last line",
      "Combine all lines and call `json.loads` only once",
      "First print the id, then read the line"
    ],
    "answer": "Read one line → Parse that line → Use id → Next line",
    "explanation": "1) StringIO provides a stream, such as a file containing multiple JSON lines. 2) ``for line in stream:`` reads one line at a time. 3) Each line is converted into a dictionary using ``json.loads``. 4) Since only the ID of the current row is used directly, a full list of rows is not created beforehand.",
    "side_card_ids": [
      "CS_json_jsonl_001"
    ],
    "project_context": "This ties into understanding code that inspects large JSONL files containing cards and nodes without loading the entire file into memory.",
    "authoring_version": "V2.3",
    "primary_concept": "for_statement",
    "reading_goal_internal": "`for line in stream:`이 JSONL 레코드를 한 줄씩 처리하게 하는 흐름을 읽는다.",
    "concept_explanation": {
      "what_it_is": "`for` iterates through an iterable, retrieving elements one by one and executing the block repeatedly.",
      "how_to_read": "In JSONL, we verify that a single loop variable represents a record string and check whether that line is parsed immediately.",
      "key_point": "`for line in stream:` The structure processes records one line at a time rather than converting the entire file into a list all at once.",
      "common_mistake": "A `for` loop does not always use less memory simply because it is a `for` loop. If you collect every row into a list again inside the loop, the benefits of streaming are reduced."
    },
    "teaching_example": {
      "code": "import json\nfrom io import StringIO\nstream = StringIO('{\"n\": 1}\\n{\"n\": 2}\\n')\nfor line in stream:\n    print(json.loads(line)['n'])",
      "walkthrough": "In each iteration, it reads and parses only one line and prints 1 and 2 in sequence."
    },
    "answer_explanation": {
      "step_by_step": "1) StringIO provides a stream, such as a file containing multiple JSON lines. 2) ``for line in stream:`` reads one line at a time. 3) Each line is converted into a dictionary using ``json.loads``. 4) Since only the ID of the current row is used directly, a full list of rows is not created beforehand.",
      "why_correct": "`for line in stream:` The structure processes records one line at a time rather than converting the entire file into a list all at once.",
      "common_wrong_choice": {
        "choice": "Combine all lines and call `json.loads` only once",
        "why_wrong": "Since JSONL treats each line as an independent JSON record, this code calls `json.loads` on every line.",
        "misread_step": "Verify that each iteration of the loop corresponds to a single JSONL record."
      },
      "takeaway": "Read JSONL streaming as a repetition of **obtain one line → parse one record → process it immediately**."
    },
    "target_statement": "for line in stream:",
    "focus_span": "for line in stream:",
    "focus_mode": "whole_statement",
    "coverage_domain": "files_paths_serialization",
    "coverage_topics": [
      "jsonl",
      "streaming",
      "json.loads",
      "for loop"
    ],
    "pedagogical_intent": "read_execution_order"
  },
  {
    "id": "PY4_L06_tsv_dictreader_001",
    "level": 6,
    "title": "Reading the delimiter keyword argument that enables TSV reading",
    "concepts": [
      "argument_passing",
      "call",
      "literal"
    ],
    "reading_goal": "`delimiter='\\t'` The keyword argument defines the column boundaries in the TSV file.",
    "code": "import csv\nfrom io import StringIO\nf = StringIO('title\\tlevel\\nJSONL\\t6\\n')\nreader = csv.DictReader(f, delimiter='\\t')\nrow = next(reader)\nprint(row['title'])",
    "question_type": "meaning_choice",
    "question": "What does `delimiter='\\t'` do?",
    "choices": [
      "Use the tab character as the column separator.",
      "Add a tab character after every value.",
      "Read only the first column",
      "Automatically convert TSV to JSON"
    ],
    "answer": "Use the tab character as the column separator.",
    "explanation": "1) StringIO contains tab-separated TSV text. 2) When calling ``csv.DictReader``, ``delimiter='\\t'`` is passed as a keyword argument. 3) `DictReader` uses tabs as column delimiters. 4) Each row is read as a dictionary with header names as keys.",
    "side_card_ids": [
      "DATA_schema_001"
    ],
    "project_context": "This connects to understanding the configuration difference when reading CSV and TSV learning data with the same `DictReader` API.",
    "authoring_version": "V2.3",
    "primary_concept": "argument_passing",
    "reading_goal_internal": "`delimiter='\\t'` keyword argument가 TSV의 컬럼 경계를 정하는 의미를 읽는다.",
    "concept_explanation": {
      "what_it_is": "A keyword argument specifies the name of the argument to clearly indicate which setting value is being passed.",
      "how_to_read": "Read the name `delimiter` and the fact that `\\t` is a tab character together.",
      "key_point": "`delimiter='\\t'` This specifies that DictReader should use tabs instead of commas as column delimiters.",
      "common_mistake": "`\\t` Do not interpret “`\\t`” as two characters: a backslash followed by a “t.” In Python strings, “” is a tab character."
    },
    "teaching_example": {
      "code": "import csv\nfrom io import StringIO\nf = StringIO('name\\tscore\\nA\\t3\\n')\nreader = csv.DictReader(f, delimiter='\\t')\nprint(next(reader)['score'])",
      "walkthrough": "`delimiter='\\t'` Therefore, the name and score columns are separated by tabs."
    },
    "answer_explanation": {
      "step_by_step": "1) StringIO contains tab-separated TSV text. 2) When calling ``csv.DictReader``, ``delimiter='\\t'`` is passed as a keyword argument. 3) `DictReader` uses tabs as column delimiters. 4) Each row is read as a dictionary with header names as keys.",
      "why_correct": "`delimiter='\\t'` This specifies that DictReader should use tabs instead of commas as column delimiters.",
      "common_wrong_choice": {
        "choice": "Automatically convert TSV to JSON",
        "why_wrong": "DictReader simply reads lines based on specified delimiters; it does not perform JSON conversion.",
        "misread_step": "Read the role of the setting specified by the keyword."
      },
      "takeaway": "In TSV, ``delimiter='\\t'`` determines **where to split columns**."
    },
    "target_statement": "delimiter='\\t'",
    "focus_span": "delimiter='\\t'",
    "focus_mode": "subexpression",
    "coverage_domain": "files_paths_serialization",
    "coverage_topics": [
      "tsv",
      "csv.DictReader",
      "delimiter",
      "keyword argument"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PY4_L06_file_suffix_001",
    "level": 6,
    "title": "Separate the stem and suffix of “Path” when reading filenames",
    "concepts": [
      "attribute_access",
      "call",
      "assignment"
    ],
    "reading_goal": "Accurately read the parts returned by `p.suffix` and `p.stem` from the filename.",
    "code": "from pathlib import Path\np = Path('report.jsonl')\nprint(p.stem, p.suffix)",
    "question_type": "output_prediction",
    "question": "Which option is closest to the output?",
    "choices": [
      "report .jsonl",
      "report jsonl",
      "report.jsonl .jsonl",
      ".jsonl report"
    ],
    "answer": "report .jsonl",
    "explanation": "1) `Path('report.jsonl')` creates a path object. 2) `p.stem` reads the filename portion excluding the final extension. 3) `p.suffix` reads the final extension `.jsonl`. 4) It prints both values.",
    "side_card_ids": [],
    "project_context": "This applies when branching based on the `.json`/`.jsonl` format of card data files or when generating result filenames.",
    "authoring_version": "V2.3",
    "primary_concept": "attribute_access",
    "reading_goal_internal": "`p.suffix`와 `p.stem`이 파일명에서 반환하는 부분을 정확히 읽는다.",
    "concept_explanation": {
      "what_it_is": "Attribute access uses the `.` followed by a name after an object to access the attributes or functions provided by that object.",
      "how_to_read": "Distinguish between the full path and the filename element, and identify which parts are represented by `stem` and `suffix`, respectively.",
      "key_point": "`p.suffix` ``report.jsonl`` returns ``.jsonl``, and ``p.stem`` returns ``report``.",
      "common_mistake": "It’s easy to assume that `suffix` returns only `jsonl` without the period, but `Path.suffix` includes the leading period as well."
    },
    "teaching_example": {
      "code": "from pathlib import Path\np = Path('archive.tar.gz')\nprint(p.stem, p.suffix)",
      "walkthrough": "Since only the final suffix is used as the basis, the stem is `archive.tar`, and the suffix is `.gz`."
    },
    "answer_explanation": {
      "step_by_step": "1) `Path('report.jsonl')` creates a path object. 2) `p.stem` reads the filename portion excluding the final extension. 3) `p.suffix` reads the final extension `.jsonl`. 4) It prints both values.",
      "why_correct": "`p.suffix` ``report.jsonl`` returns ``.jsonl``, and ``p.stem`` returns ``report``.",
      "common_wrong_choice": {
        "choice": "report jsonl",
        "why_wrong": "`Path.suffix` includes the period in the file extension.",
        "misread_step": "Distinguish which parts of the original filename are the stem and which are the suffix."
      },
      "takeaway": "`stem` \"`suffix`\" refers to the name excluding the final file extension, while \"\" refers to the name including the final file extension."
    },
    "target_statement": "p.suffix",
    "focus_span": "p.suffix",
    "focus_mode": "subexpression",
    "coverage_domain": "files_paths_serialization",
    "coverage_topics": [
      "pathlib",
      "stem",
      "suffix",
      "filename"
    ],
    "pedagogical_intent": "interpret_result"
  },
  {
    "id": "PY4_L06_resolve_path_001",
    "level": 6,
    "title": "Converting Relative Paths to Absolute Paths Using `Path.resolve()`",
    "concepts": [
      "call",
      "assignment",
      "from_import_statement"
    ],
    "reading_goal": "Read the difference between `resolve()` interpreting a relative path as an absolute `Path` and checking whether the file exists.",
    "code": "from pathlib import Path\npath = Path('data/items.json').resolve()\nprint(path.is_absolute())",
    "question_type": "meaning_choice",
    "question": "Which of the following is the closest description of the result of `Path('data/items.json').resolve()`?",
    "choices": [
      "This is an absolute path relative to the current working directory; the existence of the file must be verified separately.",
      "A dictionary containing the contents of the items.json file",
      "A True value indicating that the file definitely exists",
      "As a result of changing the current working directory"
    ],
    "answer": "This is an absolute path relative to the current working directory; the existence of the file must be verified separately.",
    "explanation": "1) Create a relative path. 2) `resolve()` calculates the absolute path relative to the current working directory. 3) Store the result. 4) Calculating the absolute path does not guarantee that the file exists.",
    "side_card_ids": [],
    "project_context": "When debugging issues where relative paths vary depending on the execution location, this helps distinguish between the actual interpretation path and whether a file exists.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "`resolve()`가 상대 Path를 현재 작업 폴더 기준의 절대 Path로 바꾸어 반환하며, 이 결과 자체가 파일 존재를 증명하지는 않는다는 점을 읽는다.",
    "concept_explanation": {
      "what_it_is": "`Path.resolve()` is a method call that interprets a Path as an absolute path and returns a new Path result.",
      "how_to_read": "First, check whether the original `Path` is a relative path, and then read how the result of the ``resolve()`` call becomes an absolute path relative to which reference point.",
      "key_point": "With the default `strict=False` setting, paths that do not exist can be interpreted as absolute paths whenever possible; therefore, a successful `resolve` result does not guarantee that the file exists.",
      "common_mistake": "`resolve()` It’s easy to assume that it reads the file contents or even performs the `exists()` check on your behalf."
    },
    "teaching_example": {
      "code": "from pathlib import Path\nconfig = Path('config/app.json').resolve()\nprint(config.is_absolute())",
      "walkthrough": "Calling `resolve` on a relative path returns an absolute path relative to the current working directory."
    },
    "answer_explanation": {
      "step_by_step": "1) ``data/items.json`` is a relative path. 2) ``resolve()`` interprets this as an absolute path relative to the current working directory. 3) The returned path is stored in ``path``. 4) ``is_absolute()`` only checks whether the path is absolute; it does not verify whether the file actually exists.",
      "why_correct": "`resolve()` The key is path resolution, since reading the file’s contents or verifying its existence are separate tasks.",
      "common_wrong_choice": {
        "choice": "A True value indicating that the file definitely exists",
        "why_wrong": "Calculating absolute paths and checking for file existence are two separate questions.",
        "misread_step": "`resolve()` It separates the return type from the role of existence checks, such as ``exists()``."
      },
      "takeaway": "Read `resolve()` as **\"resolves a relative path → returns an absolute path\"**, and **check for existence separately**."
    },
    "target_statement": "Path('data/items.json').resolve()",
    "focus_span": "Path('data/items.json').resolve()",
    "focus_mode": "subexpression",
    "coverage_domain": "files_paths_serialization",
    "coverage_topics": [
      "pathlib",
      "resolve",
      "absolute path",
      "relative path",
      "file existence"
    ],
    "pedagogical_intent": "interpret_result"
  },
  {
    "id": "PY4_L07_request_timeout_001",
    "level": 7,
    "title": "Reading the `timeout=10` keyword argument",
    "concepts": [
      "argument_passing",
      "call",
      "assignment"
    ],
    "reading_goal": "`timeout=10`\" within an HTTP call is interpreted as a keyword argument that passes a value to a specific parameter.",
    "code": "import requests\n\nresponse = requests.get(\"https://example.com\", timeout=10)",
    "question_type": "multiple_choice",
    "question": "Which of the following is the most accurate interpretation of `timeout=10` from the perspective of Python call syntax?",
    "choices": [
      "Pass the value 10 as a keyword argument to the `timeout` parameter.",
      "Call `requests.get` 10 times.",
      "Change the name “response” to 10.",
      "Select the tenth character of the URL."
    ],
    "answer": "Pass the value 10 as a keyword argument to the `timeout` parameter.",
    "explanation": "The call begins with `requests.get(`. The URL is the first positional argument. `timeout=10` is the second argument, where the parameter name is specified directly. This is because the `name=value` format inside the call parentheses is a keyword argument. Common misconception: `requests.get` is called 10 times. There is no loop or `range` statement, and the number 10 is passed to the `timeout` parameter, not used to count the number of calls. Point of Misinterpretation: You must verify which syntactic structure the number is part of. `function(..., name=value)` is a keyword argument that explicitly passes a value to the `name` parameter.",
    "side_card_ids": [
      "AI_latency_001"
    ],
    "project_context": "This refers to the context of specifying a request timeout to prevent indefinite waiting during Harvest or external API calls.",
    "authoring_version": "V2.3",
    "primary_concept": "argument_passing",
    "reading_goal_internal": "HTTP 호출 안의 `timeout=10`을 특정 매개변수에 값을 전달하는 keyword argument로 읽는다.",
    "concept_explanation": {
      "what_it_is": "A keyword argument is a syntax where values are passed in the form `parameter_name=value` when calling a function.",
      "how_to_read": "Check the function name and the arguments inside the parentheses; in `timeout=10`, read the left side as the parameter name and the right side as the value to be passed.",
      "key_point": "The \"10-second timeout\" refers to how the requests library works, while the Python syntax focus is on passing keyword arguments.",
      "common_mistake": "`timeout=10` It is easier to read as a variable assignment outside the function. Inside the call parentheses, it is a keyword argument."
    },
    "teaching_example": {
      "code": "def fetch(path, timeout):\n    return timeout\n\nseconds = fetch(\"/health\", timeout=5)\nprint(seconds)",
      "walkthrough": "Since the call `timeout=5` passes 5 to the `timeout` parameter, the function returns 5."
    },
    "answer_explanation": {
      "step_by_step": "The call begins with `requests.get(`. The URL is the first positional argument. `timeout=10` is the second argument, where the parameter name is specified explicitly.",
      "why_correct": "The `name=value` format inside the call parentheses is because it is a keyword argument.",
      "common_wrong_choice": {
        "choice": "Call `requests.get` 10 times.",
        "why_wrong": "There are no loops or `range` statements, and the number 10 is passed to the `timeout` parameter, not as the number of calls.",
        "misread_step": "You must check which grammatical structure the number belongs to."
      },
      "takeaway": "`function(..., name=value)` is a keyword argument that explicitly passes a value to the `name` parameter."
    },
    "target_statement": "response = requests.get(\"https://example.com\", timeout=10)",
    "focus_span": "timeout=10",
    "focus_mode": "subexpression",
    "coverage_domain": "web_http_api",
    "coverage_topics": [
      "requests",
      "HTTP",
      "timeout",
      "API"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PY4_L07_retry_loop_001",
    "level": 7,
    "title": "`for attempt in range(3)` Reading the number of retry attempts",
    "concepts": [
      "for_statement",
      "call",
      "break_statement"
    ],
    "reading_goal": "Track the maximum number of attempts by considering both the three values produced by `range(3)` and the possibility of `break`.",
    "code": "def call_api(attempt):\n    return attempt == 1\n\nfor attempt in range(3):\n    if call_api(attempt):\n        break\n    print(\"retry\", attempt)",
    "question_type": "multiple_choice",
    "question": "Looking only at `for attempt in range(3):`, what is the maximum number of times the loop body can be executed?",
    "choices": [
      "No. 1",
      "No. 2",
      "No. 3",
      "Infinite loop"
    ],
    "answer": "No. 3",
    "explanation": "`range(3)` generates 0, 1, and 2 in sequence. Since the `for` loop enters the `body` for each value, it enters at most three times if `break` is never executed. This is because `range` provides three values, so the maximum number of cycles for the `for` loop is also three. Common source of confusion: The final value “2” in option 2 refers to the value assigned to `attempt` in the third cycle, not the number of iterations. Common Misinterpretation: You must distinguish between the value of the loop variable and the number of iterations. `for x in range(n)` iterates over n values from 0 to n-1 by default, so the body is executed at most n times.",
    "side_card_ids": [
      "DEV_debugging_001"
    ],
    "project_context": "This refers to a context where a limited number of retries are allowed for API calls that may temporarily fail.",
    "authoring_version": "V2.3",
    "primary_concept": "for_statement",
    "reading_goal_internal": "`range(3)`이 제공하는 세 값과 `break` 가능성을 함께 읽어 최대 시도 횟수를 추적한다.",
    "concept_explanation": {
      "what_it_is": "A `for` statement is a compound statement that retrieves values one by one from an iterable object and executes the loop body.",
      "how_to_read": "For `for variable in iterable:`, first determine which values the iterable produces, then check whether the loop body contains a statement such as `break` that can terminate the loop early.",
      "key_point": "Since `range(3)` returns the values 0, 1, and 2, the body will execute three times unless a `break` is present.",
      "common_mistake": "It’s easy to think that `range(3)` generates 1, 2, and 3. However, when the start value is omitted, `range` starts at 0 and does not include the stop value of 3."
    },
    "teaching_example": {
      "code": "for index in range(2):\n    print(index)",
      "walkthrough": "`range(2)` produces 0 and 1, so the loop body runs twice and prints 0 and 1."
    },
    "answer_explanation": {
      "step_by_step": "`range(3)` generates 0, 1, and 2 in sequence. Since the `for` loop executes the `body` for each value, it enters the `body` up to three times—unless `break` is executed at any point.",
      "why_correct": "Since `range` provides three values, the maximum number of cycles in the `for` loop is also three.",
      "common_wrong_choice": {
        "choice": "No. 2",
        "why_wrong": "The final value 2 refers not to the count but to the value passed to `attempt` in the third cycle.",
        "misread_step": "You must distinguish between the value of an iteration variable and the number of iterations."
      },
      "takeaway": "`for x in range(n)` Since it iterates over n values from 0 to n-1 by default, the body is executed at most n times."
    },
    "target_statement": "for attempt in range(3):",
    "focus_span": "for attempt in range(3):",
    "focus_mode": "whole_statement",
    "coverage_domain": "web_http_api",
    "coverage_topics": [
      "retry",
      "range",
      "for",
      "API_reliability"
    ],
    "pedagogical_intent": "read_execution_order"
  },
  {
    "id": "PY4_L07_auth_header_001",
    "level": 7,
    "title": "Reading the Authorization Header Dict Assignment",
    "concepts": [
      "assignment",
      "f_string",
      "literal"
    ],
    "reading_goal": "Read the assignment flow where a Bearer string created with an f-string becomes a dict value and is stored in the header name.",
    "code": "token = \"abc123\"\nheaders = {\"Authorization\": f\"Bearer {token}\"}\nprint(headers[\"Authorization\"])",
    "question_type": "output_prediction",
    "question": "What is the string printed on the last line?",
    "choices": [
      "Bearer abc123",
      "abc123",
      "Authorization",
      "Bearer token"
    ],
    "answer": "Bearer abc123",
    "explanation": "The token is abc123. The f-string concatenates `Bearer ` with the token value to form `Bearer abc123`. This string is stored in the Authorization key, and the final subscription reads that value. This is because the `Authorization` value in the completed dictionary on the right side of the assignment is exactly `Bearer abc123`. A common source of confusion: `abc123` is the original token value, while the value stored in the headers is a new string created by the f-string with a prefix. Point of confusion: You must consider the order in which the f-string is evaluated before the dict value is stored. The assignment first evaluates all nested expressions on the right-hand side and then stores the resulting value in the left-hand target.",
    "side_card_ids": [
      "WEB_auth_001"
    ],
    "project_context": "This refers to the context of configuring a token as the value of the Authorization header in an external API request.",
    "authoring_version": "V2.3",
    "primary_concept": "assignment",
    "reading_goal_internal": "f-string으로 만든 Bearer 문자열이 dict value가 되어 headers 이름에 저장되는 대입 흐름을 읽는다.",
    "concept_explanation": {
      "what_it_is": "An assignment is an expression that first evaluates the right-hand side and then stores the result in the left-hand side name or location.",
      "how_to_read": "I interpret this to mean that the value expression inside the dict literal on the right is evaluated first, and the entire resulting dict is then stored in the name on the left.",
      "key_point": "`Authorization` and `Bearer` are HTTP-authentication context; the Python syntax focus is the assignment that evaluates the dict on the right and stores it in `headers`.",
      "common_mistake": "`token` It’s easy to assume that the string itself is stored in `headers` as-is. The actual value is a new string created by the f-string, including the ``Bearer `` prefix."
    },
    "teaching_example": {
      "code": "key = \"xyz\"\nmeta = {\"X-Api-Key\": f\"Key {key}\"}\nprint(meta[\"X-Api-Key\"])",
      "walkthrough": "The key value “xyz” is inserted into an f-string to become “Key xyz,” and that string is stored as the value for the X-Api-Key key."
    },
    "answer_explanation": {
      "step_by_step": "`token` is `abc123`. The f-string combines `Bearer ` with the token value to create `Bearer abc123`. This string is stored under the `Authorization` key, and the final subscription reads that value.",
      "why_correct": "This is because the `Authorization` value of the completed `dict` on the right side of the assignment is exactly ``Bearer abc123``.",
      "common_wrong_choice": {
        "choice": "abc123",
        "why_wrong": "`abc123` is the original token value, and the value stored in `headers` is a new string created by prepending an f-string.",
        "misread_step": "You need to consider the order of operations: the f-string is evaluated first, before the `dict` value is stored."
      },
      "takeaway": "The `assignment` operator first evaluates all nested expressions on the right side and then stores the resulting value in the target on the left."
    },
    "target_statement": "headers = {\"Authorization\": f\"Bearer {token}\"}",
    "focus_span": "headers = {\"Authorization\": f\"Bearer {token}\"}",
    "focus_mode": "whole_statement",
    "coverage_domain": "security_and_auth",
    "coverage_topics": [
      "HTTP",
      "Authorization_header",
      "Bearer_token",
      "API_auth"
    ],
    "pedagogical_intent": "read_execution_order"
  },
  {
    "id": "PY4_L08_logging_level_001",
    "level": 8,
    "title": "Logging an ERROR-level message by calling logging.error()",
    "concepts": [
      "call",
      "attribute_access",
      "argument_passing",
      "literal"
    ],
    "reading_goal": "When reading the `logging.error(message)` call, distinguish between the target of the call, the message passed, and the log level.",
    "code": "import logging\n\nlogging.basicConfig(level=logging.INFO)\nlogging.info(\"started\")\nlogging.error(\"failed\")",
    "question_type": "multiple_choice",
    "question": "What is the log level recorded by `logging.error(\"failed\")`?",
    "choices": [
      "DEBUG",
      "INFO",
      "WARNING",
      "ERROR"
    ],
    "answer": "ERROR",
    "explanation": "`logging.error` The call name determines the log level, and ``failed`` is the message to be logged at that level. This is because ``logging.error()`` is a call in the Python logging API that logs a message at the ERROR level. A common source of confusion: `INFO` is the level used by ``logging.info(\"started\")`` on the previous line. Source of Misreading: This occurs when the names of two consecutive `logging` calls are read in the wrong order. When reading `logging` calls, **the call name indicates the level, and the argument inside the parentheses indicates the message**.",
    "side_card_ids": [
      "DEV_logging_001"
    ],
    "project_context": "It distinguishes between INFO, WARNING, and ERROR in server and batch logs and links them to the code to determine issue priority.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "`logging.error(message)` 호출에서 호출 대상과 전달된 메시지, 로그 레벨을 구분해 읽는다.",
    "concept_explanation": {
      "what_it_is": "A call in the form of `object.name(...)` locates the name within an object or module and then passes arguments to execute it.",
      "how_to_read": "`logging.error(\"failed\")` This reads as: \"`logging`\" passes the string \"`failed`\" to the ``error`` call.",
      "key_point": "`error` is the name of the call that selects the log level, not the message content itself.",
      "common_mistake": "`logging.error()` It is easy to assume that this automatically raises a Python exception. This call simply logs an ERROR-level message."
    },
    "teaching_example": {
      "code": "import logging\n\nlogging.warning(\"disk almost full\")",
      "walkthrough": "Pass a string to the `warning()` call to record a WARNING-level message."
    },
    "answer_explanation": {
      "step_by_step": "`logging.error` The call name determines the log level, and ``failed`` is the message to be logged at that level.",
      "why_correct": "This is because `logging.error()` is a call in the Python logging API that logs messages at the ERROR level.",
      "common_wrong_choice": {
        "choice": "INFO",
        "why_wrong": "INFO is the level used by the ``logging.info(\"started\")`` in the previous line.",
        "misread_step": "This is an example where the names of two consecutive logging calls were read in the wrong order."
      },
      "takeaway": "A `logging` call is read by distinguishing **the level by the call name and the message by the argument in parentheses**."
    },
    "target_statement": "logging.error(\"failed\")",
    "focus_span": "logging.error(\"failed\")",
    "focus_mode": "whole_statement",
    "coverage_domain": "logging_monitoring_operations",
    "coverage_topics": [
      "logging",
      "ERROR level",
      "server log",
      "batch log"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PY4_L08_checkpoint_001",
    "level": 8,
    "title": "Saving the checkpoint state by calling `write_text()`",
    "concepts": [
      "call",
      "argument_passing",
      "attribute_access",
      "literal"
    ],
    "reading_goal": "Read the sequence in which a JSON string is first created in a nested call and its result is passed as input to `write_text`.",
    "code": "import json\nfrom pathlib import Path\n\nstate = {\"last_shard\": 42}\nPath(\"checkpoint.json\").write_text(json.dumps(state), encoding=\"utf-8\")",
    "question_type": "multiple_choice",
    "question": "What is the first argument actually passed to `write_text()`?",
    "choices": [
      "the `state` dictionary itself",
      "The JSON string generated by `json.dumps(state)`",
      "Only the number 42",
      "Path object"
    ],
    "answer": "The JSON string generated by `json.dumps(state)`",
    "explanation": "Python evaluates call arguments first. Therefore, `json.dumps(state)` creates a JSON string, and its return value is passed to `write_text()`. `write_text()` is a method that writes a string, and in this case, it receives the return string from the nested `json.dumps()`. Common source of confusion: The `state` dictionary itself is first passed to `json.dumps()`, and the value received by `write_text()` is the return string from that call. Point of Misinterpretation: This occurs when the order in which the inner result is passed to the outer argument in a nested call is overlooked. Nested calls should be read in the order: **inner call result → outer call argument**.",
    "side_card_ids": [],
    "project_context": "It is linked to the checkpoint file code, which saves the progress made in processing long shards and nodes so that the process can be resumed.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "중첩 호출에서 JSON 문자열이 먼저 만들어지고 그 결과가 write_text의 입력으로 전달되는 순서를 읽는다.",
    "concept_explanation": {
      "what_it_is": "`Path(...).write_text(text, encoding=...)` is a method call that writes a string to a file.",
      "how_to_read": "It reads that `write_text()` inside the argument is evaluated before the outer `json.dumps(state)` to create a string.",
      "key_point": "Nested calls are read in the order where the result of the inner call becomes an argument for the outer call.",
      "common_mistake": "It’s easy to assume that the `state` dictionary itself is written to the file as-is. First, `json.dumps()` serializes it into a string."
    },
    "teaching_example": {
      "code": "import json\nfrom pathlib import Path\n\nstate = {\"page\": 3}\nPath(\"state.json\").write_text(json.dumps(state), encoding=\"utf-8\")",
      "walkthrough": "`json.dumps()` converts `state` into a JSON string, and `write_text()` writes that string to a file."
    },
    "answer_explanation": {
      "step_by_step": "Python evaluates call arguments first. Therefore, ``json.dumps(state)`` creates a JSON string, and its return value is passed to ``write_text()``.",
      "why_correct": "`write_text()` is a method that writes a string, and here it receives the return string from the inner `json.dumps()`.",
      "common_wrong_choice": {
        "choice": "the `state` dictionary itself",
        "why_wrong": "The `dict` is first passed to ``json.dumps()``, and the value received by ``write_text()`` is the return string from that call.",
        "misread_step": "This is a case where, in a nested call, the order in which the inner result is passed to the outer argument is skipped."
      },
      "takeaway": "Nested calls are read in the order of **inner call result → outer call argument**."
    },
    "target_statement": "Path(\"checkpoint.json\").write_text(json.dumps(state), encoding=\"utf-8\")",
    "focus_span": "Path(\"checkpoint.json\").write_text(json.dumps(state), encoding=\"utf-8\")",
    "focus_mode": "whole_statement",
    "coverage_domain": "files_paths_serialization",
    "coverage_topics": [
      "checkpoint",
      "resume",
      "json",
      "write_text"
    ],
    "pedagogical_intent": "read_execution_order"
  },
  {
    "id": "PY4_L08_resume_filter_001",
    "level": 8,
    "title": "Use a filtered list comprehension to exclude completed IDs.",
    "concepts": [
      "list_comprehension",
      "membership_test",
      "assignment",
      "literal"
    ],
    "reading_goal": "`not in` Read the sequence that creates a new list containing only the remaining values, excluding completed items where the condition is false.",
    "code": "all_ids = [\"001\", \"002\", \"003\"]\ndone = {\"001\", \"002\"}\npending = [id for id in all_ids if id not in done]\nprint(pending)",
    "question_type": "multiple_choice",
    "question": "What is the list stored in `pending`?",
    "choices": [
      "[\"001\", \"002\"]",
      "[\"001\", \"002\", \"003\"]",
      "[\"003\"]",
      "[]"
    ],
    "answer": "[\"003\"]",
    "explanation": "Since 001 and 002 are in the `done` set, the condition ``not in`` is false. Only 003 is not in `done`, so it is included in the new list. This is because the filtered list comprehension only includes items for which the condition is true. A common source of confusion: [\"001\", \"002\"]—since those two values are already in `done`, they do not satisfy the condition ``not in done``. Misinterpretation: This occurs when the phrase “`not in`” is read as “`in`.” The `resume` filter is interpreted as **collecting only items not in the `done` set into a new list**.",
    "side_card_ids": [
      "DATA_dedup_001"
    ],
    "project_context": "This connects to the code that creates a pending list to ensure that shards and cards that have already been processed are not reprocessed when resuming after a failure.",
    "authoring_version": "V2.3",
    "primary_concept": "list_comprehension",
    "reading_goal_internal": "`not in` 조건이 거짓인 완료 항목을 제외하고 남은 값만 새 리스트로 만드는 순서를 읽는다.",
    "concept_explanation": {
      "what_it_is": "`[x for x in items if condition]` creates a new list containing only the `x` values for which the condition is true.",
      "how_to_read": "It reads that for each `id`, the code checks for `id not in done` and includes in the result only those that are not in `done`.",
      "key_point": "Create a new list `pending` without modifying the original `all_ids` or `done`.",
      "common_mistake": "`not in` It’s easy to misinterpret the condition as meaning to keep only the completed items."
    },
    "teaching_example": {
      "code": "jobs = [\"a\", \"b\", \"c\"]\nfinished = {\"a\"}\npending = [job for job in jobs if job not in finished]",
      "walkthrough": "`a` is omitted from `finished`, leaving only `b` and `c` in the new list."
    },
    "answer_explanation": {
      "step_by_step": "Since 001 and 002 are in the `done` set, the condition ``not in`` is false. Only 003 is not in the `done` set, so it is included in the new list.",
      "why_correct": "A filtered list comprehension includes only those items for which the condition is true in the result.",
      "common_wrong_choice": {
        "choice": "[\"001\", \"002\"]",
        "why_wrong": "Since those two values are already in `done`, they do not satisfy the condition ``not in done``.",
        "misread_step": "This is an instance where `not in` was read as if it were `in`."
      },
      "takeaway": "The resume filter **collects only items not in the complete set into a new list**."
    },
    "target_statement": "pending = [id for id in all_ids if id not in done]",
    "focus_span": "[id for id in all_ids if id not in done]",
    "focus_mode": "subexpression",
    "coverage_domain": "cli_and_automation",
    "coverage_topics": [
      "resume",
      "pending ids",
      "set membership",
      "deduplication"
    ],
    "pedagogical_intent": "interpret_result"
  },
  {
    "id": "PY4_L09_cache_dict_001",
    "level": 9,
    "title": "Return the summary stored in the cache hit immediately",
    "concepts": [
      "return_statement",
      "subscription",
      "if_statement",
      "membership_test",
      "function_definition"
    ],
    "reading_goal": "Read the execution flow that immediately returns the result of a dict lookup when the cache key exists, thereby skipping subsequent calculations.",
    "code": "cache = {\"doc-1\": \"cached summary\"}\n\ndef summarize(doc_id):\n    return f\"new summary for {doc_id}\"\n\ndef get_summary(doc_id):\n    if doc_id in cache:\n        return cache[doc_id]\n    summary = summarize(doc_id)\n    cache[doc_id] = summary\n    return summary\n\nresult = get_summary(\"doc-1\")",
    "question_type": "multiple_choice",
    "question": "`result` What value is stored in [this]?",
    "choices": [
      "cached summary",
      "new summary for doc-1",
      "doc-1",
      "None"
    ],
    "answer": "cached summary",
    "explanation": "Because `doc-1` is in the cache keys, the first `return` executes and the cached summary becomes the function result. This is because `return` immediately ends function execution. Common wrong answer: `new summary for doc-1`. Because of the cache hit, execution never reaches the `summarize` call. Misreading point: This happens when code after `return` is assumed to keep running. Read cache code as **check hit → return immediately → skip expensive computation**.",
    "side_card_ids": [
      "PL_cache_001"
    ],
    "project_context": "This ties into the use of memory caching to avoid repeatedly performing computationally expensive operations, such as document summarization, embedding, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "return_statement",
    "reading_goal_internal": "cache key가 존재할 때 dict lookup 결과를 즉시 반환해 뒤 계산을 건너뛰는 실행 흐름을 읽는다.",
    "concept_explanation": {
      "what_it_is": "`return value` It completes the function execution and returns `value` to the point where it was called.",
      "how_to_read": "First, check the conditions for reaching `return`; I believe the statements below the return statement in the same function are not executed.",
      "key_point": "On a cache hit, return the existing summary immediately so the same computation is not repeated.",
      "common_mistake": "It’s easy to think that `summarize` continues to run even after `return`. A function terminates at the `return` statement."
    },
    "teaching_example": {
      "code": "cache = {\"a\": \"ready\"}\ndef get_value(key):\n    if key in cache:\n        return cache[key]\n    return \"new\"\nresult = get_value(\"a\")",
      "walkthrough": "Because `a` is in the cache, `ready` is returned immediately and the later `return` does not execute."
    },
    "answer_explanation": {
      "step_by_step": "Since doc-1 is in the cache key, the first return statement is executed, and the cached summary becomes the function’s result.",
      "why_correct": "This is because `return` immediately terminates function execution.",
      "common_wrong_choice": {
        "choice": "new summary for doc-1",
        "why_wrong": "Due to a cache hit, the program does not proceed to the point where `summarize` is called.",
        "misread_step": "This is a case where the code is assumed to continue executing after the `return` statement."
      },
      "takeaway": "The cache code is interpreted as **check for a hit → return immediately → skip expensive calculations**."
    },
    "target_statement": "return cache[doc_id]",
    "focus_span": "cache[doc_id]",
    "focus_mode": "subexpression",
    "coverage_domain": "performance_and_compute",
    "coverage_topics": [
      "cache",
      "dict",
      "summary",
      "performance"
    ],
    "pedagogical_intent": "read_execution_order"
  },
  {
    "id": "PY4_L09_prompt_template_001",
    "level": 9,
    "title": "Creating Context and Question prompts using f-strings",
    "concepts": [
      "f_string",
      "return_statement",
      "name_reference",
      "function_definition"
    ],
    "reading_goal": "Read the process of constructing the final prompt string by inserting the current context and question values into the curly braces of the f-string.",
    "code": "def build_prompt(question, docs):\n    context = \"\\n\".join(docs)\n    return f\"Context:\\n{context}\\n\\nQuestion: {question}\"\n\nprompt = build_prompt(\"캐시란?\", [\"캐시는 결과를 저장한다.\", \"반복 계산을 줄인다.\"])",
    "question_type": "multiple_choice",
    "question": "What is the question string that must be included in `prompt`?",
    "choices": [
      "What is a cache?",
      "Python",
      "Question",
      "None"
    ],
    "answer": "What is a cache?",
    "explanation": "The value of “question” in the function call is “`캐시란?`,” and it is inserted exactly as-is into the “`{question}`” placeholder in the f-string. This is because the f-string inserts the current variable value into the string. A common source of confusion: “Python Python” is the value in the teaching example, but the actual “question” in the problem’s function call is “What is a cache?” Point of Misreading: This occurs when the values from the example and the problem are mixed up. The prompt template should be read as **Check variable value → f-string substitution → Final string**.",
    "side_card_ids": [
      "AI_prompt_001"
    ],
    "project_context": "This connects to the workflow of combining the retrieved reference documents and the user’s question into a single RAG prompt and passing it to the LLM.",
    "authoring_version": "V2.3",
    "primary_concept": "f_string",
    "reading_goal_internal": "f-string의 중괄호에 현재 context와 question 값을 넣어 최종 prompt 문자열을 구성하는 과정을 읽는다.",
    "concept_explanation": {
      "what_it_is": "An f-string creates a new string by inserting the result of an expression into the positions marked with ``{expression}`` within the string.",
      "how_to_read": "Find the current value of the name inside the curly braces and combine it with the remaining fixed text in the string.",
      "key_point": "Here, the search document is placed in the \"Context\" field, and the user’s question is placed in the \"Question\" field.",
      "common_mistake": "`{context}` It’s easy to assume that this will be printed verbatim. In an f-string, it is replaced with the actual variable value."
    },
    "teaching_example": {
      "code": "topic = \"Python\"\ntext = f\"Topic: {topic}\"",
      "walkthrough": "Curly braces are replaced with Python syntax, so the text becomes `Topic: Python`."
    },
    "answer_explanation": {
      "step_by_step": "The value of `question` in the function call is ``캐시란?``, and it is inserted as-is into the ``{question}`` placeholder in the f-string.",
      "why_correct": "This is because f-strings insert the current variable value into a string.",
      "common_wrong_choice": {
        "choice": "Python",
        "why_wrong": "\"Python\" is the value in the teaching example, and the question in the actual problem—which involves a function call—is \"What is a cache?\"",
        "misread_step": "This is an example of a case where the values from the example and the problem were mixed up."
      },
      "takeaway": "Read the prompt template as **Check variable values → Substitute f-strings → Final string**."
    },
    "target_statement": "return f\"Context:\\n{context}\\n\\nQuestion: {question}\"",
    "focus_span": "f\"Context:\\n{context}\\n\\nQuestion: {question}\"",
    "focus_mode": "subexpression",
    "coverage_domain": "ai_llm_rag",
    "coverage_topics": [
      "prompt",
      "template",
      "LLM",
      "context",
      "question"
    ],
    "pedagogical_intent": "interpret_result"
  },
  {
    "id": "PY4_L09_evidence_check_001",
    "level": 9,
    "title": "Preventing empty docs with `if not`",
    "concepts": [
      "if_statement",
      "boolean_operation",
      "return_statement",
      "call",
      "literal"
    ],
    "reading_goal": "Use the falsy behavior of an empty list and the `not` operator to analyze the flow that causes the \"insufficient evidence\" branch to execute.",
    "code": "def call_llm(prompt):\n    return \"generated\"\n\ndef build_prompt(question, docs):\n    return \"prompt\"\n\ndef answer(question, docs):\n    if not docs:\n        return \"근거 문서가 부족합니다.\"\n    return call_llm(build_prompt(question, docs))\n\nresult = answer(\"질문\", [])",
    "question_type": "multiple_choice",
    "question": "`result` What is the value of ?",
    "choices": [
      "There is insufficient supporting documentation.",
      "generated",
      "prompt",
      "None"
    ],
    "answer": "There is insufficient supporting documentation.",
    "explanation": "Since an empty list is falsy, `not docs` evaluates to True. The first `return` is executed, returning a “not enough evidence” message. This is because the `if guard` and `return` are executed before the LLM call below. A common source of confusion: the function terminates at the first `return`, so it does not proceed to `call_llm`. Point of misinterpretation: This occurs when one assumes that code continues to execute after the guard return. The evidence guard is interpreted as **detecting an empty input → early return → skipping the model call**.",
    "side_card_ids": [
      "AI_hallucination_001"
    ],
    "project_context": "This is linked to the RAG safety flow, which halts the process with a \"lack of evidence\" status rather than forcing a response when no search results are available.",
    "authoring_version": "V2.3",
    "primary_concept": "if_statement",
    "reading_goal_internal": "빈 리스트의 falsy 성질과 not을 적용해 evidence 부족 branch가 실행되는 흐름을 읽는다.",
    "concept_explanation": {
      "what_it_is": "`if condition:` executes the body of the statement when `condition` is truthy.",
      "how_to_read": "First determine the truth value of `docs`, and if `not` is present, invert that result.",
      "key_point": "In empty docs, the guard triggers before the LLM is called and returns a “insufficient evidence” message.",
      "common_mistake": "It’s easy to assume that an empty list is False, so the `if` statement won’t execute. However, the preceding `not` changes it to True."
    },
    "teaching_example": {
      "code": "items = []\nif not items:\n    result = \"empty\"\nelse:\n    result = \"ready\"",
      "walkthrough": "An empty list is falsy, and applying `not` makes the condition True, so `result` becomes `empty`."
    },
    "answer_explanation": {
      "step_by_step": "Since an empty list is falsy, ``not docs`` evaluates to `True`. The first `return` statement is executed, returning a \"not found\" message.",
      "why_correct": "This is because the `if guard` and `return` are executed before the LLM call below.",
      "common_wrong_choice": {
        "choice": "generated",
        "why_wrong": "The function terminates at the first `return` statement, so it does not proceed to `call_llm`.",
        "misread_step": "This is the case where the code continues to execute after the `guard return`."
      },
      "takeaway": "\"evidence guard\" is interpreted as **detecting an empty input → early return → skipping the model call**."
    },
    "target_statement": "if not docs:",
    "focus_span": "if not docs:",
    "focus_mode": "whole_statement",
    "coverage_domain": "ai_llm_rag",
    "coverage_topics": [
      "evidence",
      "validation",
      "RAG",
      "guard",
      "LLM"
    ],
    "pedagogical_intent": "read_execution_order"
  },
  {
    "id": "PY4_L09_rerank_flow_001",
    "level": 9,
    "title": "Understanding the meaning of `reverse=True` in `rerank sort`",
    "concepts": [
      "argument_passing",
      "call",
      "lambda_expression",
      "literal"
    ],
    "reading_goal": "The `reverse` keyword argument in the `sort` call is interpreted as reversing the order so that candidates with higher scores appear first.",
    "code": "scored = [(0.2, \"doc-a\"), (0.9, \"doc-b\"), (0.5, \"doc-c\")]\nscored.sort(key=lambda pair: pair[0], reverse=True)\ntop_hit = scored[0][1]",
    "question_type": "multiple_choice",
    "question": "`top_hit` What is the value of ?",
    "choices": [
      "doc-a",
      "doc-b",
      "doc-c",
      "0.9"
    ],
    "answer": "doc-b",
    "explanation": "The scores are 0.2, 0.9, and 0.5, and the list is sorted in descending order with `reverse=True`. The first item is `doc-b` with the highest score of 0.9 because the `reverse` keyword argument in `sort` is set to `True`. A common source of confusion: doc-a. Since doc-a has the lowest score (0.2), it appears later in the descending order. Point of misreading: This occurs when the `reverse=True` setting is ignored and the list is read in ascending order. The `rerank` function should be interpreted as **score key → reverse option → order of top candidates**.",
    "side_card_ids": [
      "AI_rag_001"
    ],
    "project_context": "It connects to the RAG reranker workflow, which re-ranks the initial search candidates using more precise scores to select the top supporting documents.",
    "authoring_version": "V2.3",
    "primary_concept": "argument_passing",
    "reading_goal_internal": "sort 호출의 reverse keyword argument가 score가 높은 후보를 앞에 두도록 순서를 뒤집는 의미를 읽는다.",
    "concept_explanation": {
      "what_it_is": "`reverse=True` is a keyword argument passed by name to a function or method call.",
      "how_to_read": "Check the keyword name and boolean value to see if the option is enabled.",
      "key_point": "In the `sort` function, setting `reverse=True` sorts the list in descending order, which is the opposite of the default ascending order.",
      "common_mistake": "It’s easy to assume that `reverse=True` simply creates a copy of the original list without sorting it. ``list.sort()`` reverses the order of the list itself."
    },
    "teaching_example": {
      "code": "pairs = [(1, \"a\"), (3, \"b\"), (2, \"c\")]\npairs.sort(key=lambda pair: pair[0], reverse=True)\nfirst = pairs[0][1]",
      "walkthrough": "Sorting in descending order based on the first value makes \"b\" (with a score of 3) the first item."
    },
    "answer_explanation": {
      "step_by_step": "The scores are 0.2, 0.9, and 0.5, and the list is sorted in descending order with `reverse=True`. The first item is the one with the highest score, 0.9, for doc-b.",
      "why_correct": "This is because the `reverse` keyword argument of `sort` is set to `True`.",
      "common_wrong_choice": {
        "choice": "doc-a",
        "why_wrong": "Since doc-a has the lowest score of 0.2, it appears further down the list when sorted in descending order.",
        "misread_step": "This is a case where `reverse=True` is ignored and the code is read in ascending order."
      },
      "takeaway": "\"rerank\" should be read as **\"score key → reverse option → order of top candidates\"**."
    },
    "target_statement": "scored.sort(key=lambda pair: pair[0], reverse=True)",
    "focus_span": "reverse=True",
    "focus_mode": "subexpression",
    "coverage_domain": "ai_llm_rag",
    "coverage_topics": [
      "rerank",
      "retrieval",
      "score",
      "sorting",
      "top k"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PY4_L10_node_candidate_extract_001",
    "level": 10,
    "title": "Check if a term in lowercase appears within the chunk text using `in`",
    "concepts": [
      "membership_test",
      "call",
      "subscription",
      "if_statement"
    ],
    "reading_goal": "Read the process of normalizing two strings using `lower()` and then determining whether the left `term` string is contained within the right `chunk text` using ``in``.",
    "code": "TERMS = [\"RAG\"]\nchunk = {\"id\": \"c1\", \"text\": \"A rag pipeline uses retrieval.\"}\nrows = []\n\nfor term in TERMS:\n    if term.lower() in chunk[\"text\"].lower():\n        rows.append({\"term\": term, \"chunk_id\": chunk[\"id\"]})\n\nprint(rows)",
    "question_type": "output_prediction",
    "question": "What is the result of `term.lower() in chunk[\"text\"].lower()` in the first iteration?",
    "choices": [
      "True",
      "False",
      "RAG",
      "c1"
    ],
    "answer": "True",
    "explanation": "1) The first term is RAG. 2) ``term.lower()`` returns RAG. 3) The result of `lower()` on `chunk text` includes ``rag``. 4) Therefore, the result of the membership test is True. 5) This True is used in the `if` condition, causing the candidate row to be added.",
    "side_card_ids": [
      "CS_graph_001"
    ],
    "project_context": "This is connected to the preprocessing context in the KG node pass, where simple candidates are collected to determine whether specific terms appear in the chunk text.",
    "authoring_version": "V2.3",
    "primary_concept": "membership_test",
    "reading_goal_internal": "두 문자열을 lower()로 정규화한 뒤 왼쪽 term 문자열이 오른쪽 chunk text에 포함되는지 `in`으로 판정하는 과정을 읽는다.",
    "concept_explanation": {
      "what_it_is": "`x in container` checks whether the value on the left is contained within the container on the right, returning True or False.",
      "how_to_read": "If there are calls on both sides, first obtain the return values of each call, and then perform a ``in`` check using those two actual values.",
      "key_point": "In this code, instead of directly comparing case sensitivity, it performs a **membership check on the results of `lower()`**.",
      "common_mistake": "`in` It’s easy to think that only the exact same entire string evaluates to True. In strings, if the left substring is contained within the right one, the result is True."
    },
    "teaching_example": {
      "code": "term = \"API\"\ntext = \"Fast api server\"\nmatched = term.lower() in text.lower()\nprint(matched)",
      "walkthrough": "`api` Since it is contained within ``fast api server``, `matched` returns `True`."
    },
    "answer_explanation": {
      "step_by_step": "1) The first term is RAG. 2) ``term.lower()`` returns RAG. 3) The result of `lower()` on `chunk text` includes ``rag``. 4) Therefore, the result of the membership test is True. 5) This True is used in the `if` condition, causing the candidate row to be added.",
      "why_correct": "If you replace both `lower()` results with their actual strings, `rag` is contained in the string on the right, so the result is True.",
      "common_wrong_choice": {
        "choice": "False",
        "why_wrong": "Although the original text uses “RAG” and “rag” with different capitalization, the difference disappears because both are converted to lowercase using `lower()` before comparison.",
        "misread_step": "Before evaluating `in`, first determine which strings are returned by both method calls."
      },
      "takeaway": "The membership test is read in the following order: **calculate the operand value → check for inclusion → True/False result**."
    },
    "target_statement": "term.lower() in chunk[\"text\"].lower()",
    "focus_span": "term.lower() in chunk[\"text\"].lower()",
    "focus_mode": "subexpression",
    "coverage_domain": "ai_llm_rag",
    "coverage_topics": [
      "kg",
      "node_candidate",
      "extraction",
      "substring_match"
    ],
    "pedagogical_intent": "interpret_result"
  },
  {
    "id": "PY4_L10_edge_promotion_001",
    "level": 10,
    "title": "Check whether both the `score` and `evidence` conditions are satisfied using “and”",
    "concepts": [
      "boolean_operation",
      "comparison",
      "subscription",
      "if_statement"
    ],
    "reading_goal": "It evaluates the comparison on the left side of “and,” and when that is true, checks the comparison on the right side to determine whether both conditions are true.",
    "code": "candidate = {\"score\": 0.85, \"evidence_count\": 2}\n\nif candidate[\"score\"] >= 0.8 and candidate[\"evidence_count\"] >= 2:\n    status = \"promoted\"\nelse:\n    status = \"rejected\"\n\nprint(status)",
    "question_type": "output_prediction",
    "question": "What is the result of the entire `and` condition in this candidate?",
    "choices": [
      "True",
      "False",
      "0.85",
      "2"
    ],
    "answer": "True",
    "explanation": "1) Since `score` is 0.85, `0.85 >= 0.8` is True. 2) Since the left side is True, the right side is also checked. 3) Since `evidence_count` is 2, `2 >= 2` is also True. 4) Combining the two results with `and` yields True. 5) Therefore, the “promoted” branch is selected.",
    "side_card_ids": [
      "CS_graph_001"
    ],
    "project_context": "It is connected to an evidence-first validation workflow that promotes KG edge candidates based on multiple criteria, such as scores and the number of supporting examples.",
    "authoring_version": "V2.3",
    "primary_concept": "boolean_operation",
    "reading_goal_internal": "and의 왼쪽 비교부터 평가하고 참일 때 오른쪽 비교까지 확인해 두 조건이 모두 참인지 판단한다.",
    "concept_explanation": {
      "what_it_is": "`A and B` is a logical operation where the result is true only if both A and B are truthy.",
      "how_to_read": "Evaluate A on the left first; if A is true, evaluate B on the right as well, and combine the two final results.",
      "key_point": "Satisfying only one of the two thresholds does not make the entire condition true. **Both must be true.**",
      "common_mistake": "It’s easy to conclude that the entire `if` statement is true the moment the `score` condition is true. However, you must also check the `evidence` condition to the right of `and`."
    },
    "teaching_example": {
      "code": "score = 0.9\nevidence = 1\nready = score >= 0.8 and evidence >= 2\nprint(ready)",
      "walkthrough": "Although the left side is True, the `evidence` condition is False, so the overall `and` result is False."
    },
    "answer_explanation": {
      "step_by_step": "1) Since `score` is 0.85, `0.85 >= 0.8` is True. 2) Since the left side is True, the right side is also checked. 3) Since `evidence_count` is 2, `2 >= 2` is also True. 4) Combining the two results with `and` yields True. 5) Therefore, the “promoted” branch is selected.",
      "why_correct": "Since both conditional expressions are True, the result of `and` is also True.",
      "common_wrong_choice": {
        "choice": "False",
        "why_wrong": "Since the second condition is `>= 2` rather than `> 2`, the condition is satisfied even if evidence_count is exactly 2.",
        "misread_step": "Calculate each comparison operator accurately, then combine them with “and” at the end."
      },
      "takeaway": "`and` is read in the following order: **evaluate the left-hand condition → evaluate the right-hand condition if necessary → combine the two results**."
    },
    "target_statement": "candidate[\"score\"] >= 0.8 and candidate[\"evidence_count\"] >= 2",
    "focus_span": "candidate[\"score\"] >= 0.8 and candidate[\"evidence_count\"] >= 2",
    "focus_mode": "subexpression",
    "coverage_domain": "ai_llm_rag",
    "coverage_topics": [
      "kg",
      "edge",
      "promotion",
      "evidence_threshold"
    ],
    "pedagogical_intent": "read_execution_order"
  },
  {
    "id": "PY4_L10_label_adjudication_001",
    "level": 10,
    "title": "Get the number of occurrences of \"KEEP\" using the `labels.count('KEEP')` call",
    "concepts": [
      "call",
      "attribute_access",
      "comparison",
      "if_statement"
    ],
    "reading_goal": "Read the flow where calling the `count` method on a list returns the number of occurrences of the specified value as an integer, and that value is used in the next comparison.",
    "code": "labels = [\"KEEP\", \"REJECT\", \"KEEP\"]\n\nif labels.count(\"KEEP\") >= 2:\n    result = \"KEEP\"\nelif \"REJECT\" in labels:\n    result = \"REJECT\"\nelse:\n    result = \"REVIEW\"\n\nprint(result)",
    "question_type": "meaning_choice",
    "question": "What value does the `labels.count(\"KEEP\")` call itself return?",
    "choices": [
      "2",
      "True",
      "KEEP",
      "3"
    ],
    "answer": "2",
    "explanation": "1) Checking the items in `labels` reveals KEEP, REJECT, and KEEP. 2) `count('KEEP')` counts the number of items matching \"KEEP.\" 3) The first and third items match, for a total of 2. 4) Therefore, the return value of the method call itself is the integer 2. 5) The subsequent code compares this 2 to `>= 2`, resulting in True.",
    "side_card_ids": [
      "AI_labeling_001"
    ],
    "project_context": "It is integrated into the dataset adjudication workflow, which determines the final verdict based on the number of labels provided by multiple reviewers.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "리스트의 count 메서드 호출이 지정한 값의 등장 횟수를 정수로 반환하고 그 값이 다음 비교에 사용되는 흐름을 읽는다.",
    "concept_explanation": {
      "what_it_is": "`list.count(value)` is a method call that counts how many times an item equal to `value` appears in a list and returns an integer.",
      "how_to_read": "Check the actual items in the list of call targets, count the number of items—such as the value inside the parentheses—and read that number as the result of the call.",
      "key_point": "The result of a `count` call is not the `KEEP` string itself or `True`/`False`, but rather the **integer count of occurrences**.",
      "common_mistake": "Since `>= 2` appears afterward, it’s easy to assume that `count` itself returns `True`. First, `count` produces a number, and then the comparison produces a Boolean."
    },
    "teaching_example": {
      "code": "votes = [\"YES\", \"NO\", \"YES\"]\nyes_count = votes.count(\"YES\")\nprint(yes_count)",
      "walkthrough": "Since there are two instances of \"YES,\" the `count` call returns the integer 2."
    },
    "answer_explanation": {
      "step_by_step": "1) Checking the items in `labels` reveals KEEP, REJECT, and KEEP. 2) `count('KEEP')` counts the number of items matching \"KEEP.\" 3) The first and third items match, for a total of 2. 4) Therefore, the return value of the method call itself is the integer 2. 5) The subsequent code compares this 2 to `>= 2`, resulting in True.",
      "why_correct": "Since the string \"KEEP\" appears exactly twice, the return value of the `count` call is `2`.",
      "common_wrong_choice": {
        "choice": "True",
        "why_wrong": "`True` is the result of the next step, which compares the 2 returned by `count` with `>= 2`.",
        "misread_step": "Read the result of the method call separately from the result of the outer comparison expression that uses that result."
      },
      "takeaway": "If a method call appears within an expression, **the return value of the call is evaluated first and then used in the outer operation**."
    },
    "target_statement": "labels.count(\"KEEP\")",
    "focus_span": "labels.count(\"KEEP\")",
    "focus_mode": "subexpression",
    "coverage_domain": "data_processing_and_analysis",
    "coverage_topics": [
      "labeling",
      "adjudication",
      "dataset",
      "vote_count"
    ],
    "pedagogical_intent": "interpret_result"
  },
  {
    "id": "PY4_L10_shard_status_001",
    "level": 10,
    "title": "Generate 1 value for each DONE shard using a generator expression",
    "concepts": [
      "generator_expression",
      "for_statement",
      "comparison",
      "call"
    ],
    "reading_goal": "Read the execution sequence where the generator expression iterates through the statuses and generates a 1 only for the iterations marked as DONE, passing the result to the outer sum() function.",
    "code": "statuses = [\"DONE\", \"DONE\", \"FAILED\", \"PENDING\"]\ndone = sum(1 for status in statuses if status == \"DONE\")\nprint(done)",
    "question_type": "meaning_choice",
    "question": "How many times is the number `sum()`, which this generator expression passes to `1`, generated in total?",
    "choices": [
      "No. 1",
      "No. 2",
      "No. 3",
      "No. 4"
    ],
    "answer": "No. 2",
    "explanation": "1) The first status is DONE, so the condition is True, and it generates 1. 2) The second is also DONE, so it generates 1 again. 3) FAILED and PENDING have False conditions, so they do not generate any values. 4) Therefore, the generator yields two values: 1 and 1. 5) The outer `sum()` function adds these together to produce 2.",
    "side_card_ids": [
      "CS_batch_shard_001"
    ],
    "project_context": "It connects to code that aggregates the number of completed shards in large-scale LLM and data processing tasks to determine the scope of re-execution and the remaining work.",
    "authoring_version": "V2.3",
    "primary_concept": "generator_expression",
    "reading_goal_internal": "generator expression이 statuses를 순회하면서 DONE인 회차에만 1을 만들어 바깥 sum()에 전달하는 실행 순서를 읽는다.",
    "concept_explanation": {
      "what_it_is": "A generator expression, such as `(expression for variable in iterable if condition)`, does not generate a list of values all at once, but instead generates the necessary values sequentially as they are needed.",
      "how_to_read": "First, retrieve one current value from the iteration target, and if there is an if condition, check the condition. Only in iterations where the condition is true is the value of the first expression generated.",
      "key_point": "The `1 for ... if ...` in this card does not output the status string as-is, but rather generates **one number 1 for each item marked as DONE**.",
      "common_mistake": "It is easy to assume that a value of 1 is created four times because of all the entries in `statuses`. However, a value is generated only for the iterations that pass the subsequent `if` condition."
    },
    "teaching_example": {
      "code": "states = [\"OK\", \"FAIL\", \"OK\"]\ncount = sum(1 for state in states if state == \"OK\")\nprint(count)",
      "walkthrough": "Since 1 is generated only in the first and third iterations (which are OK), sum() returns 2."
    },
    "answer_explanation": {
      "step_by_step": "1) The first status is DONE, so the condition is True, and it generates 1. 2) The second is also DONE, so it generates 1 again. 3) FAILED and PENDING have False conditions, so they do not generate any values. 4) Therefore, the generator yields two values: 1 and 1. 5) The outer `sum()` function adds these together to produce 2.",
      "why_correct": "Since the \"DONE\" state occurs only twice, and 1 is returned only in the run that passes the generator’s if condition, the correct answer is 2.",
      "common_wrong_choice": {
        "choice": "No. 4",
        "why_wrong": "Although there are four items in `statuses`, the `FAILED` and `PENDING` entries do not generate the preceding `1` because the `if` condition is False.",
        "misread_step": "Distinguish between the number of iterations and the number of times actual values are generated to verify the result of the `if` statement for each iteration."
      },
      "takeaway": "Read a generator expression with a condition as **iterate → check condition → produce a value only for passing iterations**."
    },
    "target_statement": "1 for status in statuses if status == \"DONE\"",
    "focus_span": "1 for status in statuses if status == \"DONE\"",
    "focus_mode": "subexpression",
    "coverage_domain": "performance_and_compute",
    "coverage_topics": [
      "shard",
      "batch_status",
      "completion_count",
      "resume"
    ],
    "pedagogical_intent": "read_execution_order"
  }
]
