[
  {
    "id": "L01_len_001",
    "level": 1,
    "title": "Counting with `len()`",
    "concepts": [
      "print",
      "len",
      "list"
    ],
    "reading_goal": "I understand that `len(items)` returns the number of elements in the list.",
    "code": "items = [\"UAM\", \"ADAS\", \"Robotics\"]\ncount = len(items)\nprint(count)",
    "question_type": "output_prediction",
    "question": "What is the output of `len(items)`?",
    "choices": [
      "1",
      "2",
      "3",
      "UAM"
    ],
    "answer": "3",
    "explanation": "The `items` list contains three elements, so `len(items)` returns 3. What `len()` counts depends on the object: characters in a string, elements in a list, and keys in a dictionary. It does not measure a dictionary's values themselves or the magnitude of a number. `len()` is often used to check whether data is empty before a loop or to verify how many results were produced.",
    "side_card_ids": [
      "SC_list_why_used"
    ],
    "project_context": "This is a pattern often used when checking the number of nodes or documents in a list."
  },
  {
    "id": "L02_var_flow_001",
    "level": 2,
    "title": "Tracking the Values Assigned to Variables",
    "concepts": [
      "variable",
      "assignment",
      "print"
    ],
    "reading_goal": "It reads the flow where the value stored in a variable is used in the next line.",
    "code": "label = \"LiDAR\"\nname = label\nprint(name)",
    "question_type": "output_prediction",
    "question": "After copying from \"label\" to \"name,\" what is the final output?",
    "choices": [
      "label",
      "name",
      "LiDAR",
      "None"
    ],
    "answer": "LiDAR",
    "explanation": "Since the value \"LiDAR\" stored in `label` is copied to `name`, `print(name)` outputs \"LiDAR.\" In this case, the copy operation allows the value to be tracked under the new name. To determine the exact final output, you must check whether there are any subsequent lines where `label` or `name` is reassigned. When dealing with variables, it is safest to trace the code sequentially, starting with the right-hand side expression and then storing the result in the left-hand side variable.",
    "side_card_ids": [
      "SC_variable_flow"
    ],
    "project_context": "When reading code, it is important to follow the flow of how values move from one variable to another."
  },
  {
    "id": "L03_dict_001",
    "level": 3,
    "title": "Retrieving a value by its key from a dictionary",
    "concepts": [
      "print",
      "dict",
      "key",
      "value"
    ],
    "reading_goal": "I read that `node[\"kind\"]` retrieves the value of `kind`.",
    "code": "node = {\"label\": \"LiDAR\", \"kind\": \"Sensor\"}\nprint(node[____])",
    "question_type": "blank",
    "question": "Which key belongs in the blank so that the code prints Sensor?",
    "choices": [
      "\"label\"",
      "\"kind\"",
      "\"Sensor\"",
      "\"node\""
    ],
    "answer": "\"kind\"",
    "explanation": "The blank needs the key \"kind\", not the value \"Sensor\". Then `node[\"kind\"]` retrieves \"Sensor\", and `print` displays Sensor. In dictionary questions, distinguish the key placed inside the brackets from the value retrieved by that key. Bracket access raises `KeyError` when the key is missing, so consider `get()` when a key may not exist.",
    "side_card_ids": [
      "SC_dict_json"
    ],
    "project_context": "A single KG node is often represented as a dictionary."
  },
  {
    "id": "L03_get_001",
    "level": 3,
    "title": "Reading dict.get()",
    "concepts": [
      "print",
      "dict",
      "get",
      "missing_key"
    ],
    "reading_goal": "I understand that `row.get(\"doc_id\")` is code intended to safely retrieve the `doc_id` value.",
    "code": "row = {\"label\": \"Radar\"}\nvalue = row.get(\"doc_id\", \"NO_DOC\")\nprint(value)",
    "question_type": "output_prediction",
    "question": "What is the output of `row.get('doc_id', 'NO_DOC')`?",
    "choices": [
      "Radar",
      "doc_id",
      "NO_DOC",
      "None"
    ],
    "answer": "NO_DOC",
    "explanation": "Since the `row` does not contain a `doc_id` key, `get()` returns the default value 'NO_DOC' provided as the second argument. `dict.get` returns the default value without raising an error when the key is missing. This is a good way to create a safe fallback when reading configuration values or optional fields.",
    "side_card_ids": [
      "SC_get_safety"
    ],
    "project_context": "Since there may be missing fields in the actual data, `get()` is frequently used."
  },
  {
    "id": "L04_for_if_001",
    "level": 4,
    "title": "Selecting the Desired Item Using \"for\" and \"if\"",
    "concepts": [
      "print",
      "for",
      "if",
      "append",
      "dict",
      "list"
    ],
    "reading_goal": "Read the flowchart that uses loops and conditional statements to select only the Sensor nodes.",
    "code": "nodes = [\n    {\"label\": \"LiDAR\", \"kind\": \"Sensor\"},\n    {\"label\": \"UAM\", \"kind\": \"System\"},\n    {\"label\": \"Radar\", \"kind\": \"Sensor\"}\n]\n\nselected = []\n\nfor node in nodes:\n    if node[\"kind\"] == \"Sensor\":\n        selected.append(node[\"label\"])\n\nprint(selected)",
    "question_type": "output_prediction",
    "question": "What will ultimately be included in `selected`?",
    "choices": [
      "Only ['UAM'] remains",
      "Only ['Sensor'] remains",
      "It becomes an empty list []",
      "['LiDAR', 'Radar'] remain"
    ],
    "answer": "['LiDAR', 'Radar'] remain",
    "explanation": "The `for` loop iterates through multiple items one by one, while the `if` statement selects and processes only those items that meet the condition. In this example, only the labels for LiDAR and Radar with a `kind` of \"Sensor\" are added to `selected`. You need to examine the process step by step: first, which key in the dictionary is checked within the loop, and then, which value is appended after the condition is met. The final list consists only of the items that passed the condition, not all items.",
    "side_card_ids": [
      "SC_filtering_pattern"
    ],
    "project_context": "This is a common pattern when selecting only a specific type of node candidate."
  },
  {
    "id": "L05_function_chain_001",
    "level": 5,
    "title": "Tracking Function Call Results",
    "concepts": [
      "function",
      "print",
      "def",
      "return",
      "strip",
      "lower"
    ],
    "reading_goal": "Understand the flow in which values are passed into a function and the values returned by the function are printed.",
    "code": "def normalize_label(label):\n    return label.strip().lower()\n\nresult = normalize_label(\"  LiDAR  \")\nprint(result)",
    "question_type": "output_prediction",
    "question": "What is the final output after applying `strip()` and `lower()` in sequence?",
    "choices": [
      "LiDAR",
      "  LiDAR  ",
      "lidar",
      "LIDAR"
    ],
    "answer": "lidar",
    "explanation": "Since `strip()` first removes leading and trailing whitespace from the string, and `lower()` converts the remaining characters to lowercase, the output is the cleaned-up lowercase value. In a chained sequence, the result of the left method becomes the input for the right method, so you simply follow the sequence step by step. Therefore, the output is “lidar.”",
    "side_card_ids": [
      "SC_normalization"
    ],
    "project_context": "Label normalization is important for removing duplicates and performing canonicalization in KG."
  },
  {
    "id": "L06_json_loads_001",
    "level": 6,
    "title": "Converting a JSON string to a dictionary",
    "concepts": [
      "import",
      "print",
      "json.loads",
      "dict",
      "json"
    ],
    "reading_goal": "Understand the process by which `json.loads(line)` converts a string into a dictionary.",
    "code": "import json\n\nline = \"{\\\"label\\\": \\\"LiDAR\\\", \\\"kind\\\": \\\"Sensor\\\"}\"\nrow = json.loads(line)\nprint(row[\"label\"])",
    "question_type": "output_prediction",
    "question": "What is the output of this code?",
    "choices": [
      "line",
      "label",
      "LiDAR",
      "Sensor"
    ],
    "answer": "LiDAR",
    "explanation": "`line` is still a JSON-formatted string. `json.loads(line)` converts that string into a Python value; because the outer JSON form in this example is an object `{}`, the result is a dictionary. `row[\"label\"]` then retrieves \"LiDAR\", so the output is LiDAR. Invalid JSON syntax can raise `JSONDecodeError`.",
    "side_card_ids": [
      "SC_json_jsonl"
    ],
    "project_context": "This is the core function that converts each line into a dictionary when reading a JSONL file."
  },
  {
    "id": "L06_jsonl_read_001",
    "level": 6,
    "title": "JSONL File Reading Workflow",
    "concepts": [
      "import",
      "print",
      "open",
      "with",
      "for",
      "json.loads",
      "jsonl"
    ],
    "reading_goal": "It reads JSONL line by line and converts each line into a dictionary.",
    "code": "import json\n\nwith open(\"nodes.jsonl\", \"r\", encoding=\"utf-8\") as f:\n    for line in f:\n        row = json.loads(line)\n        print(row[\"label\"])",
    "question_type": "meaning_choice",
    "question": "What is the main function of this code?",
    "choices": [
      "Convert a CSV file to Excel.",
      "Classify images.",
      "Start the web server.",
      "Read the JSONL file line by line and print the label."
    ],
    "answer": "Read the JSONL file line by line and print the label.",
    "explanation": "Open the file using `with open`, read it line by line using `for line in f`, and convert it to a dictionary using `json.loads`. A JSONL file contains one JSON object per line. By reading it line by line and parsing it with `json.loads`, even large datasets can be processed sequentially with ease.",
    "side_card_ids": [
      "SC_jsonl_llm_dataset"
    ],
    "project_context": "This is the basic pattern for reading chunks, nodes, and edges files."
  },
  {
    "id": "L07_pipeline_001",
    "level": 7,
    "title": "Reading the load-filter-write structure",
    "concepts": [
      "def",
      "function",
      "pipeline",
      "load_jsonl",
      "filter",
      "write_jsonl",
      "main"
    ],
    "reading_goal": "Read about the structure of a small program, which is divided into input, processing, and output.",
    "code": "def load_jsonl(path):\n    ...\n\ndef filter_nodes(rows, kind):\n    ...\n\ndef write_jsonl(rows, path):\n    ...\n\ndef main():\n    rows = load_jsonl(\"nodes.jsonl\")\n    selected = filter_nodes(rows, \"Sensor\")\n    write_jsonl(selected, \"sensor_nodes.jsonl\")",
    "question_type": "meaning_choice",
    "question": "What is the order of execution in `main()`?",
    "choices": [
      "Download the website and save it as a file.",
      "Convert a PDF to an image file.",
      "Create a new folder and only rename a file.",
      "Select only the Sensor nodes from `nodes.jsonl` and save them to `sensor_nodes.jsonl`."
    ],
    "answer": "Select only the Sensor nodes from `nodes.jsonl` and save them to `sensor_nodes.jsonl`.",
    "explanation": "Inside the `main()` function, `load`, `filter`, and `write` are called in that order. This creates a pipeline flow for reading, filtering, and writing data. This structure separates the input, processing, and output stages, making debugging and testing easier. Therefore, the correct answer is “Select only the Sensor nodes from `nodes.jsonl` and save them to `sensor_nodes.jsonl`.”",
    "side_card_ids": [
      "SC_pipeline_io"
    ],
    "project_context": "Most small batch scripts can be understood as following an input-processing-output structure."
  },
  {
    "id": "L08_reverse_import_001",
    "level": 8,
    "title": "Inferring Functions from the Import List",
    "concepts": [
      "import",
      "pathlib",
      "json",
      "argparse"
    ],
    "reading_goal": "Look at the import statements to infer what kind of tasks the program performs.",
    "code": "import json\nimport argparse\nfrom pathlib import Path",
    "question_type": "reverse_inference",
    "question": "Looking at this list of imports, which program feature seems the most plausible?",
    "choices": [
      "3D game rendering software",
      "Music Player",
      "Image editing software",
      "A script that processes JSON data by accepting a file path and command-line options"
    ],
    "answer": "A script that processes JSON data by accepting a file path and command-line options",
    "explanation": "`json` is commonly used for JSON conversion, `argparse` for command-line options, and `Path` for finding and handling file paths. Together, these imports make ‘A script that processes JSON data by accepting a file path and command-line options’ the most plausible choice. Imports are clues, not proof of what the program actually does, so confirm the inference by checking the later function calls.",
    "side_card_ids": [
      "SC_import_pm_reading"
    ],
    "project_context": "Even just by looking at the `import` statements, you can get a general idea of the code's functionality before reading the entire code."
  },
  {
    "id": "L09_function_selection_001",
    "level": 9,
    "title": "Selecting the Functions Needed for the Functional Goals",
    "concepts": [
      "for",
      "import",
      "print",
      "Path.glob",
      "json.loads",
      "set",
      "csv",
      "pandas"
    ],
    "reading_goal": "Break a requirement into file discovery, JSON parsing, deduplication, and CSV output, then choose the function used for file discovery.",
    "code": "from pathlib import Path\nimport json\n\nfiles = list(Path(\"data\").glob(\"*.json\"))\nrows = []\nfor path in files:\n    rows.append(json.loads(path.read_text(encoding=\"utf-8\")))\nprint(len(rows))",
    "question_type": "function_selection",
    "question": "Which function is used to find multiple `.json` files in the `data` folder?",
    "choices": [
      "json.loads()",
      "list()",
      "set()",
      "Path.glob()"
    ],
    "answer": "Path.glob()",
    "explanation": "`Path(\"data\").glob(\"*.json\")` finds file paths in the data folder that match the pattern, so the answer is `Path.glob()`. `json.loads()` parses JSON text, while `set()` can help remove duplicates. Writing `summary.csv` requires another tool such as the `csv` module or pandas. Read a larger requirement by matching one tool to each step.",
    "side_card_ids": [],
    "project_context": "This is an exercise in breaking down required features into components from a project manager's perspective."
  },
  {
    "id": "L10_risk_001",
    "level": 10,
    "title": "Identifying Risks in Unstable Code",
    "concepts": [
      "for",
      "print",
      "encoding",
      "missing_key",
      "import",
      "error_handling"
    ],
    "reading_goal": "Identify a missing-key failure that can remain even after the import and file encoding are specified.",
    "code": "import json\n\nwith open(\"chunks.jsonl\", \"r\", encoding=\"utf-8\") as f:\n    for line in f:\n        row = json.loads(line)\n        print(row[\"doc_id\"])",
    "question_type": "risk_finding",
    "question": "Which data-dependent risk still remains in this code?",
    "choices": [
      "No encoding was specified",
      "It won't run without the JSON import.",
      "If a line is missing the `doc_id`, an error occurs.",
      "There is no risk"
    ],
    "answer": "If a line is missing the `doc_id`, an error occurs.",
    "explanation": "The code now imports `json` and explicitly uses UTF-8. However, `row[\"doc_id\"]` assumes that every parsed row contains that key, so a row without it raises `KeyError`. The answer is ‘If a line is missing the `doc_id`, an error occurs.’ Use `row.get(\"doc_id\")` for an optional field, or raise a clear validation error immediately when `doc_id` is required.",
    "side_card_ids": [
      "SC_pm_code_review"
    ],
    "project_context": "This is a training exercise for PMs to identify risk points that need to be verified with developers."
  }
]
