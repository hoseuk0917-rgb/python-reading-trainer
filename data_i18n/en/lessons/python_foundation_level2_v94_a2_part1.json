[
  {
    "id": "PYF94_A2_L02_IF_001",
    "level": 2,
    "title": "Execute the indented block if the condition is true",
    "concepts": [
      "if_statement",
      "comparison",
      "call"
    ],
    "reading_goal": "Read the core behavior of an if statement in the order of actual execution, tracking values and flow.",
    "code": "score = 80\nif score >= 60:\n    print(\"pass\")",
    "question_type": "meaning_choice",
    "question": "When `score` is 80, does the `if` block execute?",
    "choices": [
      "It is executed",
      "Skip",
      "Goes to `else`",
      "Error"
    ],
    "answer": "It is executed",
    "explanation": "1) score is 80. 2) `80 >= 60` is True. 3) The if block executes. 4) `\"pass\"` is printed. Following the actual values through the final step, the result being asked for is `is executed`. Common wrong answer: skipped. The if block is skipped only when the condition is False; because 80 is at least 60, the condition is True. Misread point: recheck the value of the condition and the branch selected by it.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "if_statement",
    "reading_goal_internal": "if_statement의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`if` executes the indented block below only when the condition is `True`.",
      "how_to_read": "First, the condition is evaluated. If it is true, the indented lines are executed; if false, that block is skipped.",
      "key_point": "Even if the code inside the `if` statement is written on the screen, it will not execute if the condition is false.",
      "common_mistake": "It’s easy to assume that code is read from top to bottom and that indented lines are always executed. The `if` statement first determines whether that block should be executed."
    },
    "teaching_example": {
      "code": "age = 18\nif age >= 13:\n    print(\"teen+\")",
      "walkthrough": "Since `18 >= 13` is True, the indented `print` statement is executed."
    },
    "answer_explanation": {
      "step_by_step": "1) `score` is 80. 2) `80 >= 60` is True. 3) The `if` block runs. 4) `\"pass\"` is printed.",
      "why_correct": "Following the actual value through the final step, the result being asked for is `is executed`.",
      "common_wrong_choice": {
        "choice": "Skip",
        "why_wrong": "The `if` block is skipped when the condition is `False`. Since 80 is greater than or equal to 60, the condition is `True`.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "In a conditional statement, you first determine whether the condition is True or False, and then select the block to be executed."
    },
    "target_statement": "if score >= 60:",
    "focus_span": "if score >= 60:",
    "focus_mode": "compound_header",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "if",
      "true branch",
      "comparison"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_002",
    "level": 2,
    "title": "If the `if` condition is false, skip the entire block.",
    "concepts": [
      "if_statement",
      "comparison",
      "call"
    ],
    "reading_goal": "Read the core behavior of an if statement in the order of actual execution, tracking values and flow.",
    "code": "score = 40\nif score >= 60:\n    print(\"pass\")",
    "question_type": "meaning_choice",
    "question": "When `score` is 40, is the indented `print()` executed?",
    "choices": [
      "It is executed",
      "It does not execute",
      "It runs twice",
      "Error"
    ],
    "answer": "It does not execute",
    "explanation": "1) `score` is 40. 2) `40 >= 60` is False. 3) The `if` block is skipped. 4) There is no other output statement, so nothing is printed. Following the actual values through the final step, the result asked for is `does not execute`. Common wrong answer: it executes. Seeing a `print` in the code does not mean it always runs. If the `if` condition is False, the indented line is skipped. Misreading point: If you chose this answer, recheck step by step which value changed when, which path the condition selected, or what the actual result of the function call was. For a conditional statement, determine the condition’s True/False value first, then select the block to execute.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "if_statement",
    "reading_goal_internal": "if_statement의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`if` executes the indented block below only when the condition is `True`.",
      "how_to_read": "First, the condition is evaluated. If it is true, the indented lines are executed; if false, that block is skipped.",
      "key_point": "Even if the code inside the `if` statement is written on the screen, it will not execute if the condition is false.",
      "common_mistake": "It’s easy to assume that code is read from top to bottom and that indented lines are always executed. The `if` statement first determines whether that block should be executed."
    },
    "teaching_example": {
      "code": "temp = 10\nif temp > 20:\n    print(\"hot\")",
      "walkthrough": "Since `10 > 20` is False, the `print` statement following the `if` statement is not executed, and the program proceeds to the next line of code."
    },
    "answer_explanation": {
      "step_by_step": "1) The score is 40. 2) 40 >= 60 is False. 3) The if block is skipped. 4) Since there are no other output statements, nothing is printed.",
      "why_correct": "Following the actual values through the final step, the result being asked for is `is not executed`.",
      "common_wrong_choice": {
        "choice": "It is executed",
        "why_wrong": "Just because you see `print` in the code doesn’t mean it will always be executed. If the `if` condition is `False`, the indented lines are skipped.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "In a conditional statement, you first determine whether the condition is True or False, and then select the block to be executed."
    },
    "target_statement": "if score >= 60:",
    "focus_span": "if score >= 60:",
    "focus_mode": "compound_header",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "if",
      "false branch",
      "no output"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_003",
    "level": 2,
    "title": "Selecting the `else` block if `if` is false",
    "concepts": [
      "if_statement",
      "comparison",
      "else_clause",
      "call"
    ],
    "reading_goal": "Read the core behavior of an if statement in the order of actual execution, tracking values and flow.",
    "code": "score = 40\nif score >= 60:\n    print(\"pass\")\nelse:\n    print(\"retry\")",
    "question_type": "meaning_choice",
    "question": "Which block is selected when `score` is 40?",
    "choices": [
      "if block",
      "else block",
      "Both",
      "Not a block"
    ],
    "answer": "else block",
    "explanation": "1) 40 >= 60 is False. 2) The if block is skipped. 3) The else block is executed. 4) \"retry\" is printed. If you follow the actual values through to the final step, the `else block` yields the result asked for in the question. Commonly confusing wrong answer: the if block. The if block is executed only when the condition is True. The current comparison result is False. Point of confusion: If you selected this incorrect answer, go back step by step to verify which values changed at what point, which path the condition took, or what the actual result of the function call was. A conditional statement first determines whether the condition is True or False, and then selects the block to execute.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "if_statement",
    "reading_goal_internal": "if_statement의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`if/else` is a syntax that executes only one of two branches depending on the result of a condition.",
      "how_to_read": "First, the condition following `if` is evaluated. If `True`, the `if` block is executed, and `else` is skipped. If `False`, `if` is skipped, and the `else` block is executed.",
      "key_point": "Within the same `if/else` block, do not execute both blocks.",
      "common_mistake": "Since both blocks are visible in the code, it’s easy to assume that both will be executed. In reality, only the block on the side where the condition is true will be executed."
    },
    "teaching_example": {
      "code": "age = 9\nif age >= 13:\n    print(\"teen\")\nelse:\n    print(\"child\")",
      "walkthrough": "Since `9 >= 13` is False, the `if` statement is skipped, and `\"child\"` is printed in the `else` block."
    },
    "answer_explanation": {
      "step_by_step": "1) 40 >= 60 is False. 2) The if block is skipped. 3) The else block is executed. 4) \"retry\" is printed.",
      "why_correct": "Following the actual values through the final step, the result being asked for is the `else block`.",
      "common_wrong_choice": {
        "choice": "if block",
        "why_wrong": "The `if` block is executed only when the condition is True. The current comparison result is False.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "In a conditional statement, you first determine whether the condition is True or False, and then select the block to be executed."
    },
    "target_statement": "if score >= 60:",
    "focus_span": "if score >= 60:",
    "focus_mode": "compound_header",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "if else",
      "branch",
      "selection"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_004",
    "level": 2,
    "title": "== If the comparison is true, it returns True",
    "concepts": [
      "comparison",
      "if_statement",
      "name_reference"
    ],
    "reading_goal": "Read the core behavior of the `comparison` statement in the order it is actually executed, tracking values and flow.",
    "code": "mode = \"easy\"\nif mode == \"easy\":\n    print(\"start\")",
    "question_type": "meaning_choice",
    "question": "`mode == \"easy\"` What are the results of the comparison?",
    "choices": [
      "True",
      "False",
      "easy",
      "start"
    ],
    "answer": "True",
    "explanation": "1) The current value of `mode` is \"easy.\" 2) The right-hand side string is also \"easy.\" 3) Since the two values are equal, the result of `==` is `True`. 4) The `if` block is executed. If you follow the actual values through to the final step, `True` is the result the question is asking for. Commonly confusing wrong answer: “easy easy” is the string value used in the comparison. The `==` expression itself evaluates to either True or False. Where it went wrong: If you selected this incorrect answer, go back step by step to verify which values changed at what point, which path the condition took, or what the actual result of the function call was. A comparison expression produces a new result—True or False—rather than the original values.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "comparison",
    "reading_goal_internal": "comparison의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "Comparison operators such as `>=`, `==`, and `!=` compare two values and return either `True` or `False`.",
      "how_to_read": "First, check the values on the left and right sides, then determine whether they satisfy the relationship specified by the operator.",
      "key_point": "The result of a comparison is not the original value, but a **true/false value**.",
      "common_mistake": "It’s easy to assume that the numbers or strings used in a comparison are the results themselves. Once the comparison is complete, new values—such as `True` or `False`—are created."
    },
    "teaching_example": {
      "code": "state = \"ready\"\nprint(state == \"ready\")",
      "walkthrough": "Since the value of the variable on the left is exactly the same as the string on the right, the comparison returns True."
    },
    "answer_explanation": {
      "step_by_step": "1) The current value of `mode` is \"easy.\" 2) The right-hand side string is also \"easy.\" 3) Since the two values are equal (==), the result is True. 4) The `if` block is executed.",
      "why_correct": "If you follow the actual values through to the final step, `True` is the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "easy",
        "why_wrong": "\"easy\" is a string value used in the comparison. The `==` expression itself evaluates to either True or False.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "A comparison expression does not return the original value but produces a new result—either True or False."
    },
    "target_statement": "if mode == \"easy\":",
    "focus_span": "mode == \"easy\"",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "equality",
      "comparison",
      "if"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_005",
    "level": 2,
    "title": "The!= comparison returns True when two values are different",
    "concepts": [
      "comparison",
      "if_statement",
      "name_reference"
    ],
    "reading_goal": "Read the core behavior of the `comparison` statement in the order it is actually executed, tracking values and flow.",
    "code": "name = \"bot\"\nif name != \"human\":\n    print(\"robot\")",
    "question_type": "meaning_choice",
    "question": "`name` When `\"bot\"`, what is the result of `name!= \"human\"`?",
    "choices": [
      "True",
      "False",
      "bot",
      "human"
    ],
    "answer": "True",
    "explanation": "1) The value of `name` is \"bot\". 2) Compare whether \"bot\" and \"human\" are different. 3) Since they are different, the result of `!=` is `True`. 4) The `if` block is executed. If you follow the actual values through to the final step, `True` is the result asked for in the problem. Commonly Confused Wrong Answer: `False`. `!=` asks, “Are they not equal?” Since \"bot\" and \"human\" are different, the result is True. Where it went wrong: If you selected this incorrect answer, go back step by step to verify which values changed at which points, which path the condition took, or what the actual result of the function call was. A comparison expression produces a new result—True or False—rather than the original values.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "comparison",
    "reading_goal_internal": "comparison의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "Comparison operators such as `>=`, `==`, and `!=` compare two values and return either `True` or `False`.",
      "how_to_read": "First, check the values on the left and right sides, then determine whether they satisfy the relationship specified by the operator.",
      "key_point": "The result of a comparison is not the original value, but a **true/false value**.",
      "common_mistake": "It’s easy to assume that the numbers or strings used in a comparison are the results themselves. Once the comparison is complete, new values—such as `True` or `False`—are created."
    },
    "teaching_example": {
      "code": "role = \"guest\"\nprint(role != \"admin\")",
      "walkthrough": "Since `\"guest\"` and `\"admin\"` are different, the result of the `!=` comparison is True."
    },
    "answer_explanation": {
      "step_by_step": "1) The value of `name` is \"bot.\" 2) It compares whether \"bot\" and \"human\" are different. 3) Since they are different, the result of `!=` is `True`. 4) The `if` block is executed.",
      "why_correct": "If you follow the actual values through to the final step, `True` is the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "False",
        "why_wrong": "`!=` It asks, “Isn’t it the same?” Since the bot and the human are different, the result is True.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "A comparison expression does not return the original value but produces a new result—either True or False."
    },
    "target_statement": "if name != \"human\":",
    "focus_span": "name != \"human\"",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "not equal",
      "comparison",
      "if"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_006",
    "level": 2,
    "title": "An empty string is treated as False in conditions",
    "concepts": [
      "if_statement",
      "else_clause"
    ],
    "reading_goal": "Read the core behavior of an if statement in the order of actual execution, tracking values and flow.",
    "code": "text = \"\"\nif text:\n    print(\"present\")\nelse:\n    print(\"empty\")",
    "question_type": "meaning_choice",
    "question": "When the empty string `\"\"` is evaluated in `if text:`, which block executes?",
    "choices": [
      "if block",
      "else block",
      "Both",
      "Error"
    ],
    "answer": "else block",
    "explanation": "1) text contains the empty string \"\". 2) An empty string is falsy. 3) The if block is skipped. 4) The else block prints `empty`. Following the execution through the final step, the result being asked for is the `else block`. Common wrong answer: the if block. A string variable is not automatically truthy; when its content is empty, the condition is false. Misread point: recheck the actual string value before choosing the branch.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "if_statement",
    "reading_goal_internal": "if_statement의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "If you use a string directly in a conditional expression, an empty string `\"\"` is evaluated as `False`, while a string containing at least one character is evaluated as `True`.",
      "how_to_read": "`if text:` It checks whether the text contains any characters. If it is empty, the result is false; if it contains content—such as `\"hi\"`—the result is true.",
      "key_point": "Whether the string’s **contents are empty** affects the condition result.",
      "common_mistake": "It’s easy to assume that just because it’s a string variable, it’s always True. If the value is an empty string, the condition is false."
    },
    "teaching_example": {
      "code": "message = \"\"\nif message:\n    print(\"text\")\nelse:\n    print(\"empty\")",
      "walkthrough": "An empty string with zero characters is evaluated as False in the condition, causing the else block to execute."
    },
    "answer_explanation": {
      "step_by_step": "1) The text contains an empty string \"\". 2) An empty string evaluates to False. 3) The if block is skipped. 4) The else block prints \"Empty.\"",
      "why_correct": "Following the actual values through the final step, the result being asked for is the `else block`.",
      "common_wrong_choice": {
        "choice": "if block",
        "why_wrong": "It is not True simply because it is a string variable. If the content is empty, it is evaluated as False in a conditional statement.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "In a conditional statement, you first determine whether the condition is True or False, and then select the block to be executed."
    },
    "target_statement": "if text:",
    "focus_span": "if text:",
    "focus_mode": "compound_header",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "truthiness",
      "empty string",
      "if"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_007",
    "level": 2,
    "title": "Strings containing characters are evaluated as True in conditions",
    "concepts": [
      "if_statement",
      "call"
    ],
    "reading_goal": "Read the core behavior of an if statement in the order of actual execution, tracking values and flow.",
    "code": "text = \"hi\"\nif text:\n    print(\"present\")",
    "question_type": "meaning_choice",
    "question": "If the string `\"hi\"` is evaluated as `if text:`, what is the result of the condition?",
    "choices": [
      "True",
      "False",
      "hi",
      "None"
    ],
    "answer": "True",
    "explanation": "1) The text contains \"hi.\" 2) Since it is not an empty string, it evaluates to True. 3) The if block is executed. 4) \"Present\" is printed. If you follow the actual values through to the final step, `True` is the result the question is asking for. Commonly Confused Wrong Answer: False. What evaluates to False is an empty string with zero characters. `\"hi\"` actually contains characters. Where it went wrong: If you selected this incorrect answer, go back step by step to verify which values changed at each stage, which path the condition took, or what the actual result of the function call was. A conditional statement first determines whether the condition is True or False, and then selects the block to execute.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "if_statement",
    "reading_goal_internal": "if_statement의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "If you use a string directly in a conditional expression, an empty string `\"\"` is evaluated as `False`, while a string containing at least one character is evaluated as `True`.",
      "how_to_read": "`if text:` It checks whether the text contains any characters. If it is empty, the result is false; if it contains content—such as `\"hi\"`—the result is true.",
      "key_point": "Whether the string’s **contents are empty** affects the condition result.",
      "common_mistake": "It’s easy to assume that just because it’s a string variable, it’s always True. If the value is an empty string, the condition is false."
    },
    "teaching_example": {
      "code": "message = \"ok\"\nif message:\n    print(\"has text\")",
      "walkthrough": "Since there are characters within the string, the condition evaluates to True, causing the if block to execute."
    },
    "answer_explanation": {
      "step_by_step": "1) The text contains \"hi.\" 2) Since it is not an empty string, it evaluates to True. 3) The if block is executed. 4) \"It is present\" is printed.",
      "why_correct": "If you follow the actual values through to the final step, `True` is the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "False",
        "why_wrong": "A value that is evaluated as `False` is an empty string containing zero characters. `\"hi\"` contains actual characters.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "In a conditional statement, you first determine whether the condition is True or False, and then select the block to be executed."
    },
    "target_statement": "if text:",
    "focus_span": "if text:",
    "focus_mode": "compound_header",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "truthiness",
      "nonempty string",
      "if"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_008",
    "level": 2,
    "title": "\"and\" returns True only when both conditions are true",
    "concepts": [
      "boolean_operation",
      "comparison",
      "if_statement"
    ],
    "reading_goal": "Read the core behavior of Boolean operation in the order it is actually executed, and track the values and flow.",
    "code": "age = 12\nscore = 90\nif age >= 10 and score >= 80:\n    print(\"ok\")",
    "question_type": "meaning_choice",
    "question": "What is the overall result of `and` when both comparisons are true?",
    "choices": [
      "True",
      "False",
      "12",
      "90"
    ],
    "answer": "True",
    "explanation": "1) age>=10 is equivalent to 12>=10, so it is True. 2) score>=80 is equivalent to 90>=80, so it is True. 3) \"True and True\" evaluates to True. 4) The `if` block is executed. If you follow the actual values through to the final step, `True` is the result asked for in the question. Common mistake: \"False and\" evaluates to False if either condition is false. Here, both comparisons are true. Common misunderstanding: If you selected this incorrect answer, go back step by step to verify which values changed at which points in the above steps, which path the condition took, or what the actual result of the function call was. Read “and” and “or” according to their combinatorial rules: “all true” and “at least one true,” respectively.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "boolean_operation",
    "reading_goal_internal": "boolean_operation의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`and` is a Boolean operator that evaluates to true **only when both** conditions are true.",
      "how_to_read": "Calculate the left and right conditions separately to verify that both are `True`.",
      "key_point": "If either condition is false, the result of `and` is false.",
      "common_mistake": "It’s easy to assume that the condition is satisfied if even one condition is met. That’s similar to `or`, whereas `and` requires that all conditions be met."
    },
    "teaching_example": {
      "code": "age = 20\nscore = 70\nprint(age >= 18 and score >= 60)",
      "walkthrough": "Since 20 >= 18 is True and 70 >= 60 is True, the result of `True and True` is True."
    },
    "answer_explanation": {
      "step_by_step": "1) age >= 10 is True because 12 >= 10. 2) score >= 80 is True because 90 >= 80. 3) True and True is True. 4) The if block is executed.",
      "why_correct": "If you follow the actual values through to the final step, `True` is the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "False",
        "why_wrong": "`and` returns `False` if either of the two conditions is false. Here, both comparisons are true.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "\"and/or\" are interpreted according to the conjunction rules \"all true\" and \"at least one true,\" respectively."
    },
    "target_statement": "if age >= 10 and score >= 80:",
    "focus_span": "age >= 10 and score >= 80",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "and",
      "boolean",
      "compound condition"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_009",
    "level": 2,
    "title": "\"or\" returns True if any one of the conditions is true",
    "concepts": [
      "boolean_operation",
      "comparison",
      "if_statement"
    ],
    "reading_goal": "Read the core behavior of Boolean operation in the order it is actually executed, and track the values and flow.",
    "code": "role = \"admin\"\nif role == \"admin\" or role == \"owner\":\n    print(\"open\")",
    "question_type": "meaning_choice",
    "question": "`role` When `\"admin\"`, what is the result of the entire expression `or`?",
    "choices": [
      "True",
      "False",
      "owner",
      "open"
    ],
    "answer": "True",
    "explanation": "1) `role==\"admin\"` is True. 2) `role==\"owner\"` is False. 3) \"or\" evaluates to True if any single condition is True. 4) The `if` block is executed. If you follow the actual values through to the final step, `True` is the result the question is asking for. Commonly confusing wrong answer: False. With \"or,\" both conditions do not need to be true. Since the first comparison is already True, the overall result is also True. Where it went wrong: If you selected this incorrect answer, go back step by step to verify which values changed at which points in the steps above, which path the condition took, or what the actual result of the function call was. Read the \"and\" and \"or\" operators as \"all must be true\" and \"at least one must be true,\" respectively.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "boolean_operation",
    "reading_goal_internal": "boolean_operation의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`or` is a Boolean operator that evaluates to true if **any one** of several conditions is true.",
      "how_to_read": "Check the results of each condition and see if at least one is `True`.",
      "key_point": "In an `or` statement, not all conditions need to be true.",
      "common_mistake": "It’s easy to assume that both conditions must be true. However, while `and` requires both to be true, `or` is true as long as at least one of them is true."
    },
    "teaching_example": {
      "code": "mode = \"manual\"\nprint(mode == \"auto\" or mode == \"manual\")",
      "walkthrough": "Although the first comparison is False, the second comparison is True, so the result of `False or True` is True."
    },
    "answer_explanation": {
      "step_by_step": "1) role==\"admin\" is True. 2) role==\"owner\" is False. 3) With \"or,\" if any one condition is True, the entire expression is True. 4) The if block is executed.",
      "why_correct": "If you follow the actual values through to the final step, `True` is the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "False",
        "why_wrong": "The `or` operator does not require both conditions to be true. Since the first comparison is already True, the overall result is also True.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "\"and/or\" are interpreted according to the conjunction rules \"all true\" and \"at least one true,\" respectively."
    },
    "target_statement": "if role == \"admin\" or role == \"owner\":",
    "focus_span": "role == \"admin\" or role == \"owner\"",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "or",
      "boolean",
      "alternative condition"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_010",
    "level": 2,
    "title": "Turning False into True using `not`",
    "concepts": [
      "unary_operation",
      "if_statement",
      "name_reference"
    ],
    "reading_goal": "Read the core behavior of unary operation in the order it is actually executed, tracking values and flow.",
    "code": "is_done = False\nif not is_done:\n    print(\"todo\")",
    "question_type": "meaning_choice",
    "question": "What is the result of `is_done` when `False` is `not is_done`?",
    "choices": [
      "True",
      "False",
      "todo",
      "None"
    ],
    "answer": "True",
    "explanation": "1) `is_done` is False. 2) `not` inverts `False`. 3) The result is True. 4) The `if` block is executed. If you trace the actual values through to the final step, `True` is the result the question is asking for. Commonly confused incorrect answer: `False`. `False` is the value before applying `not`. Since `not` inverts the Boolean value, the final result is True. Where it went wrong: If you selected this incorrect answer, go back step by step to verify which values changed at what point, which path the condition took, or what the actual result of the function call was. The `not` operator first evaluates the Boolean value that follows it and then inverts its truth value.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "unary_operation",
    "reading_goal_internal": "unary_operation의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`not` is an unary operator that inverts the true/false value that follows it.",
      "how_to_read": "If `not is_done`, first check the current Boolean value of `is_done`, then invert `True` to `False` and `False` to `True`.",
      "key_point": "`not False` is `True`, and `not True` is `False`.",
      "common_mistake": "It is easy to simply read `not` as the word “no” and overlook the fact that the actual Boolean value is inverted."
    },
    "teaching_example": {
      "code": "ready = True\nprint(not ready)",
      "walkthrough": "Since `ready` is True, `not ready` inverts the value and becomes False."
    },
    "answer_explanation": {
      "step_by_step": "1) `is_done` is False. 2) `not` inverts False. 3) The result is True. 4) The `if` block is executed.",
      "why_correct": "If you follow the actual values through to the final step, `True` is the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "False",
        "why_wrong": "`False` is the value before applying `not`. Since `not` inverts the Boolean value, the final result is `True`.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "The `not` operator first evaluates the Boolean value that follows it and then inverts its truth value (true to false or false to true)."
    },
    "target_statement": "if not is_done:",
    "focus_span": "not is_done",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "not",
      "unary boolean",
      "if"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_011",
    "level": 2,
    "title": "`elif` checks the next condition when the previous condition is false",
    "concepts": [
      "elif_clause",
      "if_statement",
      "comparison",
      "else_clause"
    ],
    "reading_goal": "Read the core behavior of the `elif_clause` in the actual order of execution and track values and flow.",
    "code": "score = 75\nif score >= 90:\n    print(\"A\")\nelif score >= 70:\n    print(\"B\")\nelse:\n    print(\"C\")",
    "question_type": "meaning_choice",
    "question": "`score` What is the condition that first evaluates to true when 75?",
    "choices": [
      "score >= 90",
      "score >= 70",
      "else",
      "Both"
    ],
    "answer": "score >= 70",
    "explanation": "1) `75 >= 90` is False. 2) Since the first `if` is false, check `elif`. 3) `75 >= 70` is True. 4) Print B and skip the later `else`. Following the actual values through the final step, the result asked for is `score >= 70`. Common wrong answer: `score >= 90`. 75 is not at least 90, so the first condition is false; the next `elif` is the first true condition. Misreading point: If you chose this answer, recheck step by step which value changed when, which path the condition selected, or what the actual function-call result was. An `elif` chain checks from the top and executes only the first branch that becomes true.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "elif_clause",
    "reading_goal_internal": "elif_clause의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`elif` is a syntax that checks the next condition when the preceding `if` condition is false.",
      "how_to_read": "Check conditions from top to bottom starting with `if`. Once the first True branch runs, the remaining `elif` branches and `else` are skipped.",
      "key_point": "Even if there are multiple conditions, only **the branch that first evaluates to true from the top** is selected.",
      "common_mistake": "It is easy to think all conditions are checked to the end and multiple blocks execute in sequence. Once one branch is selected, later conditions in the same chain are not evaluated."
    },
    "teaching_example": {
      "code": "score = 65\nif score >= 80:\n    print(\"high\")\nelif score >= 60:\n    print(\"mid\")\nelse:\n    print(\"low\")",
      "walkthrough": "Since the first condition is false and the next `65>=60` is true, `\"mid\"` in the `elif` block is selected."
    },
    "answer_explanation": {
      "step_by_step": "1) 75 >= 90 is False. 2) Since the preceding if statement is false, the elif statement is evaluated. 3) 75 >= 70 is True. 4) “B” is printed, and the subsequent else block is skipped.",
      "why_correct": "If you follow the actual values through to the final step, `score >= 70` is the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "score >= 90",
        "why_wrong": "Since 75 is not greater than or equal to 90, the first condition is false. The next `elif` condition becomes true for the first time.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "The `elif` chain is evaluated from the top down, and only the first branch that evaluates to true is executed."
    },
    "target_statement": "elif score >= 70:",
    "focus_span": "elif score >= 70:",
    "focus_mode": "clause_header",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "elif",
      "branch order",
      "threshold"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_012",
    "level": 2,
    "title": "Using the latest variable values before checking conditions",
    "concepts": [
      "if_statement",
      "assignment",
      "arithmetic",
      "comparison"
    ],
    "reading_goal": "Read the core behavior of an if statement in the order of actual execution, tracking values and flow.",
    "code": "score = 50\nscore = score + 20\nif score >= 60:\n    print(\"pass\")",
    "question_type": "meaning_choice",
    "question": "After reassignment, is the `if score >= 60:` block executed?",
    "choices": [
      "It is executed",
      "It does not execute",
      "It runs only when the score is 50.",
      "Error"
    ],
    "answer": "It is executed",
    "explanation": "1) `score` starts at 50. 2) `score = score + 20` changes it to 70. 3) The `if` statement checks whether `70 >= 60` using the latest value, 70. 4) Since the condition is True, it prints `pass`. If you follow the actual values through to the final step, “it executes” is the result the question is asking for. Common mistake: “It does not execute.” Before the `if` statement is evaluated, `score` changes from 50 to 70. Since the latest value, 70, is greater than or equal to 60, the condition is True. Where it went wrong: If you selected this incorrect answer, go back step by step to verify which values changed and when, which path the conditional statement took, or what the actual result of the function call was. A conditional statement first determines whether the condition is True or False, and then selects the block of code to execute.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "if_statement",
    "reading_goal_internal": "if_statement의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`if` executes the indented block below only when the condition is `True`.",
      "how_to_read": "First, the condition is evaluated. If it is true, the indented lines are executed; if false, that block is skipped.",
      "key_point": "Even if the code inside the `if` statement is written on the screen, it will not execute if the condition is false.",
      "common_mistake": "It’s easy to assume that code is read from top to bottom and that indented lines are always executed. The `if` statement first determines whether that block should be executed."
    },
    "teaching_example": {
      "code": "count = 5\ncount = count + 3\nif count >= 8:\n    print(\"ok\")",
      "walkthrough": "Since the loop is re-iterated and `count` becomes 8 first before the condition is checked, `8>=8` is True."
    },
    "answer_explanation": {
      "step_by_step": "1) `score` starts at 50. 2) `score = score + 20` changes to 70. 3) The `if` statement checks whether `70 >= 60` using the latest value of 70. 4) Since the condition is True, it prints `pass`.",
      "why_correct": "Following the actual value through the final step, the result being asked for is `is executed`.",
      "common_wrong_choice": {
        "choice": "It does not execute",
        "why_wrong": "Before the `if` statement is evaluated, `score` changes from 50 to 70. Since the latest value, 70, is 60 or higher, the condition is True.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "In a conditional statement, you first determine whether the condition is True or False, and then select the block to be executed."
    },
    "target_statement": "if score >= 60:",
    "focus_span": "if score >= 60:",
    "focus_mode": "compound_header",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "if",
      "reassignment",
      "current value"
    ],
    "pedagogical_intent": "trace_state"
  },
  {
    "id": "PYF94_A2_L02_IF_013",
    "level": 2,
    "title": "String comparison is case-sensitive",
    "concepts": [
      "comparison",
      "if_statement",
      "else_clause"
    ],
    "reading_goal": "Read the core behavior of the `comparison` statement in the order it is actually executed, tracking values and flow.",
    "code": "answer = \"Yes\"\nif answer == \"yes\":\n    print(\"ok\")\nelse:\n    print(\"check\")",
    "question_type": "meaning_choice",
    "question": "What is the result of comparing `\"Yes\" == \"yes\"`?",
    "choices": [
      "True",
      "False",
      "Yes",
      "yes"
    ],
    "answer": "False",
    "explanation": "1) The value of `answer` is \"Yes.\" 2) The comparison target is \"yes.\" 3) The first letters, Y and y, are different. 4) The result of `==` is `False`, so the `else` block is executed. If you follow the actual values through to the final step, `False` is the result the question is asking for. Common mistake: `True`. Even though they look like the same word to a human reader, standard string comparison treats the uppercase `Y` and lowercase `y` as different characters. Point of confusion: If you selected this incorrect answer, go back step by step to verify which values changed at which points in the steps above, which path the condition took, or what the actual result of the function call was. A comparison expression produces a new result—True or False—rather than the original value.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "comparison",
    "reading_goal_internal": "comparison의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "A string `==` comparison checks equality including the case of each letter.",
      "how_to_read": "When evaluating `\"Yes\" == \"yes\"`, the first characters `Y` and `y` differ, so the comparison is False.",
      "key_point": "In basic string comparison, uppercase and lowercase letters are treated as different characters.",
      "common_mistake": "To the human eye, they may look like the same word, so it’s easy to assume they are identical. In code, however, even the uppercase and lowercase letters of each character are taken into account when comparing them."
    },
    "teaching_example": {
      "code": "word = \"Python\"\nprint(word == \"python\")",
      "walkthrough": "Since the first letter \"P\" is uppercase and \"p\" is lowercase, the two strings are not equal, so the result is False."
    },
    "answer_explanation": {
      "step_by_step": "1) The value of `answer` is \"Yes.\" 2) The comparison target is \"yes.\" 3) The first letters, Y and y, are different. 4) The result of `==` is `False`, so the `else` block is executed.",
      "why_correct": "If you follow the actual values through to the final step, `False` is the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "True",
        "why_wrong": "Even though they appear to be the same word to a human reader, standard string comparison treats the uppercase letter Y and the lowercase letter y as different characters.",
        "misread_step": "If you selected this incorrect answer, go back step by step to verify which values changed at which stage, which path the condition took, or what the actual result of the function call was."
      },
      "takeaway": "A comparison expression does not return the original value but produces a new result—either True or False."
    },
    "target_statement": "if answer == \"yes\":",
    "focus_span": "answer == \"yes\"",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "case sensitivity",
      "string comparison",
      "if"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_014",
    "level": 2,
    "title": "Comparing string numbers and actual numbers as distinct values",
    "concepts": [
      "comparison",
      "if_statement",
      "literal"
    ],
    "reading_goal": "Read the core behavior of the `comparison` statement in the order it is actually executed, tracking values and flow.",
    "code": "value = \"10\"\nif value == 10:\n    print(\"same\")\nelse:\n    print(\"different\")",
    "question_type": "meaning_choice",
    "question": "When `value` is the string `\"10\"`, what is the result of `value == 10`?",
    "choices": [
      "True",
      "False",
      "10",
      "Error"
    ],
    "answer": "False",
    "explanation": "1) The variable `value` contains the string \"10\". 2) The value being compared, 10, is an integer. 3) Because the data types are different, the result of `==` is `False`. 4) The `if` statement is skipped, and `different` is printed in the `else` block. If you apply the steps above in order to the problem code, `False` will be the result asked for in the problem. Commonly Confusing Wrong Answer: `True`—Even though both appear to be 10 on the surface, `\"10\"` is a string and `10` is an integer, so they are not compared as equal values. Where it went wrong: If you selected this incorrect answer, compare it against the sequence above to see which step—the current value, index position, condition path, iteration state, or method return value—has changed. A comparison expression produces a new result (True or False), not the original value.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "comparison",
    "reading_goal_internal": "comparison의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`==` compares whether two values are equal. Even if they look like numbers, the string `\"10\"` and the integer `10` are different types of values and are therefore not considered equal.",
      "how_to_read": "First, check both the **content and data type** of the left and right-hand values. If there are quotation marks, it’s a string; if not, it may be a number.",
      "key_point": "Even if they appear exactly the same on the screen—such as `10`—a string and an integer are not the same value.",
      "common_mistake": "It’s easy to assume that two values are the same just by looking at their appearance. When comparing values, you must also consider the syntax that indicates the type of value, such as the presence or absence of quotation marks."
    },
    "teaching_example": {
      "code": "left = \"5\"\nright = 5\nprint(left == right)",
      "walkthrough": "Although both appear to be 5, the left is a string and the right is an integer, so `left == right` is False."
    },
    "answer_explanation": {
      "step_by_step": "1) The variable `value` contains the string \"10\". 2) The value being compared, 10, is an integer. 3) Because the data types are different, the result of `==` is `False`. 4) The `if` statement is skipped, and `different` is printed in the `else` block.",
      "why_correct": "If you apply the steps above in order to the problem code, `False` will be the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "True",
        "why_wrong": "Even though both appear to be 10 on the surface, `\"10\"` is a string and `10` is an integer, so they are not compared as having the same value.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "A comparison expression does not return the original value but produces a new result—either True or False."
    },
    "target_statement": "if value == 10:",
    "focus_span": "value == 10",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "comparison",
      "string vs int",
      "type"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_015",
    "level": 2,
    "title": "Convert a numeric string to an integer with `int()` and then compare it.",
    "concepts": [
      "call",
      "assignment",
      "comparison",
      "if_statement"
    ],
    "reading_goal": "Read the core behavior of a `call` in the order it actually executes, tracking values and flow.",
    "code": "value = int(\"10\")\nif value == 10:\n    print(\"same\")",
    "question_type": "meaning_choice",
    "question": "What is the first value returned by `int(\"10\")`?",
    "choices": [
      "The string \"10\"",
      "Integer 10",
      "True",
      "same"
    ],
    "answer": "Integer 10",
    "explanation": "1) Pass the string `\"10\"` to `int()`. 2) `int()` returns the integer 10. 3) That integer 10 is stored in `value`. 4) `value == 10` is True, so `same` is printed. Applying these steps in order, the result asked for is `integer 10`. Common wrong answer: the string `\"10\"`. That is the input before conversion; the result of `int()` is the actual integer 10. Misreading point: If you chose this answer, compare the sequence above to find which step differs among the current value, index position, condition path, loop state, or method return value. For function and method calls, distinguish the input value, return value, and any change to the original state.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "call의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "A function call can receive a value calculated inside the function through `return` and use that value in another calculation.",
      "how_to_read": "`double(4) + 1` First, calculate the return value of the call to `double(4)`. If the return value is 8, replace the outer expression with `8 + 1` and continue the calculation.",
      "key_point": "If a function call appears within an expression, **the return value is evaluated first, followed by the outer calculation**.",
      "common_mistake": "It’s easy to overlook the call section and simply add 1 to argument 4. First, you must check what the function body actually returns."
    },
    "teaching_example": {
      "code": "text = \"7\"\nnumber = int(text)\nprint(number == 7)",
      "walkthrough": "First, since `int(text)` converts the string `\"7\"` to the integer 7, the subsequent comparison `number == 7` evaluates to True."
    },
    "answer_explanation": {
      "step_by_step": "1) Pass the string `\"10\"` to `int()`. 2) `int()` returns the integer 10. 3) That integer 10 is stored in `value`. 4) `value == 10` is True, so `same` is printed.",
      "why_correct": "If you apply the steps above in order to the code in the problem, `integer 10` will be the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "The string \"10\"",
        "why_wrong": "That value is the input before conversion. The result of the `int()` call is the actual integer 10.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "When reading function and method calls, identify the input values, return values, and changes to the original state."
    },
    "target_statement": "value = int(\"10\")",
    "focus_span": "int(\"10\")",
    "focus_mode": "subexpression",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "int",
      "conversion",
      "comparison"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_IF_016",
    "level": 2,
    "title": "Use indentation to distinguish between inside and outside of an `if` block",
    "concepts": [
      "if_statement",
      "call"
    ],
    "reading_goal": "Read the core behavior of an if statement in the order of actual execution, tracking values and flow.",
    "code": "ready = True\nif ready:\n    print(\"A\")\nprint(\"B\")",
    "question_type": "output_prediction",
    "question": "When `ready` is True, which of the following is the correct output order?",
    "choices": [
      "B-level",
      "A only",
      "A followed by B",
      "No output"
    ],
    "answer": "A followed by B",
    "explanation": "1) ready is True. 2) The indented `print(\"A\")` is inside the if block, so it executes. 3) `print(\"B\")` is outside the indentation, so it runs after the if block finishes. 4) The output is A followed by B. Following the problem code in order, the result is `A followed by B`. Common wrong answer: only A. Because `print(\"B\")` is outside the if block, it still runs after the if statement.",
    "side_card_ids": [
      "PYF94_A2_IF_BOOL_FLOW",
      "PYF94_A2_IF_ELSE_FLOW",
      "PYF94_A2_COMPARE_OPERATOR"
    ],
    "project_context": "Conditional statements are the basic structures used to read user selections, filtering, and error-handling flows.",
    "authoring_version": "V2.3",
    "primary_concept": "if_statement",
    "reading_goal_internal": "if_statement의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "In Python, indentation determines which lines belong inside an `if` block.",
      "how_to_read": "Only the lines indented further to the right of the `if` statement are affected by the condition. Lines that are indented less to the left are outside the `if` block and can therefore be executed in the following order regardless of the condition.",
      "key_point": "The scope of conditional statements is defined by **indentation depth**.",
      "common_mistake": "It’s easy to assume that all the lines appearing below the `if` statement are part of the same condition. You must check each line individually to ensure they have the same indentation."
    },
    "teaching_example": {
      "code": "ready = False\nif ready:\n    print(\"inside\")\nprint(\"outside\")",
      "walkthrough": "Although `inside` is indented because `ready` is False, it is skipped; however, `outside`, which is not indented, is executed because it is not part of the if block."
    },
    "answer_explanation": {
      "step_by_step": "1) `ready` is `True`. 2) The indented `print(\"A\")` is inside the `if` block, so it executes. 3) `print(\"B\")` is outside the indentation, so it executes after the `if` block ends. 4) The output is \"A\" followed by \"B\".",
      "why_correct": "If you apply the steps above in order to the problem code, `A followed by B` is the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "A only",
        "why_wrong": "`print(\"B\")` is outside the `if` block, so it executes after the `if` finishes as well.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "In a conditional statement, you first determine whether the condition is True or False, and then select the block to be executed."
    },
    "target_statement": "if ready:",
    "focus_span": "if ready:",
    "focus_mode": "compound_header",
    "coverage_domain": "python_core",
    "coverage_topics": [
      "indentation",
      "if block",
      "execution order"
    ],
    "pedagogical_intent": "predict_output"
  },
  {
    "id": "PYF94_A2_L02_LIST_001",
    "level": 2,
    "title": "Retrieving the first value from a list using index 0",
    "concepts": [
      "subscription",
      "call"
    ],
    "reading_goal": "Read the core operations of `subscription` in the order they are actually executed, and track values and flow.",
    "code": "items = [\"A\", \"B\", \"C\"]\nprint(items[0])",
    "question_type": "output_prediction",
    "question": "What value is retrieved by `items[0]`?",
    "choices": [
      "B",
      "C",
      "A",
      "0"
    ],
    "answer": "A",
    "explanation": "1) The values of `items` are [\"A\", \"B\", \"C\"]. 2) Index 0 is the first position. 3) The first value is A. If you apply the steps above in order to the code in the problem, `A` is the result the problem is asking for. Commonly confusing wrong answer: 0. 0 is the index number. The result of `items[0]` is the actual value at that position, which is A. Where it went wrong: If you selected this incorrect answer, compare it to the sequence above to see which step—the current value, index position, condition path, iteration state, or method return value—has changed. The bracket notation proceeds from left to right, one step at a time, to locate the actual item.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "subscription",
    "reading_goal_internal": "subscription의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "Each item in a list is assigned an index, which is a position number. In Python, the first item is numbered 0.",
      "how_to_read": "As in `items[1]`, you look at the number inside the square brackets to retrieve the actual value at that position. `0=first`, `1=second`, `2=third`.",
      "key_point": "The index number and the actual value of the item are different.",
      "common_mistake": "Since people count the first item as 1, it’s easy to think of `items[1]` as the first value. In Python, the first value is `items[0]`."
    },
    "teaching_example": {
      "code": "colors = [\"red\", \"blue\"]\nprint(colors[0])",
      "walkthrough": "Since the index of the first item is 0, `colors[0]` returns `\"red\"`."
    },
    "answer_explanation": {
      "step_by_step": "1) The values of `items` are [\"A\", \"B\", \"C\"]. 2) Index 0 is the first position. 3) The first value is A.",
      "why_correct": "If you apply the steps above in order to the problem code, `A` will be the result asked for in the problem.",
      "common_wrong_choice": {
        "choice": "0",
        "why_wrong": "0 is the position number. The result of `items[0]` is the actual value at that position, `A`.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "The bracket-based approach searches for actual items one step at a time, starting from the left."
    },
    "target_statement": "print(items[0])",
    "focus_span": "items[0]",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "list",
      "index 0",
      "subscription"
    ],
    "pedagogical_intent": "predict_output"
  },
  {
    "id": "PYF94_A2_L02_LIST_002",
    "level": 2,
    "title": "Retrieve the second list value with index 1.",
    "concepts": [
      "subscription",
      "call"
    ],
    "reading_goal": "Read the core operations of `subscription` in the order they are actually executed, and track values and flow.",
    "code": "items = [\"A\", \"B\", \"C\"]\nprint(items[1])",
    "question_type": "output_prediction",
    "question": "What is the value retrieved by `items[1]`?",
    "choices": [
      "A",
      "B",
      "C",
      "1"
    ],
    "answer": "B",
    "explanation": "1) The positions of the items are 0:A, 1:B, and 2:C. 2) Index 1 corresponds to the second position. 3) The value at that position is B. If you apply the steps above in order to the problem code, `B` is the result the problem is asking for. Commonly confused incorrect answer: A. The first item has index 0. Index 1 points to the second item, B. Where it went wrong: If you selected this incorrect answer, compare the above sequence to see which step—current value, index position, condition path, iteration state, or method return value—differed. The bracket approach proceeds from left to right, one step at a time, to locate the actual element.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "subscription",
    "reading_goal_internal": "subscription의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "Each item in a list is assigned an index, which is a position number. In Python, the first item is numbered 0.",
      "how_to_read": "As in `items[1]`, you look at the number inside the square brackets to retrieve the actual value at that position. `0=first`, `1=second`, `2=third`.",
      "key_point": "The index number and the actual value of the item are different.",
      "common_mistake": "Since people count the first item as 1, it’s easy to think of `items[1]` as the first value. In Python, the first value is `items[0]`."
    },
    "teaching_example": {
      "code": "colors = [\"red\", \"blue\", \"green\"]\nprint(colors[1])",
      "walkthrough": "Index 1 is the second position in human counting, so `colors[1]` is `\"blue\"`."
    },
    "answer_explanation": {
      "step_by_step": "1) The positions of the items are 0:A, 1:B, and 2:C. 2) Index 1 corresponds to the second position. 3) The value at that position is B.",
      "why_correct": "If you apply the above steps in order to the problem code, `B` becomes the result asked for in the problem.",
      "common_wrong_choice": {
        "choice": "A",
        "why_wrong": "The first item is at index 0. Index 1 refers to the second item, B.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "The bracket-based approach searches for actual items one step at a time, starting from the left."
    },
    "target_statement": "print(items[1])",
    "focus_span": "items[1]",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "list",
      "index 1",
      "subscription"
    ],
    "pedagogical_intent": "predict_output"
  },
  {
    "id": "PYF94_A2_L02_LIST_003",
    "level": 2,
    "title": "After one `append`, use `len()` to count the current number of items.",
    "concepts": [
      "call",
      "attribute_access"
    ],
    "reading_goal": "Read the core behavior of a `call` in the order it actually executes, tracking values and flow.",
    "code": "items = []\nitems.append(\"A\")\nprint(len(items))",
    "question_type": "meaning_choice",
    "question": "What is the result of `append(\"A\")` after `len(items)`?",
    "choices": [
      "0",
      "1",
      "A",
      "items"
    ],
    "answer": "1",
    "explanation": "1) `items` is an empty list. 2) After `append(\"A\")`, it becomes `[\"A\"]`. 3) There is currently one item. 4) `len(items)` is 1. If you apply the steps above in order to the problem code, `1` is the result the question is asking for. Common mistake: 0. 0 is the length before the `append`. After `A` is added, there is currently one item. Where it went wrong: If you selected this incorrect answer, compare the sequence above to see which stage—current value, index position, condition path, iteration state, or method return value—has changed. When reading function or method calls, distinguish between the input values, return values, and changes to the original state.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "call의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`append()` `append()` modifies the existing list by adding an item to the end, and `len()` counts the number of items at that point.",
      "how_to_read": "First, describe how the list changes as `append` is applied line by line, then count the actual items in the final state to determine the `len` result.",
      "key_point": "`len()` sees **the current list state after all preceding `append` operations have been applied**.",
      "common_mistake": "It’s easy to assume that `len` is 0 just by looking at the empty list at first. Once the `append` in the previous line is executed, the state of the list has already changed."
    },
    "teaching_example": {
      "code": "items = []\nitems.append(\"X\")\nitems.append(\"Y\")\nprint(len(items))",
      "walkthrough": "If you append twice, there will be two items, so the result of `len(items)` is 2."
    },
    "answer_explanation": {
      "step_by_step": "1) `items` is an empty list. 2) After `append(\"A\")`, it becomes `[\"A\"]`. 3) There is currently one item. 4) `len(items)` is 1.",
      "why_correct": "If you apply the steps above in order to the problem code, `1` will be the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "0",
        "why_wrong": "0 is the length before the append operation. After A is added, there is currently one item.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "When reading function and method calls, identify the input values, return values, and changes to the original state."
    },
    "target_statement": "print(len(items))",
    "focus_span": "len(items)",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "append",
      "len",
      "list state"
    ],
    "pedagogical_intent": "trace_state"
  },
  {
    "id": "PYF94_A2_L02_LIST_004",
    "level": 2,
    "title": "Reading index 1 from a list modified using `append`",
    "concepts": [
      "subscription",
      "call",
      "attribute_access"
    ],
    "reading_goal": "Read the core operations of `subscription` in the order they are actually executed, and track values and flow.",
    "code": "items = []\nitems.append(\"A\")\nitems.append(\"B\")\nprint(items[1])",
    "question_type": "output_prediction",
    "question": "What is the output of `items[1]` after appending twice?",
    "choices": [
      "A",
      "B",
      "2",
      "Error"
    ],
    "answer": "B",
    "explanation": "1) After the first `append`, it is [\"A\"]. 2) After the second `append`, it is [\"A\", \"B\"]. 3) Index 1 corresponds to the second position. 4) `items[1]` is B. If you apply the steps above in order to the problem code, `B` is the result asked for in the problem. Common mistake: A. A is the value at index 0. After the second `append`, index 1 contains B. Where it went wrong: If you selected this incorrect answer, compare the sequence above to see which step—current value, index position, condition path, iteration state, or method return value—differs. The bracket approach proceeds from left to right, one step at a time, to locate the actual item.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "subscription",
    "reading_goal_internal": "subscription의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "Each item in a list is assigned an index, which is a position number. In Python, the first item is numbered 0.",
      "how_to_read": "As in `items[1]`, you look at the number inside the square brackets to retrieve the actual value at that position. `0=first`, `1=second`, `2=third`.",
      "key_point": "The index number and the actual value of the item are different.",
      "common_mistake": "Since people count the first item as 1, it’s easy to think of `items[1]` as the first value. In Python, the first value is `items[0]`."
    },
    "teaching_example": {
      "code": "items = []\nitems.append(\"red\")\nitems.append(\"blue\")\nprint(items[1])",
      "walkthrough": "After `append`, the list becomes `[\"red\",\"blue\"]`, and index 1 points to the second value, `\"blue\"`."
    },
    "answer_explanation": {
      "step_by_step": "1) After the first `append`, it is [\"A\"]. 2) After the second `append`, it is [\"A\", \"B\"]. 3) Index 1 corresponds to the second position. 4) `items[1]` is B.",
      "why_correct": "If you apply the above steps in order to the problem code, `B` becomes the result asked for in the problem.",
      "common_wrong_choice": {
        "choice": "A",
        "why_wrong": "A is the value at index 0. After the second `append`, B is at index 1.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "The bracket-based approach searches for actual items one step at a time, starting from the left."
    },
    "target_statement": "print(items[1])",
    "focus_span": "items[1]",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "append",
      "index",
      "list state"
    ],
    "pedagogical_intent": "predict_output"
  },
  {
    "id": "PYF94_A2_L02_LIST_005",
    "level": 2,
    "title": "Counting the number of items in a list using `len()`",
    "concepts": [
      "call",
      "assignment"
    ],
    "reading_goal": "Read the core behavior of a `call` in the order it actually executes, tracking values and flow.",
    "code": "items = [\"red\", \"blue\"]\ncount = len(items)\nprint(count)",
    "question_type": "meaning_choice",
    "question": "`items` When there are two items, \"red\" and \"blue,\" what is the result of `len(items)`?",
    "choices": [
      "1",
      "2",
      "red",
      "blue"
    ],
    "answer": "2",
    "explanation": "1) The `items` list contains two items: `red` and `blue`. 2) `len` counts the number of items in the list, not the number of characters. 3) The number of items is 2. 4) The value 2 is stored in `count`. If you apply the above steps in order to the problem code, `2` will be the result asked for in the problem. Commonly Confusing Wrong Answer: 1. The list contains one item each of `red` and `blue`, so the actual number of items is 2. Point of Misreading: If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, condition path, iteration state, or method return value—has changed. When reading function or method calls, distinguish between the input values, return values, and changes to the original state.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "call의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`append()` `append()` modifies the existing list by adding an item to the end, and `len()` counts the number of items at that point.",
      "how_to_read": "First, describe how the list changes as `append` is applied line by line, then count the actual items in the final state to determine the `len` result.",
      "key_point": "`len()` sees **the current list state after all preceding `append` operations have been applied**.",
      "common_mistake": "It’s easy to assume that `len` is 0 just by looking at the empty list at first. Once the `append` in the previous line is executed, the state of the list has already changed."
    },
    "teaching_example": {
      "code": "items = [\"x\", \"y\", \"z\"]\nprint(len(items))",
      "walkthrough": "Since there are three items—x, y, and z—`len(items)` returns 3."
    },
    "answer_explanation": {
      "step_by_step": "1) The `items` list contains two elements: `red` and `blue`. 2) `len` counts the number of elements in the list, not the number of characters. 3) The number of elements is 2. 4) The value 2 is stored in `count`.",
      "why_correct": "If you apply the steps above in order to the sample code, `2` is the result asked for in the problem.",
      "common_wrong_choice": {
        "choice": "1",
        "why_wrong": "The list contains one item named \"red\" and one named \"blue,\" so there are actually two items.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "When reading function and method calls, identify the input values, return values, and changes to the original state."
    },
    "target_statement": "count = len(items)",
    "focus_span": "len(items)",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "len",
      "list length",
      "count"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_LIST_006",
    "level": 2,
    "title": "Using a negative index of -1 to retrieve the last value in a list",
    "concepts": [
      "subscription",
      "call"
    ],
    "reading_goal": "Read the core operations of `subscription` in the order they are actually executed, and track values and flow.",
    "code": "items = [\"A\", \"B\", \"C\"]\nprint(items[-1])",
    "question_type": "output_prediction",
    "question": "What is the value retrieved using `items[-1]`?",
    "choices": [
      "A",
      "B",
      "C",
      "-1"
    ],
    "answer": "C",
    "explanation": "1) The items are [\"A\", \"B\", \"C\"]. 2) -1 is the second-to-last position. 3) The last item is C. If you apply the steps above in order to the problem code, `C` is the result the problem is asking for. Commonly confusing wrong answer: -1. -1 is an index that specifies a position. The actual result is the value at that position, which is C. Where it went wrong: If you selected this incorrect answer, compare the above sequence to see which step—the current value, index position, condition path, loop state, or method return value—has changed. The bracket approach proceeds from left to right, one step at a time, to find the actual item.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "subscription",
    "reading_goal_internal": "subscription의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "Negative list indices count from the end: `-1` selects the last item, `-2` the second-to-last item, and so on.",
      "how_to_read": "Read `items[-1]` as ‘the last item in `items`.’ If the index is `-2`, move one position farther left to the second-to-last item.",
      "key_point": "`-1` does not refer to the number -1, but rather to the **index pointing to the last position**.",
      "common_mistake": "It’s easy to assume that the -1 inside the square brackets is the result itself. However, -1 is a number that specifies a position, and the result is the actual item at that position."
    },
    "teaching_example": {
      "code": "items = [\"red\", \"blue\", \"green\"]\nprint(items[-1])",
      "walkthrough": "Since `-1` refers to the position from the end—that is, the last item—it selects `\"green\"`."
    },
    "answer_explanation": {
      "step_by_step": "1) The items are [\"A\", \"B\", \"C\"]. 2) -1 is the second-to-last position. 3) The last item is C.",
      "why_correct": "If you apply the steps above in order to the sample code, `C` will be the result asked for in the problem.",
      "common_wrong_choice": {
        "choice": "-1",
        "why_wrong": "-1 is an index that specifies a position. The actual result is the value C at that position.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "The bracket-based approach searches for actual items one step at a time, starting from the left."
    },
    "target_statement": "print(items[-1])",
    "focus_span": "items[-1]",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "negative index",
      "last item",
      "list"
    ],
    "pedagogical_intent": "predict_output"
  },
  {
    "id": "PYF94_A2_L02_LIST_007",
    "level": 2,
    "title": "Replacing a Value at a Specific Position in a List with a New Value",
    "concepts": [
      "assignment",
      "subscription"
    ],
    "reading_goal": "Read the core operations of the assignment in the order they are actually executed, and track values and flow.",
    "code": "items = [\"A\", \"B\"]\nitems[0] = \"Z\"\nprint(items[0])",
    "question_type": "output_prediction",
    "question": "What value is printed after changing `items[0]` to `Z`?",
    "choices": [
      "A",
      "B",
      "Z",
      "0"
    ],
    "answer": "Z",
    "explanation": "1) Initially, items[0] is A. 2) items[0] = \"Z\" selects position 0. 3) Replace the A at that position with Z. 4) Next, `items[0]` is `Z`. If you apply the above steps in order to the problem’s code, `Z` is the result the problem is asking for. Commonly Confused Wrong Answer: `A`—`A` is the value before assignment. `items[0] = \"Z\"` replaces the value at that position with `Z`. Where it went wrong: If you selected this incorrect answer, compare the sequence above to see which stage—the current value, index position, conditional path, loop state, or method return value—has changed. Read the assignment statement as creating the current value on the right-hand side first, then storing it in the target on the left-hand side.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "assignment",
    "reading_goal_internal": "assignment의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`items[0] = value` is an assignment that replaces a single item at the specified position with a new value, rather than modifying the entire list.",
      "how_to_read": "First, locate where `items[0]` is, and then store the new value on the right in that location.",
      "key_point": "If a bracket appears on the left, the **existing value at that position is replaced with the new value**.",
      "common_mistake": "It’s easy to assume that new values are added to the end of a list. Additions are read as `append()`, while position replacements are read as `items[index] =...`."
    },
    "teaching_example": {
      "code": "items = [\"red\", \"blue\"]\nitems[1] = \"green\"\nprint(items)",
      "walkthrough": "Replace the existing value `\"blue\"` at index 1 with `\"green\"`, so that the list becomes `[\"red\",\"green\"]`."
    },
    "answer_explanation": {
      "step_by_step": "1) Initially, items[0] is A. 2) items[0] = \"Z\" selects position 0. 3) The A at that position is replaced with Z. 4) Next, items[0] is Z.",
      "why_correct": "If you apply the steps above in order to the problem code, `Z` will be the result asked for in the problem.",
      "common_wrong_choice": {
        "choice": "A",
        "why_wrong": "A is the value before assignment. `items[0] = \"Z\"` replaces the value at that location with Z.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "The assignment operator is read as: first creating the current value on the right, then storing it in the target on the left."
    },
    "target_statement": "items[0] = \"Z\"",
    "focus_span": "items[0] = \"Z\"",
    "focus_mode": "whole_statement",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "list update",
      "subscription assignment",
      "mutation"
    ],
    "pedagogical_intent": "predict_output"
  },
  {
    "id": "PYF94_A2_L02_LIST_008",
    "level": 2,
    "title": "Treat an empty list as False in conditional statements",
    "concepts": [
      "if_statement",
      "else_clause"
    ],
    "reading_goal": "Read the core behavior of an if statement in the order of actual execution, tracking values and flow.",
    "code": "items = []\nif items:\n    print(\"present\")\nelse:\n    print(\"empty\")",
    "question_type": "meaning_choice",
    "question": "If an empty list `[]` is evaluated in an `if items:` statement, which block will be executed?",
    "choices": [
      "if block",
      "else block",
      "Both",
      "Error"
    ],
    "answer": "else block",
    "explanation": "1) items is the empty list []. 2) An empty list is falsy in a condition. 3) The if block is skipped. 4) The else block prints `empty`. Following the problem code in order, the result being asked for is the `else block`. Common wrong answer: the if block. Even though a list value exists, an empty list is treated as false when it contains no items. Misread point: recheck the current value and which branch the condition selects.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "if_statement",
    "reading_goal_internal": "if_statement의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "In Python, using an empty list `[]` directly in a condition is treated like False. A list with at least one item is treated like True.",
      "how_to_read": "When encountering `if items:`, the program first checks whether `items` is empty. If it is `[]`, the condition is false, so if there is an `else` clause, the program proceeds to that block.",
      "key_point": "An empty container may be interpreted as false in a condition.",
      "common_mistake": "It’s easy to assume that a list object always evaluates to True. In Python conditionals, **whether the contents are empty** is also used to determine true or false."
    },
    "teaching_example": {
      "code": "values = []\nif values:\n    print(\"data\")\nelse:\n    print(\"empty\")",
      "walkthrough": "Since an empty list evaluates to False in the condition, the `\"empty\"` block in the else statement is executed."
    },
    "answer_explanation": {
      "step_by_step": "1) `items` is an empty list []. 2) An empty list evaluates to `False` in a condition. 3) The `if` block is skipped. 4) `Nothing` is printed in the `else` block.",
      "why_correct": "If you apply the steps above in order to the code in the problem, the `else block` will be the result asked for in the problem.",
      "common_wrong_choice": {
        "choice": "if block",
        "why_wrong": "Even if a value called a list exists, if it is empty, it is treated as False in a conditional statement.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "In a conditional statement, you first determine whether the condition is True or False, and then select the block to be executed."
    },
    "target_statement": "if items:",
    "focus_span": "if items:",
    "focus_mode": "compound_header",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "truthiness",
      "empty list",
      "if"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_LIST_009",
    "level": 2,
    "title": "Treating a list with values as True in a condition",
    "concepts": [
      "if_statement",
      "call"
    ],
    "reading_goal": "Read the core behavior of an if statement in the order of actual execution, tracking values and flow.",
    "code": "items = [\"A\"]\nif items:\n    print(\"present\")",
    "question_type": "meaning_choice",
    "question": "`items = [\"A\"]` What is the result of the condition `if items:` when it is evaluated?",
    "choices": [
      "True",
      "False",
      "A",
      "None"
    ],
    "answer": "True",
    "explanation": "1) `items` contains one item, \"A.\" 2) Since it is not an empty list, it evaluates to `True`. 3) The `if` block is executed. 4) “There is” is printed. If you apply the above steps in order to the problem code, `True` is the result asked for in the question. Common mistake: False. An empty list is evaluated as False. However, this list actually contains one item, “A.” Where it went wrong: If you selected this incorrect answer, compare the above sequence to see which step—current value, index position, conditional path, iteration state, or method return value—has changed. A conditional statement first determines whether the condition is True or False, and then selects the block to execute.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "if_statement",
    "reading_goal_internal": "if_statement의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "In Python, using an empty list `[]` directly in a condition is treated like False. A list with at least one item is treated like True.",
      "how_to_read": "When encountering `if items:`, the program first checks whether `items` is empty. If it is `[]`, the condition is false, so if there is an `else` clause, the program proceeds to that block.",
      "key_point": "An empty container may be interpreted as false in a condition.",
      "common_mistake": "It’s easy to assume that a list object always evaluates to True. In Python conditionals, **whether the contents are empty** is also used to determine true or false."
    },
    "teaching_example": {
      "code": "values = [\"x\"]\nif values:\n    print(\"data\")",
      "walkthrough": "Since there is at least one item in the list, the condition evaluates to True, causing the if block to execute."
    },
    "answer_explanation": {
      "step_by_step": "1) `items` contains exactly one item, \"A.\" 2) Since it is not an empty list, it evaluates to `True`. 3) The `if` block is executed. 4) \"A\" is printed.",
      "why_correct": "If you apply the above steps in order to the problem code, `True` will be the result asked for in the problem.",
      "common_wrong_choice": {
        "choice": "False",
        "why_wrong": "An empty list is considered false. This list actually contains one item, A.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "In a conditional statement, you first determine whether the condition is True or False, and then select the block to be executed."
    },
    "target_statement": "if items:",
    "focus_span": "if items:",
    "focus_mode": "compound_header",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "truthiness",
      "nonempty list",
      "if"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_LIST_010",
    "level": 2,
    "title": "Concatenating the items of two lists using `list +`",
    "concepts": [
      "sequence_operation",
      "name_reference",
      "call"
    ],
    "reading_goal": "Read the core behavior of operation on a sequence in the actual order of execution and track values and flow.",
    "code": "a = [\"A\"]\nb = [\"B\"]\nprint(a + b)",
    "question_type": "meaning_choice",
    "question": "`a + b` What is the result?",
    "choices": [
      "AB",
      "[\"A\"]",
      "[\"A\", \"B\"]",
      "[\"B\"]"
    ],
    "answer": "[\"A\", \"B\"]",
    "explanation": "1) a is [\"A\"]. 2) b is [\"B\"]. 3) The list concatenation operator (+) concatenates elements in order. 4) The result is [\"A\", \"B\"]. If you apply the steps above in order to the problem code, `[\"A\", \"B\"]` will be the result asked for in the problem. Common mistake: This is list concatenation, not string concatenation. The result is also a list: `['A','B']`. Where it went wrong: If you selected this incorrect answer, compare the above sequence to determine which step—the current value, index position, conditional path, iteration state, or method return value—has changed. Read the code by following the actual execution order and the current value at each step.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "sequence_operation",
    "reading_goal_internal": "sequence_operation의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "The `+` operator between lists concatenates the elements of the two lists in order to create a new list.",
      "how_to_read": "Place the items from the left list first, then append the items from the right list after them.",
      "key_point": "Instead of concatenating characters like a string, combine them **on a list item basis**.",
      "common_mistake": "It’s easy to assume that `['A'] + ['B']` results in the string `AB`. The result is actually a list with two elements: `['A', 'B']`."
    },
    "teaching_example": {
      "code": "left = [1, 2]\nright = [3]\nprint(left + right)",
      "walkthrough": "Append 3 from the right list to 1 and 2 in the left list to create `[1,2,3]`."
    },
    "answer_explanation": {
      "step_by_step": "1) a is [\"A\"]. 2) b is [\"B\"]. 3) The list + operator concatenates items in order. 4) The result is [\"A\", \"B\"].",
      "why_correct": "If you apply the steps above in order to the code in the problem, `[\"A\", \"B\"]` will be the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "AB",
        "why_wrong": "This is list concatenation, not string concatenation. The result is also a list: `['A','B']`.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "Read the code by following the actual execution sequence step by step, noting the current values at each point."
    },
    "target_statement": "print(a + b)",
    "focus_span": "a + b",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "list concatenation",
      "operator",
      "sequence"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_LIST_011",
    "level": 2,
    "title": "Distinguish that `append()` modifies the list but returns `None`.",
    "concepts": [
      "call",
      "assignment",
      "attribute_access"
    ],
    "reading_goal": "Read the core behavior of a `call` in the order it actually executes, tracking values and flow.",
    "code": "items = []\nresult = items.append(\"A\")\nprint(items)",
    "question_type": "meaning_choice",
    "question": "`items.append(\"A\")` What is the state of `items` immediately after it is executed?",
    "choices": [
      "[]",
      "[\"A\"]",
      "None",
      "A"
    ],
    "answer": "[\"A\"]",
    "explanation": "1) `items` is an empty list. 2) `append(\"A\")` changes `items` itself to `[\"A\"]`. 3) The return value of the `append` call, `None`, is stored in `result`. 4) `print(items)` displays `[\"A\"]`. If you apply the steps above in order to the code in the problem, `[\"A\"]` is the result the problem is asking for. Commonly Confusing Wrong Answer: None. `None` is the return value of the `append` call. The list itself has already been changed to `['A']`. Point of Misreading: If you selected this incorrect answer, compare the sequence above to see which stage—current value, index position, conditional path, iteration state, or method return value—has changed. When reading about function or method calls, distinguish between the input values, return values, and changes to the original state.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "call의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`append()` adds an item to an existing list, but the call itself returns `None`.",
      "how_to_read": "`result = items.append('A')` First, `items` is converted to `['A']`. Next, the return value of the `append` call, `None`, is stored in `result`.",
      "key_point": "You need to examine **changes in the list’s state** and **method return values** separately.",
      "common_mistake": "It’s easy to assume that `append` returns a modified list. While the list itself is modified, the return value is `None`."
    },
    "teaching_example": {
      "code": "items = []\nreturned = items.append(\"X\")\nprint(items)\nprint(returned)",
      "walkthrough": "After the `append` call, `items` changes to `[\"X\"]`, but since the call returns `None`, `returned` is assigned `None`."
    },
    "answer_explanation": {
      "step_by_step": "1) `items` is an empty list. 2) `append(\"A\")` mutates `items` into `[\"A\"]`. 3) The return value of the `append` call, `None`, is stored in `result`. 4) `print(items)` shows `[\"A\"]`.",
      "why_correct": "If you apply the steps above in order to the code in the problem, `[\"A\"]` will be the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "None",
        "why_wrong": "`None` is the return value of the `append` call. The list itself has already been changed to `['A']`.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "When reading function and method calls, identify the input values, return values, and changes to the original state."
    },
    "target_statement": "result = items.append(\"A\")",
    "focus_span": "items.append(\"A\")",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "append",
      "return value",
      "mutation"
    ],
    "pedagogical_intent": "trace_state"
  },
  {
    "id": "PYF94_A2_L02_LIST_012",
    "level": 2,
    "title": "Splitting a string into list items using `split()`",
    "concepts": [
      "call",
      "assignment",
      "attribute_access"
    ],
    "reading_goal": "Read the core behavior of a `call` in the order it actually executes, tracking values and flow.",
    "code": "text = \"A,B\"\nitems = text.split(\",\")\nprint(items[0])",
    "question_type": "meaning_choice",
    "question": "`text` When `\"A,B\"`, what is the result of `text.split(\",\")`?",
    "choices": [
      "[\"A,B\"]",
      "[\"A\", \"B\"]",
      "A",
      ","
    ],
    "answer": "[\"A\", \"B\"]",
    "explanation": "1) The text is \"A,B\". 2) split(\",\") splits the string at the commas. 3) The result is [\"A\", \"B\"]. 4) That list is stored in `items`. If you apply the steps above in order to the problem code, `[\"A\", \"B\"]` is the result the problem is asking for. Common mistake: “A A” is the first item in the list returned by `split`. The result of the call itself is a list containing two items: A and B. Point of Misreading: If you selected this incorrect answer, compare the sequence above to see which stage—current value, index position, conditional path, iteration state, or method return value—has changed. When reading about function or method calls, distinguish between the input values, return values, and changes to the original state.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "call의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`split()` is a method that splits a string at a specified delimiter and returns the resulting fragments as a list.",
      "how_to_read": "`text.split(\",\")` It splits the string at the comma positions to create list items from each segment.",
      "key_point": "The delimiter itself is not included in the result, and the split string fragments are placed in a list.",
      "common_mistake": "It’s easy to think that commas are also treated as list items. Commas are merely a guide indicating where to break the list."
    },
    "teaching_example": {
      "code": "text = \"red,blue\"\nitems = text.split(\",\")\nprint(items)",
      "walkthrough": "Split the string at the commas to create a list named `[\"red\",\"blue\"]`."
    },
    "answer_explanation": {
      "step_by_step": "1) The text is \"A,B\". 2) split(\",\") splits the string at the commas. 3) The result is [\"A\", \"B\"]. 4) That list is stored in `items`.",
      "why_correct": "If you apply the steps above in order to the code in the problem, `[\"A\", \"B\"]` will be the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "A",
        "why_wrong": "A is the first item in the list returned by `split`. The result of the call itself is a list containing two items: A and B.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "When reading function and method calls, identify the input values, return values, and changes to the original state."
    },
    "target_statement": "items = text.split(\",\")",
    "focus_span": "text.split(\",\")",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "split",
      "list result",
      "string"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_LIST_013",
    "level": 2,
    "title": "Add two numbers taken from a list together as actual values",
    "concepts": [
      "arithmetic",
      "subscription",
      "call"
    ],
    "reading_goal": "Read the core operations of arithmetic in the order they are actually executed, and track values and flow.",
    "code": "numbers = [1, 2, 3]\nprint(numbers[0] + numbers[2])",
    "question_type": "meaning_choice",
    "question": "What is the result of the calculation `numbers[0] + numbers[2]`?",
    "choices": [
      "3",
      "4",
      "6",
      "13"
    ],
    "answer": "4",
    "explanation": "1) `numbers[0]` is the first value, 1. 2) `numbers[2]` is the third value, 3. 3) Add the retrieved values 1 and 3, not the index numbers. 4) The result is 4. If you apply the steps above in order to the problem code, `4` will yield the result asked for in the problem. A common incorrect answer: 3. 3 is just the last value retrieved. Adding the first value (1) and the third value (3) results in 4. Where it went wrong: If you selected this incorrect answer, compare the sequence above to see which stage—current value, index position, conditional path, iteration state, or method return value—differed. Read the code by following the actual execution sequence step by step, noting the current value at each point.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "arithmetic",
    "reading_goal_internal": "arithmetic의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "Comparison operators such as `>=`, `==`, and `!=` compare two values and return either `True` or `False`.",
      "how_to_read": "First, check the values on the left and right sides, then determine whether they satisfy the relationship specified by the operator.",
      "key_point": "The result of a comparison is not the original value, but a **true/false value**.",
      "common_mistake": "It’s easy to assume that the numbers or strings used in a comparison are the results themselves. Once the comparison is complete, new values—such as `True` or `False`—are created."
    },
    "teaching_example": {
      "code": "numbers = [4, 5, 6]\nprint(numbers[0] + numbers[2])",
      "walkthrough": "First retrieve `4` from `numbers[0]` and `6` from `numbers[2]`, then calculate the actual numbers: `4 + 6 = 10`."
    },
    "answer_explanation": {
      "step_by_step": "1) numbers[0] is the first value, 1. 2) numbers[2] is the third value, 3. 3) Add the retrieved values 1 and 3, not the index numbers. 4) The result is 4.",
      "why_correct": "If you apply the steps above in order to the problem code, `4` will be the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "3",
        "why_wrong": "3 is only the last value retrieved. Adding the first value 1 and the third value 3 gives 4.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "Read the code by following the actual execution sequence step by step, noting the current values at each point."
    },
    "target_statement": "print(numbers[0] + numbers[2])",
    "focus_span": "numbers[0] + numbers[2]",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "list index",
      "addition",
      "arithmetic"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_LIST_014",
    "level": 2,
    "title": "Subtract 1 from the number of items in `len()` to find the last index",
    "concepts": [
      "arithmetic",
      "call",
      "assignment"
    ],
    "reading_goal": "Read the core operations of arithmetic in the order they are actually executed, and track values and flow.",
    "code": "items = [\"A\", \"B\", \"C\"]\nlast = len(items) - 1\nprint(items[last])",
    "question_type": "meaning_choice",
    "question": "When there are 3 items, what is the result of `len(items) - 1`?",
    "choices": [
      "1",
      "2",
      "3",
      "C"
    ],
    "answer": "2",
    "explanation": "1) `items` contains three items. 2) `len(items)` returns 3, the number of items. 3) Since the indices are 0, 1, and 2, 3 - 1 = 2 is the last position. 4) `items[2]` is C. If you apply the steps above in order to the problem code, `2` is the result the problem is asking for. Common mistake: 3. 3 is the number of items. Since indices start at 0, the last position number is 2. Point of Misreading: If you selected this incorrect answer, compare the above sequence to see which step—the current value, index position, condition path, iteration state, or method return value—differs. Read the code by following the actual execution order step by step, noting the current value at each point.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "arithmetic",
    "reading_goal_internal": "arithmetic의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "When a list has n items, the indices start at 0, so the last index is `n - 1`.",
      "how_to_read": "If there are three items, the index numbers are 0, 1, and 2. `len(items)` is 3, and `len(items) - 1` is the index of the last item, which is 2.",
      "key_point": "**The number of items and the last position number differ by 1.**",
      "common_mistake": "It’s easy to mistakenly use the result of `len` (3) as the last index. Since the last index of the three items is 2, you must subtract 1."
    },
    "teaching_example": {
      "code": "items = [\"red\", \"blue\"]\nlast = len(items) - 1\nprint(last)",
      "walkthrough": "If there are two items, the position numbers are 0 and 1. Since `len` is 2, `2 - 1 = 1` becomes the last index."
    },
    "answer_explanation": {
      "step_by_step": "1) There are three items in `items`. 2) `len(items)` is 3. 3) Since the indices are 0, 1, and 2, 3 - 1 = 2 is the last position. 4) `items[2]` is \"C.\"",
      "why_correct": "If you apply the steps above in order to the sample code, `2` is the result asked for in the problem.",
      "common_wrong_choice": {
        "choice": "3",
        "why_wrong": "3 is the number of items. Since indices start at 0, the last position number is 2.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "Read the code by following the actual execution sequence step by step, noting the current values at each point."
    },
    "target_statement": "last = len(items) - 1",
    "focus_span": "len(items) - 1",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "len",
      "last index",
      "off by one"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_LIST_015",
    "level": 2,
    "title": "If a variable name is passed as an argument to `append()`, pass its current value",
    "concepts": [
      "argument_passing",
      "call",
      "name_reference"
    ],
    "reading_goal": "Read the core behavior of passing an argument in the order of actual execution and track values and flow.",
    "code": "name = \"Python\"\nitems = []\nitems.append(name)\nprint(items[0])",
    "question_type": "meaning_choice",
    "question": "What value is actually passed to `items.append(name)`?",
    "choices": [
      "name",
      "Python",
      "items",
      "None"
    ],
    "answer": "Python",
    "explanation": "1) `name` contains \"Python.\" 2) `append(name)` reads the current value of `name`. 3) The string \"Python\" is added to the end of the list. 4) `items[0]` is Python. If you apply the above steps in order to the problem code, `Python` is the result asked for in the problem. Common mistake: `name` without quotes is a variable, not a string. The current value of that variable, `Python`, is passed to `append`. Where it went wrong: If you selected this wrong answer, compare the sequence above to see which step—current value, index position, conditional path, iteration state, or method return value—has changed. Read the code by following the actual execution order step by step, noting the current value at each point.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "argument_passing",
    "reading_goal_internal": "argument_passing의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "If there is another variable name on the right, it does not store the name itself, but rather reads the value that variable currently points to and stores it in the variable on the left.",
      "how_to_read": "For `name = label`, first find the current value of `label`. If `label` contains `\"LiDAR\"`, store that string value in `name`.",
      "key_point": "The order for assignment is **read the current value on the right → store it on the left**.",
      "common_mistake": "It’s easy to assume that the text “`label`” is included. However, since “`label`” without quotes is a variable rather than a string, the program reads the variable’s current value rather than its name."
    },
    "teaching_example": {
      "code": "label = \"Radar\"\nitems = []\nitems.append(label)\nprint(items)",
      "walkthrough": "In `append(label)`, the unquoted `label` is a variable, so its current value, `\"Radar\"`, is passed as an argument and added to the list."
    },
    "answer_explanation": {
      "step_by_step": "1) `name` stores `\"Python\"`. 2) `append(name)` reads the current value of `name`. 3) The string `\"Python\"` is added to the end of the list. 4) `items[0]` is `Python`.",
      "why_correct": "If you apply the steps above in order to the problem code, the result will be what the `Python` problem is asking for.",
      "common_wrong_choice": {
        "choice": "name",
        "why_wrong": "A `name` without quotation marks is a variable, not a string character. Python passes the current value of that variable to `append`.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "Read the code by following the actual execution sequence step by step, noting the current values at each point."
    },
    "target_statement": "items.append(name)",
    "focus_span": "name",
    "focus_mode": "subexpression",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "argument passing",
      "append",
      "variable value"
    ],
    "pedagogical_intent": "identify_meaning"
  },
  {
    "id": "PYF94_A2_L02_LIST_016",
    "level": 2,
    "title": "If you pass a list variable directly to `print()`, the entire list will be printed.",
    "concepts": [
      "call",
      "name_reference"
    ],
    "reading_goal": "Read the core behavior of a `call` in the order it actually executes, tracking values and flow.",
    "code": "items = [\"A\", \"B\"]\nprint(items)",
    "question_type": "meaning_choice",
    "question": "`print(items)` What does this show?",
    "choices": [
      "A only",
      "B-level",
      "The entire list `[\"A\", \"B\"]`.",
      "String AB"
    ],
    "answer": "The entire list `[\"A\", \"B\"]`.",
    "explanation": "1) `items` contains two elements: A and B. 2) The entire `items` list is passed to `print`. 3) Since no specific index was used, only one element is not selected. 4) The entire list [\"A\", \"B\"] is printed. If you apply the steps above in order to the problem code, `the entire list ['A', 'B']` is the result the problem is asking for. Common mistake: The list items “A” and “B” are not automatically combined into a single string. The entire list is printed as-is. Point of Misreading: If you selected this wrong answer, compare the sequence above to see which step—current value, index position, condition path, iteration state, or method return value—has changed. When reading about function or method calls, distinguish between the input values, return values, and changes to the original state.",
    "side_card_ids": [
      "PYF94_A2_LIST_INDEX",
      "PYF94_A2_LIST_APPEND",
      "PYF94_A2_LIST_LEN"
    ],
    "project_context": "Lists are the structures most commonly encountered in code that handles lists of data, lists of cards, and search results.",
    "authoring_version": "V2.3",
    "primary_concept": "call",
    "reading_goal_internal": "call의 핵심 동작을 실제 실행 순서대로 읽고 값과 흐름을 추적한다.",
    "concept_explanation": {
      "what_it_is": "`print(items)` If you print the list variable itself, as shown here, it displays the entire list structure rather than just a single item.",
      "how_to_read": "If there is no bracket index, it means no specific item has been selected, so the entire current list is passed to `print`.",
      "key_point": "`items` `items[0]` are different. The former is the entire list, while the latter is the first item.",
      "common_mistake": "It’s easy to assume that the strings in the list are automatically concatenated to appear as “`AB`.” However, if you print the entire list, you’ll see the square brackets and the separators between items."
    },
    "teaching_example": {
      "code": "colors = [\"red\", \"blue\"]\nprint(colors)",
      "walkthrough": "If you pass the list variable itself to `print` without an index, the entire `[\"red\",\"blue\"]` is printed as a list."
    },
    "answer_explanation": {
      "step_by_step": "1) The `items` list contains two elements: `A` and `B`. 2) The entire `items` list is passed to `print`. 3) Since no specific index was used, only one element is not selected. 4) The entire list [\"A\", \"B\"] is printed.",
      "why_correct": "If you apply the steps above in order to the code in the problem, `the entire list ['A', 'B']` will be the result the problem is asking for.",
      "common_wrong_choice": {
        "choice": "String AB",
        "why_wrong": "List items are not automatically concatenated into a single string. The entire list is printed as-is.",
        "misread_step": "If you selected this incorrect answer, compare it with the order above to see which step—current value, index position, conditional path, loop state, or method return value—has changed."
      },
      "takeaway": "When reading function and method calls, identify the input values, return values, and changes to the original state."
    },
    "target_statement": "print(items)",
    "focus_span": "print(items)",
    "focus_mode": "whole_statement",
    "coverage_domain": "data_structures",
    "coverage_topics": [
      "print list",
      "list representation",
      "whole value"
    ],
    "pedagogical_intent": "identify_meaning"
  }
]
