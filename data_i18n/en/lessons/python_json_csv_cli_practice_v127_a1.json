[
  {
    "id": "PY127_L05_SUFFIX_BRANCH_CSV_JSON_001",
    "title": "Branching by File Extension: JSON and CSV",
    "level": 5,
    "question_type": "multiple_choice",
    "question": "To determine whether an input file is a .json or a .csv file, what is the best value to check first?",
    "code": "from pathlib import Path\n\ninput_path = Path(args.input)\nif input_path.suffix == '.json':\n    print('Process JSON')\nelif input_path.suffix == '.csv':\n    print('Process CSV')",
    "choices": [
      "args.output.upper()",
      "input_path.suffix",
      "Path.cwd().parent",
      "Indentation in `json.dumps`"
    ],
    "answer": "input_path.suffix",
    "explanation": "Path.suffix returns the final extension, so it is a useful first hint for choosing a JSON or CSV loader. Compare suffix.lower() if uppercase extensions are accepted, and still handle parser errors because an extension does not prove the content is valid.",
    "concepts": [
      "if",
      "import",
      "print",
      "Path.suffix",
      "JSON",
      "CSV",
      "branching"
    ],
    "reading_goal": "Understand why the processing flow is divided based on file type using `Path.suffix`.",
    "side_card_ids": [
      "PYV127_A1_SUFFIX_DISPATCH",
      "PYV127_A1_SUMMARY_OUTPUT"
    ]
  },
  {
    "id": "PY127_L05_JSON_LOADS_FROM_FILE_001",
    "title": "Reading JSON with `read_text` and `json.loads`",
    "level": 5,
    "question_type": "multiple_choice",
    "question": "Which of the following best describes the role of `json.loads(text)` in the following code? `text = input_path.read_text(encoding='utf-8')` `data = json.loads(text)`",
    "code": "import json\nfrom pathlib import Path\n\ninput_path = Path('data.json')\ntext = input_path.read_text(encoding='utf-8')\ndata = json.loads(text)",
    "choices": [
      "Automatically sort CSV rows",
      "Convert a JSON string to Python data",
      "Automatically deletes the output folder",
      "Recreate all terminal arguments"
    ],
    "answer": "Convert a JSON string to Python data",
    "explanation": "`read_text()` reads the contents of a file as a string, and `json.loads()` converts a JSON-formatted string into Python data types such as a dictionary or list. Rather than simply memorizing the correct answers, you should view this card as an opportunity to practice connecting the problem conditions with the order in which the code is executed.",
    "concepts": [
      "import",
      "json.loads",
      "read_text",
      "JSON",
      "dict",
      "list"
    ],
    "reading_goal": "You can see the process of converting a JSON string read as text into Python data.",
    "side_card_ids": [
      "PYV127_A1_JSON_READ_WRITE",
      "PYV127_A1_SUMMARY_OUTPUT"
    ]
  },
  {
    "id": "PY127_L05_CSV_DICTREADER_ROWS_001",
    "title": "Reading Rows with csv.DictReader",
    "level": 5,
    "question_type": "multiple_choice",
    "question": "Which of the following is a valid reason for wrapping `csv.DictReader` in a list? `rows = list(csv.DictReader(f))`",
    "code": "import csv\n\nwith open('data.csv', newline='', encoding='utf-8') as f:\n    rows = list(csv.DictReader(f))\nprint(rows[0])",
    "choices": [
      "To store a JSON string with indentation,",
      "To automatically create an output folder",
      "To handle each row of a CSV file as a list of dictionaries,",
      "To force a regular expression match to succeed"
    ],
    "answer": "To handle each row of a CSV file as a list of dictionaries,",
    "explanation": "csv.DictReader reads each row of a CSV file as a dictionary organized by column names. If you wrap it in a list, you can group multiple rows into a list and use it for iteration, filtering, and summarization.",
    "concepts": [
      "import",
      "print",
      "csv.DictReader",
      "CSV",
      "rows",
      "dict"
    ],
    "reading_goal": "Convert the DictReader results into a list to understand the process of handling CSV rows.",
    "side_card_ids": [
      "PYV127_A1_CSV_DICTREADER_WRITER",
      "PYV127_A1_SUMMARY_OUTPUT"
    ]
  },
  {
    "id": "PY127_L05_CSV_FIELDNAMES_001",
    "title": "Checking CSV Column Names",
    "level": 5,
    "question_type": "multiple_choice",
    "question": "Which of the following is a valid reason for checking `reader.fieldnames` in `DictReader`?",
    "code": "import csv\n\nwith open('data.csv', newline='', encoding='utf-8') as f:\n    reader = csv.DictReader(f)\n    print(reader.fieldnames)",
    "choices": [
      "To change the current Python version,",
      "To automatically compress JSON results",
      "To delete the input file immediately",
      "To check which columns are in the CSV file,"
    ],
    "answer": "To check which columns are in the CSV file,",
    "explanation": "`reader.fieldnames` displays the headers of the CSV file—that is, the list of column names. This is useful for determining which keys can be accessed using a syntax like `row['name']`.",
    "concepts": [
      "import",
      "print",
      "fieldnames",
      "CSV header",
      "DictReader",
      "column"
    ],
    "reading_goal": "Understand how CSV headers are mapped to the field names in DictReader.",
    "side_card_ids": [
      "PYV127_A1_CSV_DICTREADER_WRITER"
    ]
  },
  {
    "id": "PY127_L05_SUMMARY_COUNT_COLUMNS_001",
    "title": "Counting Rows and Creating a Column Summary",
    "level": 5,
    "question_type": "multiple_choice",
    "question": "After reading the CSV rows, which of the following best describes the information contained in `summary`? `summary = {'rows': len(rows), 'columns': list(rows[0].keys())}`",
    "code": "rows = [{'name': 'A', 'score': '10'}, {'name': 'B', 'score': '20'}]\nsummary = {'rows': len(rows), 'columns': list(rows[0].keys())}\nprint(summary)",
    "choices": [
      "Absolute path of the current folder",
      "List of row counts and column names",
      "Regular Expression Capture Group Numbers",
      "List of Virtual Environment Packages"
    ],
    "answer": "List of row counts and column names",
    "explanation": "len(rows) is the row count, and rows[0].keys() shows the key order of the first row. If rows is empty, however, rows[0] raises IndexError. Preserve reader.fieldnames separately or handle an empty list before accessing its first item.",
    "concepts": [
      "print",
      "summary",
      "len",
      "dict keys",
      "CSV rows"
    ],
    "reading_goal": "Understand how to read CSV or JSON data and summarize the number of rows and the list of columns.",
    "side_card_ids": [
      "PYV127_A1_SUMMARY_OUTPUT",
      "PYV127_A1_CSV_DICTREADER_WRITER"
    ]
  },
  {
    "id": "PY127_L05_JSON_DUMPS_OUTPUT_001",
    "title": "Create a string containing the result using `json.dumps`",
    "level": 5,
    "question_type": "multiple_choice",
    "question": "Which of the following is the most appropriate reason for using `ensure_ascii=False` in the following code? `text = json.dumps(summary, ensure_ascii=False, indent=2)`",
    "code": "import json\n\nsummary = {'message': 'done', 'rows': 3}\ntext = json.dumps(summary, ensure_ascii=False, indent=2)\nprint(text)",
    "choices": [
      "To automatically change the CSV delimiter to a tab,",
      "To make the argparse option a required input",
      "To save Hangul as-is, rather than in the \\uXXXX format,",
      "To ensure that `Path.exists` always returns `True`,"
    ],
    "answer": "To save Hangul as-is, rather than in the \\uXXXX format,",
    "explanation": "json.dumps() converts Python data into a JSON string. If you set ensure_ascii=False, Korean characters will not be escaped and will be stored as-is for better human readability.",
    "concepts": [
      "import",
      "print",
      "json.dumps",
      "ensure_ascii",
      "indent",
      "JSON output"
    ],
    "reading_goal": "Understand how `ensure_ascii=False` and `indent` in `json.dumps` affect the output.",
    "side_card_ids": [
      "PYV127_A1_JSON_READ_WRITE",
      "PYV127_A1_SUMMARY_OUTPUT"
    ]
  },
  {
    "id": "PY127_L06_CSV_DICTWRITER_SAVE_001",
    "title": "Saving the Resulting CSV Using csv.DictWriter",
    "level": 6,
    "question_type": "multiple_choice",
    "question": "Which of the following is a valid reason for specifying fieldnames when saving a CSV file using DictWriter?",
    "code": "import csv\n\nrows = [{'name': 'A', 'score': 10}]\nwith open('out.csv', 'w', newline='', encoding='utf-8') as f:\n    writer = csv.DictWriter(f, fieldnames=['name', 'score'])\n    writer.writeheader()\n    writer.writerows(rows)",
    "choices": [
      "To execute regular expression patterns more quickly",
      "To determine the order and names of the columns to be saved,",
      "To make it appear as though it is reading even when there is no input file",
      "To hide all terminal options"
    ],
    "answer": "To determine the order and names of the columns to be saved,",
    "explanation": "The `fieldnames` parameter of `csv.DictWriter` specifies the column names and their order to be saved in the CSV file. `writeheader()` writes the header to the first row, and `writerows()` saves multiple rows.",
    "concepts": [
      "import",
      "csv.DictWriter",
      "fieldnames",
      "writeheader",
      "writerows"
    ],
    "reading_goal": "Understand the respective roles of `fieldnames`, `writeheader`, and `writerows` in DictWriter.",
    "side_card_ids": [
      "PYV127_A1_CSV_DICTREADER_WRITER"
    ]
  },
  {
    "id": "PY127_L06_JSON_CSV_CLI_FLOW_001",
    "title": "Complete Workflow for JSON/CSV Processing via CLI",
    "level": 6,
    "question_type": "multiple_choice",
    "question": "Which of the following is the most appropriate CLI workflow for reading a JSON/CSV input file and saving a summary JSON?",
    "code": "def main():\n    args = parse_args()\n    input_path = Path(args.input)\n    output_path = Path(args.output)\n    data = load_data(input_path)\n    summary = make_summary(data)\n    output_path.write_text(json.dumps(summary, ensure_ascii=False, indent=2), encoding='utf-8')",
    "choices": [
      "Delete files, push to Git, shut down the server, ignore the results",
      "Performs only regular expression searches; does not read the input file",
      "The output is saved first, and the arguments are parsed later.",
      "Parsing arguments, reading input, generating a summary, and saving the results"
    ],
    "answer": "Parsing arguments, reading input, generating a summary, and saving the results",
    "explanation": "The flow parses arguments, loads data according to the input format, creates a summary, serializes it as JSON, and writes it. Production code should also create output_path.parent and handle file, parsing, and write errors at the appropriate stage.",
    "concepts": [
      "def",
      "function",
      "CLI flow",
      "JSON",
      "CSV",
      "summary",
      "save result"
    ],
    "reading_goal": "Understand how argparse, pathlib, reading JSON/CSV files, and saving results are integrated into a single CLI workflow.",
    "side_card_ids": [
      "PYV127_A1_SUFFIX_DISPATCH",
      "PYV127_A1_SUMMARY_OUTPUT"
    ]
  }
]
