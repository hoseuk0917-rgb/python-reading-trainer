[
  {
    "id": "PYF95_A4_FILE_001_OPEN_READ_ALL",
    "title": "Read the entire file",
    "level": 3,
    "concepts": [
      "comment",
      "print",
      "open",
      "with",
      "read"
    ],
    "reading_goal": "Review the process of opening a file using `with open`, reading its entire contents into a string variable, and then outputting it. In particular, examine the file mode, the scope of the `with` block, the variable in which the read string is stored, the path component pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "# memo.txt contents: hello\nwith open(\"memo.txt\", \"r\", encoding=\"utf-8\") as f:\n    text = f.read()\nprint(text)",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "memo.txt",
      "hello",
      "utf-8",
      "None"
    ],
    "answer": "hello",
    "explanation": "The `read` function reads the file content \"hello\" as a string and stores it in `text`. The correct answer is selected based on whether the code reads or writes the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attributes point to.",
    "project_context": "The code that reads study cards, logs, and configuration files is linked to the file-reading workflow.",
    "side_card_ids": [
      "PYF95_A4_OPEN_READ",
      "PYF95_A4_WITH_FILE"
    ]
  },
  {
    "id": "PYF95_A4_FILE_002_READLINE_FIRST",
    "title": "Read the first line",
    "level": 3,
    "concepts": [
      "comment",
      "print",
      "open",
      "readline",
      "strip"
    ],
    "reading_goal": "Review the flow where `readline` reads the first line of the file and `strip` removes the newline character. In particular, check the file mode, the scope of the `with` block, the variable where the read string is stored, the path component pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "# memo.txt contents:\n# A\n# B\nwith open(\"memo.txt\", \"r\", encoding=\"utf-8\") as f:\n    line = f.readline().strip()\nprint(line)",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "\"B\" is printed",
      "A",
      "\"A B\" is printed",
      "\"memo.txt\" is printed"
    ],
    "answer": "A",
    "explanation": "readline() reads the first line, 'A\\n', and strip() removes surrounding whitespace including the newline, leaving A. print displays A without quotation marks. If only the line ending should be removed while preserving other spaces, use a narrower operation such as rstrip('\\n').",
    "project_context": "Code that checks only the first line of a multi-line file is often used for processing headers or titles.",
    "side_card_ids": [
      "PYF95_A4_OPEN_READ"
    ]
  },
  {
    "id": "PYF95_A4_FILE_003_READLINES_LEN",
    "title": "Counting the Number of Lines",
    "level": 3,
    "concepts": [
      "comment",
      "print",
      "open",
      "readlines",
      "len"
    ],
    "reading_goal": "Examine the flow of the program, where `readlines` creates a list of lines from a file and `len` counts the number of lines. In particular, examine the file mode, the scope of the `with` block, the variable where the read string is stored, the path component pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "# memo.txt contents:\n# A\n# B\nwith open(\"memo.txt\", \"r\", encoding=\"utf-8\") as f:\n    lines = f.readlines()\nprint(len(lines))",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "1",
      "2",
      "A",
      "B"
    ],
    "answer": "2",
    "explanation": "Since the file contains two lines, the length of `lines` is 2. pathlib’s `Path` allows you to treat paths as building blocks, while `try/except` separates the normal flow of code from the error flow for code that may fail. The correct answer is determined by whether the code reads from or writes to the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attributes point to.",
    "project_context": "It is linked to validation code that checks the number of rows in the data file.",
    "side_card_ids": [
      "PYF95_A4_OPEN_READ"
    ]
  },
  {
    "id": "PYF95_A4_FILE_004_STRIP_LINES",
    "title": "Removing spaces at the end of lines",
    "level": 3,
    "concepts": [
      "comment",
      "for",
      "print",
      "strip",
      "list",
      "append"
    ],
    "reading_goal": "Understand the process of using `strip` to remove line breaks at the end of each line read from a file and adding them to a new list.",
    "code": "# Example lines value\nlines = [\"A\\n\", \"B\\n\"]\nclean = []\nfor line in lines:\n    clean.append(line.strip())\nprint(clean)",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "It becomes ['A\\n', 'B\\n']",
      "['A', 'B']",
      "It becomes ['line']",
      "It becomes an empty list []"
    ],
    "answer": "['A', 'B']",
    "explanation": "The loop applies strip() to each string, removing surrounding whitespace and newlines, and appends the new strings to clean. These inputs produce ['A', 'B']. The original lines strings are unchanged; use a narrower rstrip('\\n') when only newlines should be removed.",
    "project_context": "Line-by-line cleaning is commonly used in text file cleaning and data preprocessing.",
    "side_card_ids": []
  },
  {
    "id": "PYF95_A4_FILE_005_WRITE_TEXT",
    "title": "Writing a String to a File",
    "level": 3,
    "concepts": [
      "print",
      "open",
      "write",
      "with"
    ],
    "reading_goal": "Distinguish between the `write` operation, which writes to a file, and screen output, which is generated by a separate `print` statement. In particular, check the file mode, the scope of the `with` block, the variable where the read string is stored, the path components pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "with open(\"out.txt\", \"w\", encoding=\"utf-8\") as f:\n    f.write(\"hi\")\nprint(\"done\")",
    "question": "What is stored in the out.txt file?",
    "question_type": "output_prediction",
    "choices": [
      "done",
      "hi",
      "out.txt",
      "None"
    ],
    "answer": "hi",
    "explanation": "open(..., 'w') creates out.txt if needed and truncates an existing file as soon as it opens successfully. f.write('hi') writes hi to the file, not the screen. After with closes the file, print('done') separately displays done, so the answer to the file-content question is hi.",
    "project_context": "When reading the code for saving result files, you must distinguish between screen output and file logs.",
    "side_card_ids": [
      "PYF95_A4_WRITE_APPEND",
      "PYF95_A4_WITH_FILE"
    ]
  },
  {
    "id": "PYF95_A4_FILE_006_WRITE_RETURN_COUNT",
    "title": "write return value",
    "level": 3,
    "concepts": [
      "print",
      "write",
      "return",
      "file"
    ],
    "reading_goal": "After learning that the `write` statement can return the number of characters written, distinguish between the file contents and the values displayed on the screen. In particular, verify the file mode, the scope of the `with` block, the variable in which the read string is stored, the path components pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "with open(\"out.txt\", \"w\", encoding=\"utf-8\") as f:\n    n = f.write(\"hi\")\nprint(n)",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "\"hi\" is printed",
      "The result is 2.",
      "The result is 0.",
      "\"out.txt\" is output"
    ],
    "answer": "The result is 2.",
    "explanation": "Since the length of the string `hi` is 2, the return value `n` of `write` is 2. The correct answer is determined by whether the code reads or writes to the file, whether an error occurs, whether the `except` block is executed, and what part the `Path` attribute points to. Therefore, the result is 2.",
    "project_context": "You can see the return value in the code that checks the results of writing the file.",
    "side_card_ids": [
      "PYF95_A4_WRITE_APPEND"
    ]
  },
  {
    "id": "PYF95_A4_FILE_007_APPEND_MODE",
    "title": "Append in append mode",
    "level": 3,
    "concepts": [
      "comment",
      "print",
      "open",
      "append",
      "write"
    ],
    "reading_goal": "Understand that in append mode, data is appended to the end of the file, but screen output is determined by the `print` statement. In particular, check the file mode, the scope of the `with` block, the variable where the read string is stored, the path components pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "# Existing out.txt contents: A\nwith open(\"out.txt\", \"a\", encoding=\"utf-8\") as f:\n    f.write(\"B\")\nprint(\"saved\")",
    "question": "What about the screen output?",
    "question_type": "output_prediction",
    "choices": [
      "AB",
      "B",
      "saved",
      "A"
    ],
    "answer": "saved",
    "explanation": "Although \"B\" is added to the file, \"saved\" is displayed on the screen. The correct answer is determined by whether the code reads or writes to the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attribute points to.",
    "project_context": "It is linked to code that appends new entries to the end of existing content, similar to a log file.",
    "side_card_ids": [
      "PYF95_A4_WRITE_APPEND"
    ]
  },
  {
    "id": "PYF95_A4_FILE_008_MODE_W_CONCEPT",
    "title": "Choose the Meaning of \"w\" Mode",
    "level": 3,
    "concepts": [
      "open",
      "write",
      "file mode"
    ],
    "reading_goal": "At a conceptual level, we determine that the file mode string defines the nature of the file operation. In particular, we examine the file mode, the scope of the `with` block, the variable in which the read string is stored, the path component pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "mode = \"w\"",
    "question": "Which of the following best describes the meaning of \"mode\" when it is set to 'w' in the \"open\" command?",
    "question_type": "concept_reading",
    "choices": [
      "Read-only",
      "Open for writing and truncate an existing file",
      "Add at the end",
      "Parsing JSON"
    ],
    "answer": "Open for writing and truncate an existing file",
    "explanation": "'w' opens a text file for writing. It creates a missing file, but if the file exists, a successful open truncates it to zero length. Use append mode or a safer temporary-file replacement strategy when existing data must be preserved.",
    "project_context": "If you misinterpret the file mode, you may overlook the risks associated with the save code.",
    "side_card_ids": [
      "PYF95_A4_WRITE_APPEND"
    ]
  },
  {
    "id": "PYF95_A4_FILE_009_MODE_A_CONCEPT",
    "title": "a. Choose the meaning of the mode",
    "level": 3,
    "concepts": [
      "open",
      "append",
      "file mode"
    ],
    "reading_goal": "Determine that \"append\" mode means appending to the existing content. In particular, verify the file mode, the scope of the \"with\" block, the variable where the read string is stored, the path component pointed to by `Path`, and which of the \"try\" and \"except\" blocks is executed.",
    "code": "mode = \"a\"",
    "question": "Which of the following best describes what \"open\" means when the mode is 'a'?",
    "question_type": "concept_reading",
    "choices": [
      "Read",
      "Append at the end, creating the file if needed",
      "Check the file name",
      "Error handling"
    ],
    "answer": "Append at the end, creating the file if needed",
    "explanation": "'a' opens a file for appending at the end and creates it if it does not exist. If the existing content lacks a trailing newline, new text is joined directly, so the program must write any required separator or newline.",
    "project_context": "When working with log or cumulative log files, it is important to understand append mode.",
    "side_card_ids": [
      "PYF95_A4_WRITE_APPEND"
    ]
  },
  {
    "id": "PYF95_A4_FILE_010_WITH_SCOPE",
    "title": "Reading the Scope of a `with` Block",
    "level": 3,
    "concepts": [
      "print",
      "with",
      "indentation",
      "file"
    ],
    "reading_goal": "Use indentation to distinguish between file operations within the `with` block and output outside the block. In particular, check the file mode, the scope of the `with` block, the variable where the read string is stored, the path components pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "with open(\"out.txt\", \"w\", encoding=\"utf-8\") as f:\n    f.write(\"A\")\nprint(\"end\")",
    "question": "Which lines are executed inside the `with` block?",
    "question_type": "concept_reading",
    "choices": [
      "print(\"end\")",
      "f.write(\"A\")",
      "Both are inside the `with` block",
      "Not a single line"
    ],
    "answer": "f.write(\"A\")",
    "explanation": "Only the indented f.write('A') runs inside the with block with the open file. The context manager attempts to close the file when the block exits, including through an exception. The outer print('end') runs after the file is closed.",
    "project_context": "The section on safely closing files should be read in conjunction with the section on indentation.",
    "side_card_ids": [
      "PYF95_A4_WITH_FILE"
    ]
  },
  {
    "id": "PYF95_A4_FILE_011_PATH_JOIN",
    "title": "Concatenating Paths",
    "level": 3,
    "concepts": [
      "import",
      "print",
      "pathlib",
      "Path",
      "name"
    ],
    "reading_goal": "Read the flow of how paths are concatenated using the / operator in the Path object, with name referring to the last filename. In particular, examine the file mode, the scope of the with block, the variable where the read string is stored, the path components pointed to by Path, and which of the try and except blocks is executed.",
    "code": "from pathlib import Path\npath = Path(\"data\") / \"memo.txt\"\nprint(path.name)",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "data",
      "memo.txt",
      "Path",
      "txt"
    ],
    "answer": "memo.txt",
    "explanation": "path.name is the last component, memo.txt. open and with are ways to open a file, while read and write are operations for reading or writing content. pathlib’s `Path` allows you to treat paths as components, while `try/except` separates the normal flow of code from the error flow in cases where the code might fail. The correct answer is selected based on whether the code reads or writes to the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attributes point to.",
    "project_context": "Using pathlib when creating project file paths makes the path structure clearer.",
    "side_card_ids": [
      "PYF95_A4_PATHLIB_PATH"
    ]
  },
  {
    "id": "PYF95_A4_FILE_012_PATH_SUFFIX",
    "title": "Reading the Path Extension",
    "level": 3,
    "concepts": [
      "import",
      "print",
      "pathlib",
      "suffix",
      "path"
    ],
    "reading_goal": "Understand the flow of how `Path.suffix` returns the file extension along with a period. In particular, examine the file mode, the scope of the `with` block, the variable where the read string is stored, the path component pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "from pathlib import Path\npath = Path(\"data/cards.json\")\nprint(path.suffix)",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "json",
      ".json",
      "cards",
      "data"
    ],
    "answer": ".json",
    "explanation": "The suffix of `cards.json` is `.json`. This problem, from Level 3, is an exercise in understanding the execution flow of file handling, path handling, and exception handling. The correct answer is determined by whether the code is reading or writing to the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attributes point to.",
    "project_context": "Suffixes are frequently used in code to distinguish file types.",
    "side_card_ids": [
      "PYF95_A4_PATHLIB_PATH"
    ]
  },
  {
    "id": "PYF95_A4_FILE_013_PATH_STEM",
    "title": "Reading the Path Stem",
    "level": 3,
    "concepts": [
      "import",
      "print",
      "pathlib",
      "stem",
      "path"
    ],
    "reading_goal": "Understand the flow of how `Path.stem` returns the filename without the file extension.",
    "code": "from pathlib import Path\npath = Path(\"data/cards.json\")\nprint(path.stem)",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "cards.json",
      "cards",
      ".json",
      "data"
    ],
    "answer": "cards",
    "explanation": "The name of the file in `cards.json`, excluding the file extension, is `cards`. The correct answer is determined by whether the code is reading or writing to the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attributes point to.",
    "project_context": "It is linked to code that extracts version numbers or identifiers from file names.",
    "side_card_ids": [
      "PYF95_A4_PATHLIB_PATH"
    ]
  },
  {
    "id": "PYF95_A4_FILE_014_PATH_PARENT",
    "title": "Reading the Parent Path",
    "level": 3,
    "concepts": [
      "import",
      "print",
      "pathlib",
      "parent",
      "path"
    ],
    "reading_goal": "Read the flow where `Path.parent` points to the parent directory containing the file. In particular, verify the file mode, the scope of the `with` block, the variable where the read string is stored, the path component pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "from pathlib import Path\npath = Path(\"data/cards.json\")\nprint(path.parent)",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "cards.json",
      "data",
      ".json",
      "parent"
    ],
    "answer": "data",
    "explanation": "The parent directory of `data/cards.json` is `data`. Choose the correct answer based on whether the code reads or writes to the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attributes point to.",
    "project_context": "This is necessary to read the output folder and input file from separate locations.",
    "side_card_ids": [
      "PYF95_A4_PATHLIB_PATH"
    ]
  },
  {
    "id": "PYF95_A4_FILE_015_PATH_READ_TEXT",
    "title": "Path.read_text Flow",
    "level": 3,
    "concepts": [
      "comment",
      "import",
      "print",
      "pathlib",
      "read_text",
      "encoding"
    ],
    "reading_goal": "Understand the process by which `Path.read_text` reads the contents of a file as a string and stores it in a variable.",
    "code": "# memo.txt contents: hello\nfrom pathlib import Path\ntext = Path(\"memo.txt\").read_text(encoding=\"utf-8\")\nprint(text)",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "memo.txt",
      "hello",
      "Path",
      "utf-8"
    ],
    "answer": "hello",
    "explanation": "`read_text` reads the content \"hello\" from `memo.txt`. The correct answer is determined by whether the code reads or writes the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attributes point to.",
    "project_context": "The function `read_text` is frequently used in code that simply reads small text files.",
    "side_card_ids": [
      "PYF95_A4_PATH_READ_WRITE"
    ]
  },
  {
    "id": "PYF95_A4_FILE_016_PATH_WRITE_TEXT",
    "title": "Path.write_text Flow",
    "level": 3,
    "concepts": [
      "import",
      "print",
      "pathlib",
      "write_text",
      "file"
    ],
    "reading_goal": "Understand that `Path.write_text` writes a string to a file and is distinct from screen output.",
    "code": "from pathlib import Path\nPath(\"out.txt\").write_text(\"hi\", encoding=\"utf-8\")\nprint(\"done\")",
    "question": "What value is displayed on the screen after executing `Path.write_text`?",
    "question_type": "output_prediction",
    "choices": [
      "hi",
      "done",
      "out.txt",
      "None"
    ],
    "answer": "done",
    "explanation": "Path('out.txt').write_text('hi', encoding='utf-8') saves the string \"hi\" to the file. This call itself does not print \"hi\" to the screen; only the subsequent print('done') appears on the screen. Therefore, you must distinguish between the file contents and the console output. `write_text` modifies the file, while `print` is responsible for console output.",
    "project_context": "`write_text` can be used in code that saves short result files.",
    "side_card_ids": [
      "PYF95_A4_PATH_READ_WRITE"
    ]
  },
  {
    "id": "PYF95_A4_FILE_017_TRY_EXCEPT_VALUE_ERROR",
    "title": "Handling int Conversion Errors",
    "level": 3,
    "concepts": [
      "try_except",
      "print",
      "try",
      "except",
      "ValueError"
    ],
    "reading_goal": "Understand the flow of execution: if a ValueError occurs within the `try` block, control flows to the `except ValueError` block.",
    "code": "try:\n    number = int(\"x\")\n    print(number)\nexcept ValueError:\n    print(\"bad\")",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "'x' is printed",
      "bad",
      "\"number\" is printed",
      "Nothing is printed"
    ],
    "answer": "bad",
    "explanation": "int('x') cannot create an integer and raises ValueError. Execution skips the following print(number), enters the matching except ValueError block, and displays bad without quotation marks.",
    "project_context": "Code that converts user input into numbers must handle the possibility of failure.",
    "side_card_ids": [
      "PYF95_A4_TRY_EXCEPT",
      "PYF95_A4_EXCEPTION_TYPE"
    ]
  },
  {
    "id": "PYF95_A4_FILE_018_TRY_EXCEPT_NO_ERROR",
    "title": "Skip the `except` block if there are no errors",
    "level": 3,
    "concepts": [
      "try_except",
      "print",
      "try",
      "except",
      "int"
    ],
    "reading_goal": "Understand the flow where the `except` block is not executed if the `try` block executes normally. In particular, verify the file mode, the scope of the `with` block, the variable where the read string is stored, the path components pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "try:\n    number = int(\"3\")\n    print(number)\nexcept ValueError:\n    print(\"bad\")",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "The result is \"bad.\"",
      "3",
      "The result is None.",
      "The result is a ValueError."
    ],
    "answer": "3",
    "explanation": "Since int('3') succeeds, 3 is printed and the except block is not executed. open and with are ways to open a file, while read and write are operations for reading or writing content. The correct answer is determined by whether the code reads or writes to the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attribute points to. Therefore, the answer is 3.",
    "project_context": "It is important to get into the habit of distinguishing between normal and error flows.",
    "side_card_ids": [
      "PYF95_A4_TRY_EXCEPT"
    ]
  },
  {
    "id": "PYF95_A4_FILE_019_ZERO_DIVISION",
    "title": "Handling ZeroDivisionError",
    "level": 3,
    "concepts": [
      "try_except",
      "print",
      "try",
      "except",
      "ZeroDivisionError"
    ],
    "reading_goal": "When a division-by-zero error occurs, examine the flow of execution leading to the execution of the corresponding `except` block. In particular, check the file mode, the scope of the `with` block, the variable in which the read string is stored, the path components pointed to by `Path`, and whether the `try` or `except` block is executed.",
    "code": "try:\n    print(10 / 0)\nexcept ZeroDivisionError:\n    print(\"zero\")",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "10",
      "0",
      "zero",
      "None"
    ],
    "answer": "zero",
    "explanation": "10 / 0 raises a ZeroDivisionError, and \"zero\" is printed. This problem is a Level 3 exercise in reading the execution flow of file handling, path handling, and exception handling. The correct answer is selected based on whether the code reads or writes to the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attribute points to.",
    "project_context": "In the calculation code, you must check for the possibility of a division error.",
    "side_card_ids": [
      "PYF95_A4_TRY_EXCEPT",
      "PYF95_A4_EXCEPTION_TYPE"
    ]
  },
  {
    "id": "PYF95_A4_FILE_020_EXCEPTION_AS_E",
    "title": "Naming Error Objects",
    "level": 3,
    "concepts": [
      "try_except",
      "print",
      "except",
      "exception",
      "as"
    ],
    "reading_goal": "In `except ValueError as e`, `e` is the name of the error object, and the code within the block reads the flow of execution. In particular, it checks the file mode, the scope of the `with` block, the variable where the read string is stored, the path components pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "try:\n    int(\"x\")\nexcept ValueError as e:\n    print(\"error\")",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "The result is 'x'.",
      "error",
      "The result is 'e'.",
      "The result is a ValueError."
    ],
    "answer": "error",
    "explanation": "int('x') raises ValueError, which except ValueError as e catches and binds to e. This example does not use e and simply prints error; real code may inspect e for safe diagnostics.",
    "project_context": "The name \"exception\" frequently appears in code that logs error messages.",
    "side_card_ids": [
      "PYF95_A4_EXCEPTION_TYPE"
    ]
  },
  {
    "id": "PYF95_A4_FILE_021_FINALLY_RUNS",
    "title": "\"finally\" is executed last",
    "level": 3,
    "concepts": [
      "try_except",
      "print",
      "try",
      "except",
      "finally"
    ],
    "reading_goal": "Even if there are no errors, the finally block is executed last. In particular, check the file mode, the scope of the with block, the variable where the read string is stored, the path components pointed to by Path, and which of the try and except blocks is executed.",
    "code": "try:\n    print(\"try\")\nexcept ValueError:\n    print(\"except\")\nfinally:\n    print(\"finally\")",
    "question": "Which one is in the correct output order?",
    "question_type": "output_prediction",
    "choices": [
      "\"except\" followed by \"finally\"",
      "try, then finally",
      "\"finally\" only",
      "try followed by except"
    ],
    "answer": "try, then finally",
    "explanation": "The `try` block executes normally, the `except` block is skipped, but the `finally` block is executed. This problem is a Level 3 exercise in reading the execution flow of file handling, path handling, and exception handling. The correct answer is selected based on whether the code reads or writes to a file, whether an error occurs, whether the `except` block is executed, and what the `Path` attribute points to.",
    "project_context": "Cleanup operations or termination logs can be placed in the `finally` block.",
    "side_card_ids": [
      "PYF95_A4_TRY_EXCEPT"
    ]
  },
  {
    "id": "PYF95_A4_FILE_022_SAFE_INT_FUNCTION",
    "title": "Reading the `safe_int` Function",
    "level": 3,
    "concepts": [
      "def",
      "function",
      "try_except",
      "print",
      "try",
      "except",
      "return",
      "ValueError"
    ],
    "reading_goal": "If an error occurs within a `try/except` block inside a function, observe how the return value from the `except` block is passed back to the function’s outer scope. In particular, check the file mode, the scope of the `with` block, the variable where the read string is stored, the path components pointed to by `Path`, and which of the `try` or `except` blocks is executed.",
    "code": "def safe_int(text):\n    try:\n        return int(text)\n    except ValueError:\n        return 0\n\nprint(safe_int(\"x\"))",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "The result is 'x'.",
      "0",
      "The result is None.",
      "The result is a ValueError."
    ],
    "answer": "0",
    "explanation": "int('x') raises ValueError, so the except branch returns 0 and the outer print displays it. This default cannot distinguish invalid input from a valid input of '0'. If callers need that distinction, define a clearer contract using None, a result object, or the original exception.",
    "project_context": "Input conversion functions can be designed to return a default value in case of failure.",
    "side_card_ids": [
      "PYF95_A4_TRY_EXCEPT"
    ]
  },
  {
    "id": "PYF95_A4_FILE_023_SAFE_INT_SUCCESS",
    "title": "safe_int Correct Conversion",
    "level": 3,
    "concepts": [
      "def",
      "function",
      "try_except",
      "print",
      "try",
      "except",
      "return",
      "int"
    ],
    "reading_goal": "Understand the flow where, if the `try` block succeeds, the `except` block is not executed and the return value from the `try` block is used.",
    "code": "def safe_int(text):\n    try:\n        return int(text)\n    except ValueError:\n        return 0\n\nprint(safe_int(\"7\"))",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "The result is 0.",
      "7",
      "The result is None.",
      "The result is a ValueError."
    ],
    "answer": "7",
    "explanation": "int('7') succeeds and returns integer 7 from the try block, so the except branch is skipped. The outer print displays the returned 7.",
    "project_context": "For a function that can result in either success or failure, the two paths must be tracked separately.",
    "side_card_ids": [
      "PYF95_A4_TRY_EXCEPT"
    ]
  },
  {
    "id": "PYF95_A4_FILE_024_FILE_NOT_FOUND_CONCEPT",
    "title": "Types of \"File Not Found\" Errors",
    "level": 3,
    "concepts": [
      "FileNotFoundError",
      "open",
      "exception"
    ],
    "reading_goal": "Determine, at a conceptual level, the types of errors that can occur when a file does not exist. In particular, verify the file mode, the scope of the `with` block, the variable in which the read string is stored, the path components pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "filename = \"missing.txt\"",
    "question": "What is the name of the error you often encounter when trying to read a file that doesn't exist?",
    "question_type": "concept_reading",
    "choices": [
      "ValueError",
      "FileNotFoundError",
      "ZeroDivisionError",
      "TypeError"
    ],
    "answer": "FileNotFoundError",
    "explanation": "A `FileNotFoundError` often occurs when attempting to open a nonexistent file. This exercise in Level 3 focuses on understanding the execution flow of file handling, path handling, and exception handling. The correct answer is determined based on whether the code reads or writes to the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attributes point to.",
    "project_context": "In file input code, it is important to verify that the path is correct and that the file exists.",
    "side_card_ids": [
      "PYF95_A4_EXCEPTION_TYPE"
    ]
  },
  {
    "id": "PYF95_A4_FILE_025_TRY_FILE_NOT_FOUND",
    "title": "Handling \"File Not Found\" Exceptions",
    "level": 3,
    "concepts": [
      "try_except",
      "print",
      "try",
      "except",
      "FileNotFoundError"
    ],
    "reading_goal": "Examine the flow of the \"Failed to open file\" exception as it is caught by the `FileNotFoundError` `except` block.",
    "code": "try:\n    with open(\"missing.txt\", \"r\", encoding=\"utf-8\") as f:\n        f.read()\nexcept FileNotFoundError:\n    print(\"missing\")",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "\"missing.txt\" is displayed",
      "missing",
      "\"r\" is printed",
      "Nothing is printed"
    ],
    "answer": "missing",
    "explanation": "Assuming missing.txt is absent from the current working directory, open raises FileNotFoundError and the except block prints missing. This handler does not catch a permission error or every other open failure. If the file exists, with closes it after reading.",
    "project_context": "Apps that read external files must also have a flow in place for when the file does not exist.",
    "side_card_ids": [
      "PYF95_A4_TRY_EXCEPT",
      "PYF95_A4_EXCEPTION_TYPE"
    ]
  },
  {
    "id": "PYF95_A4_FILE_026_JSON_LOADS_STRING",
    "title": "Parsing JSON Strings",
    "level": 3,
    "concepts": [
      "import",
      "print",
      "json",
      "json.loads",
      "dict"
    ],
    "reading_goal": "Read through the process where a JSON string is converted into a dictionary using `json.loads`, and values are retrieved using their keys.",
    "code": "import json\ntext = \"{\\\"name\\\": \\\"Mina\\\"}\"\ndata = json.loads(text)\nprint(data[\"name\"])",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "name",
      "Mina",
      "text",
      "json"
    ],
    "answer": "Mina",
    "explanation": "The data following `loads` is a dictionary, and the value of `name` is `Mina`. The correct answer is determined by whether the code reads or writes to the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attributes point to.",
    "project_context": "Configuration files and API responses are converted from JSON strings to dictionaries for use.",
    "side_card_ids": [
      "PYF95_A4_JSON_FILE_FLOW"
    ]
  },
  {
    "id": "PYF95_A4_FILE_027_JSON_DUMPS_STRING",
    "title": "Converting a dict to a JSON string",
    "level": 3,
    "concepts": [
      "import",
      "print",
      "json",
      "json.dumps",
      "str"
    ],
    "reading_goal": "Examine the flow of `json.dumps` as it converts a Python dictionary into a JSON-formatted string.",
    "code": "import json\ndata = {\"name\": \"Mina\"}\ntext = json.dumps(data, ensure_ascii=False)\nprint(type(text).__name__)",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "dict",
      "str",
      "list",
      "json"
    ],
    "answer": "str",
    "explanation": "The text returned by `json.dumps` is a string. This problem is a Level 3 exercise in reading the execution flow of file handling, path handling, and exception handling. The correct answer is determined by whether the code reads from or writes to a file, whether an error occurs, whether the `except` block is executed, and what the `Path` attribute points to. Therefore, the output is ‘str’.",
    "project_context": "It may be necessary to convert the data into a string before saving it to a file or transmitting it.",
    "side_card_ids": [
      "PYF95_A4_JSON_FILE_FLOW"
    ]
  },
  {
    "id": "PYF95_A4_FILE_028_JSON_FILE_FLOW",
    "title": "Flow for Reading JSON from a File",
    "level": 3,
    "concepts": [
      "comment",
      "import",
      "print",
      "open",
      "read",
      "json.loads",
      "dict"
    ],
    "reading_goal": "We’ll examine the multi-step process of reading a string from a file, parsing it into JSON, and extracting the value as a dictionary key. In particular, we’ll look at the file mode, the scope of the `with` block, the variable where the read string is stored, the path components pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "# config.json contents: {\"level\": 3}\nimport json\nwith open(\"config.json\", \"r\", encoding=\"utf-8\") as f:\n    text = f.read()\ndata = json.loads(text)\nprint(data[\"level\"])",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "key name: level",
      "The value 3 stored under the level key",
      "The file name is config.json",
      "The variable name \"text\""
    ],
    "answer": "The value 3 stored under the level key",
    "explanation": "f.read() obtains the JSON document as text and json.loads(text) parses it into {'level': 3}. data['level'] retrieves the integer value 3; it is not a count of items.",
    "project_context": "Code for loading configuration files is a classic example of a sequence that involves reading a file followed by parsing JSON.",
    "side_card_ids": [
      "PYF95_A4_OPEN_READ",
      "PYF95_A4_JSON_FILE_FLOW"
    ]
  },
  {
    "id": "PYF95_A4_FILE_029_PATH_JSON_FLOW",
    "title": "Reading Path and JSON Together",
    "level": 3,
    "concepts": [
      "comment",
      "import",
      "print",
      "pathlib",
      "read_text",
      "json.loads",
      "bool"
    ],
    "reading_goal": "Understand the process by which a JSON string read using `Path.read_text` is converted into a dictionary and used as a boolean value.",
    "code": "# config.json contents: {\"ok\": true}\nimport json\nfrom pathlib import Path\ntext = Path(\"config.json\").read_text(encoding=\"utf-8\")\ndata = json.loads(text)\nprint(data[\"ok\"])",
    "question": "What is the output?",
    "question_type": "output_prediction",
    "choices": [
      "False",
      "True",
      "ok",
      "true"
    ],
    "answer": "True",
    "explanation": "\"true\" in JSON is parsed as `True` in a Python dictionary. pathlib’s `Path` allows you to treat paths as building blocks, while `try/except` separates the normal flow of code from the error flow for code that may fail. The correct answer is selected based on whether the code reads or writes to the file, whether an error occurs, whether the `except` block is executed, and what the `Path` attributes point to.",
    "project_context": "To load short configuration files, you can use pathlib and json together.",
    "side_card_ids": [
      "PYF95_A4_PATH_READ_WRITE",
      "PYF95_A4_JSON_FILE_FLOW"
    ]
  },
  {
    "id": "PYF95_A4_FILE_030_CHOOSE_WITH_REASON",
    "title": "Choosing the Reason for Using \"with\"",
    "level": 3,
    "concepts": [
      "with",
      "open",
      "file"
    ],
    "reading_goal": "Assess the purpose of the `with open` structure at a conceptual level. In particular, examine the file mode, the scope of the `with` block, the variable in which the read string is stored, the path component pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "with open(\"memo.txt\", \"r\", encoding=\"utf-8\") as f:\n    text = f.read()",
    "question": "Which of the following is the most appropriate reason for using \"with\"?",
    "question_type": "concept_reading",
    "choices": [
      "To sort the numbers",
      "To open and close files safely",
      "To convert a string to JSON",
      "To end the loop"
    ],
    "answer": "To open and close files safely",
    "explanation": "with uses the context manager returned by open and closes the file when the block exits, whether normally or through an exception. This reduces forgotten close calls. It does not itself handle read errors, so add appropriate exception handling separately when needed.",
    "project_context": "It is necessary to understand the resource cleanup flow in the file processing code.",
    "side_card_ids": [
      "PYF95_A4_WITH_FILE"
    ]
  },
  {
    "id": "PYF95_A4_FILE_031_CHOOSE_TRY_REASON",
    "title": "Reasons for Using try/except",
    "level": 3,
    "concepts": [
      "try_except",
      "try",
      "except",
      "error"
    ],
    "reading_goal": "At a conceptual level, explain that the `try/except` structure is used to handle code that may fail. In particular, examine the file mode, the scope of the `with` block, the variable that stores the read string, the path components pointed to by `Path`, and which of the `try` or `except` blocks is executed.",
    "code": "try:\n    number = int(text)\nexcept ValueError:\n    number = 0",
    "question": "Which of the following is the most appropriate reason for using try/except?",
    "question_type": "concept_reading",
    "choices": [
      "To rename a file",
      "To define a fallback for the expected ValueError",
      "To sort the list",
      "To reverse a string"
    ],
    "answer": "To define a fallback for the expected ValueError",
    "explanation": "try/except defines a separate path for an expected exception. This code catches only ValueError from int(text) and uses 0. Catching Exception too broadly can hide programming defects, so catch only errors the code can meaningfully handle.",
    "project_context": "Exception handling is necessary in areas where errors are likely to occur, such as input conversion, file reading, and JSON parsing.",
    "side_card_ids": [
      "PYF95_A4_TRY_EXCEPT"
    ]
  },
  {
    "id": "PYF95_A4_FILE_032_CHOOSE_PATHLIB_REASON",
    "title": "Reasons for Choosing pathlib",
    "level": 3,
    "concepts": [
      "import",
      "pathlib",
      "Path",
      "path"
    ],
    "reading_goal": "At a conceptual level, we determine that `Path` is a tool for handling file paths in a structured manner. In particular, we examine the file mode, the scope of the `with` block, the variable where the read string is stored, the path components pointed to by `Path`, and which of the `try` and `except` blocks is executed.",
    "code": "from pathlib import Path\npath = Path(\"data\") / \"memo.txt\"",
    "question": "Which of the following is the most appropriate reason for using pathlib Path?",
    "question_type": "concept_reading",
    "choices": [
      "To eliminate errors at all costs",
      "To combine path components clearly using operating-system rules",
      "To automatically translate the contents of a file",
      "To stop the loop"
    ],
    "answer": "To combine path components clearly using operating-system rules",
    "explanation": "Path('data') / 'memo.txt' combines components without manually concatenating path separators, improving clarity and portability. It does not automatically guarantee that the file exists, that access is permitted, or that a user-supplied path is secure.",
    "project_context": "As projects grow larger, the ability to safely parse path combinations and filenames becomes essential.",
    "side_card_ids": [
      "PYF95_A4_PATHLIB_PATH"
    ]
  }
]
